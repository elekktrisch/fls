---
topic: AlpenFlight PRD — FLS rewrite (rebuild 2), migration-first glider club SaaS
updated: 2026-08-29T15:23
---

- (event) Intent: create. No prior PRD run existed. Workspace bound at _bmad-output/planning-artifacts/prds/prd-fls-2026-08-25.
- (event) Upstream inputs read: product brief + addendum (brief-fls-2026-08-24), UX EXPERIENCE.md + DESIGN.md + UX memlog (ux-fls-2026-08-24), docs/modernization/01-current-state.md feature inventory, risks R1-R14, open behavioural questions Q-B1..Q-B16.
- (decision) Stakes: INTERNAL TOOL first, with promotion to a real launch later, once the closed user group is satisfied. Consequence: internal depth now, but no decision may block the later promotion. Tenant isolation, personal-data duty and audit stay at full rigor because a closed group still holds member medical data. Enterprise sections (ROI, approvals, change management) stay out.
- (decision) Working mode: FAST PATH. Roman chose it. I batch the gaps, draft the full PRD, and tag every inference with [ASSUMPTION].
- (decision) SCOPE CUT - no subscription billing in v1. C29 trial expiry, C31 subscription lifecycle states and C33 hosted checkout all move to v2. Reason: the first release serves a closed user group that pays outside the product. The architecture must still reserve the club lifecycle so the later promotion needs no rewrite. Supersedes the brief's assumption that the PRD confirms the price shape.
- (decision) MIGRATION FAILURE PATH decided (this closes the UX Flow 3 step 6 open item). When a verified invoice line does not reproduce to the cent, AlpenFlight reports every mismatch line by line, legacy against reproduced. The club administrator accepts each mismatch with a recorded reason, and then commits. Accepted mismatches stay in an open list inside the app until somebody resolves them. It never blocks, and it never needs the operator.
- (decision) PARITY SCOPE - every legacy feature ports, with NO exception. Roman rejected the recommended cut of the three system-admin surfaces. Language translations CRUD, system data CRUD and system logs stay club-reachable screens. C10 stays literal.
- (decision) INTEGRATIONS - the delivery mail export ports in v1 because it feeds club accounting. The OGN inbound path is DEFERRED. AlpenFlight publishes no OGN ingestion in v1, so risk R9 (an external writer that bypasses every domain invariant) leaves the first release. The closed group logs flights by hand.
- (decision) OFFLINE SCOPE - the flight write path plus a read of today. The flight form, the NOW stamp and the landing stamp work with no network. The device also holds today's flights, the airborne board and the 13 catalogs for offline reading. Reservations, members, accounting and reports need a connection.
- (decision) OFFLINE CONFLICT RULE confirmed (closes the UX assumption). The offline device queues its edits and marks them unsent. On reconnect it applies them when no conflicting change arrived, and shows a conflict for a person to resolve when one did. An offline device never blocks anybody else, and it never takes a hold.
- (decision) LANGUAGES - German and English in v1. French and Italian follow when a club asks. Note: legacy serves translations from the database; the v1 set may be smaller than the legacy set, which is a recorded reduction to check against the legacy translation table.
- (decision) DATA RETENTION - a club administrator can export the whole club at any time in a club-readable form. On a written request the operator deletes the club and everything in it within a stated period. Data-subject requests (see, correct, delete) run through the club administrator, not through the operator.
- (change) TENSION RECORDED - C10 'every feature, no exception' and the OGN deferral conflict. The OGN inbound path and the aircraft database sync job are legacy features. The OGN deferral becomes the ONE explicit recorded exception to C10, so the parity promise stays honest.
- (decision) VOCABULARY FIX (ASD-STE100 rule 4, one word one meaning). The brief uses 'operator' for two different people: the person at the airfield, and Roman who builds and hosts the product. The PRD splits them. FLIGHT OPERATOR = the person at the airfield who logs flights. SUPPLIER = the person who builds, hosts and supports AlpenFlight. Every downstream document must keep this split.
- (decision) VOCABULARY - HOLD and LOCK are different things and never interchange. HOLD is the concurrency claim on the flight edit form; it expires and a person can take it over. LOCK is the flight process state that the time gate sets; it makes the flight read-only.
- (change) NUANCE on the integrations decision. The external Proffix sync does NOT read the delivery mail export. It POLLS /api/v1/deliveries/* GETs (current-state section 4). So Proffix parity means AlpenFlight serves a compatible deliveries API, which the delivery CRUD parity already covers. The delivery mail export is a separate feature that emails a spreadsheet. Both stay in v1. Only OGN leaves.
- (override) SEED SACRED COW BROKEN, deliberately. docs/modernization/00-seed.md lists OGN integration as a sacred cow: 'the new system must accept the same inbound contract or the OGN side must be replaced too'. Roman deferred OGN from v1 on 2026-08-25. The PRD records this as the ONE explicit exception to feature parity, and states the consequence: a club that uses OGN today keeps its legacy system for that path, or logs by hand.
- (event) DRAFT COMPLETE (fast path). prd.md holds 14 sections, 14 features in section 4, FR-1 to FR-87, UJ-1 to UJ-6, 27 glossary terms, 9 success metrics with 4 counter-metrics, 8 risks RK-1 to RK-8, a constraint disposition table and 8 open questions. addendum.md holds the rejected alternatives, the full C1-C34 disposition, the Q-B1..Q-B16 blocking map, the broken sacred cow record and the vocabulary decisions.
- (decision) Roman reads the draft before finalize. Finalize is paused at his request. Reviewer menu chosen for the later pass: all four - the quality rubric walker, the adversarial lens, the edge-case hunter and the verification-gap lens.
- (change) GLOSSARY MOVED OUT of prd.md at Roman's request. New companion domain-model.md holds the ubiquitous language (7 term groups), the domain model (aggregates, the flight aggregate, the user-person-club triad, the rules engine pipeline), the legacy-to-target mapping for all 56 entities plus the enums, and the mistranslation evidence. prd.md section 3 now points at it.
- (event) GERMAN IS THE SOURCE LANGUAGE - confirmed. flsweb/server/mock-data/translations.json holds 330 interface keys with their German source text, and flsserver/database/FLSTest/3 insert/ seeds the domain values in German. Every English identifier in flsserver is a translation. This file is the evidence base for every rename.
- (decision) NAMING VERDICT - 38 of 56 legacy entities keep their name, 18 rename. That is 68 percent kept, close to Roman's 70 percent estimate. The renames correct false friends (Taxe=fee not tax), Germanisms (Immatrikulation=registration), wrong aviation terms (Observer=supervising pilot), junction-table names leaking into the domain (PersonClub=ClubMembership) and the Start/Takeoff/Launch collision (StartType=LaunchMethod).
- (event) FINDING - AccountingRuleFilter holds no word for 'filter' in the German. The interface says Verrechnungs-Regeln = charging rules. Somebody named the class after its internal behaviour and the internal name reached the screen. This is part of why the UX run found the accounting screens hard to understand.
- (event) FINDING - four enums name the same three flight categories (AircraftType entity, AircraftType enum, FlightAircraftTypeValue, FlightCategory) and Flight.FlightAircraftType is a flight category, not an aircraft type. Same defect class as risk hotspot R5.
- (event) FINDING - the nine ChargingRule Matched* condition fields are all delimited STRINGS holding business keys (registrations, member numbers, flight-type codes), not foreign keys, each paired with a UseRuleForAllExceptListed inversion flag. Three consequences: a renamed registration silently breaks a rule; the migration must carry the strings intact for parity; and nothing stops another club's member number entering the list, which is a second path past the club boundary alongside Q-B8 and Q-B9.
- (event) FINDING - two enums for one location type with the same ids and different names (LocationType 17 members vs AirfieldLocationType 6 members; GrasRunway is misspelled). Also found: AIRBORN and INVOICE_RECEIPIENT misspelled in translations, DaecIndex vs DEAC_INDEX split, Licence vs License mixed.
- (event) DEFECT - AccountingRuleFilterFactory.cs lines 27-30 hardcode four real club member numbers with real first names (Marc, Hermann, Heiri, Fredi) in the legacy source, in a public repository. The rewrite must not carry them.
- (override) MAJOR FINDING, CONTRADICTS FR-47. The legacy rules engine order is NOT user-configurable. DeliveryItemRulesEngine.Run() hardcodes nine phases in a fixed sequence by rule kind (no-landing-fee, flight time, engine time, instructor fee, tow flight time, extra fuel fee, launch fee, landing fee, association fee). Within a phase RulesEngine.Run() is a bare foreach over the query result - it never sorts, and ChargingRule.SortOrder exists on the entity but nothing reads it. Consequences: Q-B6 is partly answered (across kinds the phase sequence decides; within a kind the legacy has no defined answer and the result depends on database row order); FR-47 drag-to-reorder is a BEHAVIOUR CHANGE, not a port; and the FR-51 reorder guard may have nothing to guard. Recommendation to Roman: keep the ordered list as a read-only display of the nine phases, keep the dry run FR-50, drop drag-to-reorder and the reorder guard from v1.
- (event) TWO NAMING DECISIONS RAISED TO ROMAN, not applied. (1) 'flight operator' is a mistranslation of Segelflugleiter = duty flight leader; the word 'operator' also means the AOC holder in aviation. It appears throughout prd.md, EXPERIENCE.md and DESIGN.md. (2) 'Delivery' means invoice draft (German Rechnung / Buchungszeile), probably carried over from Proffix Lieferung; renaming the domain term is safe but the external Proffix sync polls /api/v1/deliveries so the wire path must stay.
- (override) CORRECTION, Roman was right. My rename DeliveryCreationTest -> BillingParityTest was wrong twice. (1) It collided with our own vocabulary: this project already uses 'parity' for legacy-vs-AlpenFlight invoice equality, a one-time migration proof. This entity is the club's rules against the club's OWN expectation, over years. One word, two meanings - it broke naming rule 1 in the same document that states the rule. (2) It described the mechanism and missed the job.
- (event) EVIDENCE, verified in DeliveryService.RunDeliveryCreationTest() and flsweb/src/masterdata/deliveryCreationTests/. The UI has a 'Create test delivery' button that runs the engine NOW and writes the result straight into the expectation field - one click re-baselines. The expectation is a hand-editable JSON textarea. Nine Ignore flags say which differences do not count. Run test reports Success or Failure naming each mismatched field. The screen stacks two tables: expected lines above, last actual lines below. Run all shows a green/red progress bar. That is a golden-master workflow: change a rule, run all, read what failed, decide whether the difference is intended, then bless it.
- (decision) RENAMED DeliveryCreationTest -> BillingExpectation (not BillingParityTest). Plus a new term 'Re-baseline' for the bless action. Rationale: the difference IS the point and is not a defect to drive to zero; 'test' also misleads a treasurer, since nothing here is a developer test.
- (event) GAP FOUND that changes the PRD. The legacy compares current output against a STORED expectation. It cannot show what a change WOULD do before you make it - you must save the rule edit first, then run, then read failure messages. There is no before-and-after view anywhere in the legacy accounting screens. This is why FR-50 dry run and the change preview are worth building even though section 7.3 removes drag-to-reorder.
- (change) FR-54 REWRITTEN from 'delivery creation test harness' to 'Billing expectations', with re-baselining as an explicit consequence and a note that it is NOT a parity test. New FR-54a 'Change preview' carries the before-and-after capability the legacy lacks, which was previously buried inside FR-51's reorder guard. Also fixed section 6.1 in-scope wording and the Admin destination in the IA table.
- (decision) MIGRATION LINK IMPROVED, not lost. The recorded legacy invoices that FR-74 verifies at migration are exactly the right seed for a club's first billing expectations. Seed the expectations from the migration, and the same suite that proved the move guards every rule edit afterwards. One mechanism, two jobs - the same shape the UX run found for the dry run.
- (change) CONSOLIDATION at Roman's request: target state only, short and precise, no fancy wording. Three documents became two. prd.md 11931 -> 6241 words. domain-model.md 7289 -> 3798 words. addendum.md 2935 words removed entirely. Total 22155 -> 10039 words, a 55 percent cut with no requirement lost. FR-1..FR-87 plus FR-54a all survive, and the numbering is unchanged so downstream references stay valid.
- (change) WHAT THE CUT REMOVED. Rhetorical emphasis and narrative climax lines. Caution and Warning blocks written as prose. Every 'the legacy does X, which is why we do Y' justification. The rejected-alternative option tables (addendum section 1). The C1-C34 rebuild-1 constraint disposition - redundant, because a kept constraint is already an FR, a deferred one is already in scope, and an architecture one is already an open question. The mistranslation evidence with German citations - the conclusions survive in the mapping tables. The 'findings that change the PRD' sections - their conclusions are now requirements or open questions.
- (change) WHAT MOVED. The Q-B1..Q-B16 blocking map moved from addendum section 3 into prd.md section 12, so every open item now lives in one place. The vocabulary decisions moved into domain-model.md section 1 naming rules. The legacy defects not to carry forward moved into domain-model.md section 4.6.
- (event) ARCHIVE. The three superseded documents are kept at _bmad-output/planning-artifacts/prds/prd-fls-2026-08-25/.archive/ - prd-verbose, domain-model-verbose and addendum. Nothing was destroyed. This memlog stays the canonical record of every decision and its reason.
- (event) bmad-review run on prd.md: adversarial, edge-case-hunter, structure, prose. 15 adversarial gaps, 22 unhandled paths, 10 structure and 15 prose rows. All applied. Added FR-88 user management and FR-89 club provisioning. Section 8 became NFR-1 to NFR-13. Sections 5 and 6 merged into section 5. Added section 13 traceability. New blocking open questions 15 to 22, new Q-B17 to Q-B26, new risks RK-9 and RK-10. Open questions 11 to 14 stay reserved for domain-model.md section 5.
- (decision) Legacy multi-tenancy read directly from flsserver. Three mechanisms: ClubId column on 18 tables, OwnerId plus OwnershipType on 36 tables, no tenant column on 23. All 18 ClubId tables also carry OwnerId with no constraint between them. Found two cross-club exposures: AuditLogsController serves every club to any authenticated user and AuditLogs carries no club column; DashboardService filters flights by person and never by club. FlightService.GetFlights(predicate) applies no club filter. FlightService:483 ignores OwnershipType. Recorded all in domain-model.md 4.6. Added questions 23 to 26 and risks RK-11, RK-12. FR-8 marked a behaviour change, not parity. FR-57 verified as parity: the report service does filter by club. Demo club requested by the operator: removed the anonymous demonstration sandbox from the Never list, added it to Deferred to v2 with a v1 architecture reservation as a club kind, added questions 27 to 29 and RK-13.
- (decision) Demo club decided by the operator. Reserve the club kind and the club lifecycle in v1, build the demo surface in v2. Shape: one demo club per visitor with an expiry and a reaper, reached through an email-gated link. Not public sign-up, which stays v2. Question 27 closed. Questions 28 and 29 stay open: which shared entity a demo user may create, and the expiry, rate limit, storage cap, and content rule.
- (decision) Subscription model changed by the operator from a temporary trial to freemium. A free account carries one club with a maximum of two aircraft, for an unlimited time. Housekeeping is by inactivity, not by age: reminder emails, then the deletion of everything of that club when no user signs in for a defined period. Removed 'A free tier' from the Never list and replaced it with 'A temporary trial'. Still v2; v1 reserves the club kind, the plan, and the aircraft limit. Added questions 30 to 33 and risks RK-14, RK-15, RK-16. Flagged two hazards: a Swiss gliding club is inactive October to March, so a period below seven months deletes an active club in winter; and Swiss law obliges a club to keep business records ten years, which the deletion removes. Also flagged that two aircraft excludes any club that flies aerotow, and that the free plan overlaps the demo club.
- (decision) Free plan and demonstration merged into one thing by the operator. Questions 27 and 33 closed. Club lifecycle decided: Active, then Locked after one month with no sign-in, then Dormant at a prolongation ceiling of two years. Monthly warning mail carries a prolong control and the FR-82 export. A dormant club is soft-deleted and kept ten years for the Swiss business-record duty; never a hard delete. A paying club has no inactivity rule. The free plan needs public sign-up, so the two v2 items ship together. Still v2; v1 reserves the club kind, the plan, the aircraft limit and the three lifecycle states. Questions 31, 32 and 34 carry what the merge left open: the locked-state semantics and what prolongs a club, the scope of the ten-year retention, and whether a new free club gets seeded demonstration data. Question 34 assigned to bmad-architecture per the operator.
