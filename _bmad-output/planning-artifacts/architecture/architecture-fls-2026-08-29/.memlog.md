---
scope: AlpenFlight - full-system architecture spine for rebuild 2 of the Flight Logging System
purpose: Fix the invariants that keep 14 feature groups and ~90 requirements from diverging. Decide tenancy enforcement, the offline write model, the rules-engine placement, and the stack.
altitude: initiative (whole product, above epics)
updated: 2026-08-29T17:36
---

- (event) Intent: create. Altitude: initiative (whole product). No prior architecture run existed. Workspace bound at _bmad-output/planning-artifacts/architecture/architecture-fls-2026-08-29.
- (event) Inputs read: prd.md (FR-1..FR-89, NFR-1..NFR-13, RK-1..RK-16, open questions 1..34 and Q-B1..Q-B26), domain-model.md, the PRD memlog, EXPERIENCE.md + DESIGN.md, brief.md + addendum.md, docs/modernization/ (01-current-state.md, legacy-migration-plan.md, form-validation-parity-audit.md), docs/legacy/server.md + web.md, CLAUDE.md.
- (decision) Working mode: COACHING PATH. Roman chose it. I lay out the alternatives for every load-bearing call and Roman decides. Rationale stays in this memlog, never in the spine.
- (constraint) No project-context.md exists, so the customize.toml persistent_facts glob loads nothing. CLAUDE.md plus docs/legacy/server.md and web.md carry the same standing facts. Read those instead.
- (constraint) CLAUDE.md marks two primary directives PROVISIONAL and assigns this run to accept or reject each: (1) working software over comprehensive documentation, (2) business logic in the domain, not the database. Directive 2 collides with PRD question 24, because row-level security places the tenant rule in the database. Must be settled in this run.
- (decision) PRD question 18 CLOSED by Roman. Platform floor is latest-minus-one for Chrome, Safari, Firefox and Edge. Binds NFR-12, the offline store choice, the service worker and the CSS feature set. Consequence: baseline-2024 web features are available, so no transpilation target below ES2022 and no polyfill budget.
- (version) Browser floor evidence needed at finalize: confirm the latest-minus-one versions of Chrome, Safari, Firefox and Edge as of the build start, and confirm that the chosen offline store API is available in all four.
- (event) PRD question 17 (data volume) has no live legacy instance to measure. Roman asked for a web-researched estimate. Evidence gathered: SFVS Jahresbericht 2024 gives 48 Segelfluggruppen and 1997 members, 1857 of them active; the 2024 OLC figures are 4183 declared flights, 152 flying days, 48 launch sites, 15620 flight hours, 823038 km, flown by 31 percent of the active members. Statista gives 514 registered gliders in Switzerland in 2023 and 241 motor gliders. Wikipedia gives Flugplatz Schaenis, the largest Swiss gliding airfield, about 11000 movements per year and over 150 active pilots. Segelfluggruppe Laegern is the largest club with over 100 active pilots. Segelfluggruppe Nidwalden reports 48 pilots, 11 aircraft and 1068 glider hours in 2023. The flsserver repository starts in February 2016, so the legacy history is about 10 years deep.
- (assumption) NATIONAL LAUNCH VOLUME estimated at 60000 to 70000 glider launches per year in Switzerland. Two independent derivations agree. Aircraft method: 514 gliders times about 130 launches per glider per year equals about 67000. OLC method: 4183 declared cross-country flights divided by an assumed 6 to 8 percent declaration rate equals about 60000. Confidence: medium. Nothing measured, both methods use a rule of thumb.
- (assumption) PER-CLUB VOLUME. The average club flies about 1250 glider launches per year (60000 divided by 48 clubs). The largest club flies about 3 to 4 times the average, because it holds over 100 active pilots against a mean of about 40. Swiss gliding is aerotow-dominant, and the legacy stores the tug flight as a second flight record, so one launch produces about 1.8 flight records. Result: average club about 2000 flight records per year, largest club about 7000. DESIGN TARGET 8000 flight records per club per year.
- (assumption) HISTORY DEPTH 10 years, design for 15. Evidence: the flsserver repository starts in February 2016. A club may also have imported data from before that, which the estimate does not cover.
- (assumption) LARGEST-CLUB ROW BUDGET at migration: 100000 flights, about 300000 invoice lines at 3 lines per flight, 250 persons, 25 aircraft, 20000 reservations, 150 charging rules, and about 1000000 audit rows. The audit table is the largest table by far, because the legacy audits every mutation. Total about 1.5 million rows and 1 to 2 GB with indexes. The whole closed user group stays below 10 GB.
- (assumption) PEAK LOAD. A club flies about 120 days per year. The largest club averages 67 flight records per day, and the best Saturday reaches about 200. Concurrent writers per club stay below 25: one duty flight leader plus the pilots who stamp their own landing. Across the whole closed user group the peak stays below 250 concurrent sessions and a few writes per second.
- (decision) LOAD-BEARING CONCLUSION from the sizing: AlpenFlight is a SINGLE-NODE workload. Peak write rate is a few per second, and the largest club holds under 2 GB. Binds every subsequent decision. Prevents: a distributed architecture, a message broker, a read replica, a cache tier, or a shard key that no measurement justifies. The architecture optimises for one-person operability and for correctness, never for scale-out.
- (decision) PERFORMANCE BUDGETS set, answering NFR-13. Nightly rules-engine run: 60 seconds per club, 10 minutes for the whole system. Monthly invoice run for the largest club: 60 seconds over about 700 flights. Migration import for the largest club: 30 minutes for the row import, plus 30 minutes for the reproduction of 10 years of invoices at 10 ms per flight. Flight form save on a phone: NFR-2 already binds it at 300 ms.
- (question) REVISIT CONDITION for the sizing. The first real migration reports the true row counts. If the largest club exceeds 150000 flights or 3 million audit rows, or if the migration import passes 60 minutes, re-open this estimate. One contradiction is unresolved: 11000 movements per year at Schaenis reads as about 5500 flights across several clubs, which is below the 7000 estimated for one largest club. The likely reason is that an airfield movement statistic counts motorised movements only. The estimate takes the higher figure, because undersizing costs more than oversizing.
- (decision) Sizing ACCEPTED by Roman. The question 17 numbers become the planning basis. NFR-13 is answered.
- (decision) DELIVERABLE SCOPE. The spine is the only artifact of this run. The readers are Roman and the build agents. The next step is bmad-create-epics-and-stories, so the spine is a build substrate, terse and convergent. No deck, no solution design, no C4 set.
- (constraint) AVAILABILITY TARGET raised by Roman: 99.4 percent uptime with redundancy. This overrides NFR-4, which said no formal uptime commitment applies to the closed user group. Offer to update the PRD at finalize.
- (event) AVAILABILITY ARITHMETIC. 99.4 percent of a calendar year permits 52.6 hours of downtime per year, or 4.4 hours per month. 99.4 percent of the flying window permits about 10 hours per year: the SFVS counted 152 flying days in 2024, and 140 days times 12 daylight hours is 1680 hours. The two measures differ by a factor of 5. The clock must be named before the target means anything.
- (version) HOSTING EVIDENCE, researched 2026-08-29. Hetzner Cloud commits 99.9 percent monthly availability for a single cloud server. Exoscale is Swiss and commits 99.95 to 99.99 percent. VSHN sells managed PostgreSQL HA in Switzerland with two replicas and a 99.99 percent SLA at CHF 480 per month. The brief sets the whole cost-recovery target near CHF 150 per month for the closed user group, at CHF 30 per club for about 5 clubs. Swiss HA PostgreSQL alone therefore costs about 3 times the total income.
- (event) FAILURE-MODE ANALYSIS. A second application node fixes only host failure. It does not fix a bad deploy or a bad migration, which it replicates. It does not fix disk corruption, which needs backups and point-in-time recovery. For a one-person supplier the dominant term is mean time to REPAIR, not mean time between failures, because the supplier is unreachable at 09:00 on a Saturday. Spend on automatic recovery and alerting first, on a second node second.
- (event) THE OFFLINE CLIENT IS THE PRIMARY REDUNDANCY. PRD section 4.5 already requires the flight write path and a read of today to work with no network, because coverage at an airfield is poor. A server outage during the flying day therefore does not stop the flying day. What an outage does break is reservations, accounting, reports, the invoice run and the public forms. None of those is minute-critical.
- (override) CORRECTION to my earlier claim that the offline client is the primary redundancy. Roman rejected it, and he is right. The server is the coordinator. If it stays down all day while many devices keep logging, the conflict volume grows past what a person will reconcile. The offline store is a BRIDGE of about one hour, not a substitute for the server.
- (decision) AVAILABILITY TARGET RESTATED by Roman, and it replaces the percentage as the driving number. A single outage must never exceed ONE HOUR during the flying window. 99.4 percent stays as the annual figure. Planned maintenance is EXCLUDED, so a window outside the flying window costs nothing. Binds the deployment shape and the recovery path. Prevents: a design that meets a yearly percentage with one long outage.
- (decision) THE OFFLINE BRIDGE AND THE MAXIMUM OUTAGE ARE THE SAME NUMBER, by design. One hour. The client must hold one hour of a busy club's writes with no network. At a busy field that is about 30 launches, so about 60 flight records plus the stamps. Storage is trivial; the real limit is how much conflict a person will reconcile. Binds the offline store, the queue and the conflict view.
- (decision) HOSTING SHAPE: OPTION A, chosen by Roman. One application node. Three conditions make it meet the one-hour cap with one supplier. (1) The application node holds NO data; the database is a separate managed PostgreSQL with point-in-time recovery. (2) Every part of the node is in code, so a rebuild is one command. (3) Deploys are blue-green behind a reverse proxy. Consequence: the application node is disposable. A dead node is a redeploy of minutes, never a restore of hours. Option B, a second instance behind a load balancer, then becomes a deployment change and never a code change.
- (decision) NEW REQUIREMENT from Roman: a feature update ships with NO notification to the user, and no interruption. This is stronger than zero-downtime deployment, because it also binds the client and the offline queue. Binds every API change, every schema change and the service worker.
- (decision) APPLICATION SERVER HOLDS NO STATE. No in-process session, no in-memory cache a request depends on, no local file the next request needs, no in-process scheduler that assumes it runs alone. Every scheduled job takes a database lock before it runs, and every job is idempotent. Binds all. Prevents: one builder parking a session in memory while another writes a singleton scheduler, which makes a second instance a rewrite instead of a configuration change. Also closes RK-10 and Q-B24, because a job that must take a lock and be idempotent cannot produce a duplicate invoice draft on a re-run.
- (decision) SCHEMA CHANGES ARE EXPAND-CONTRACT, always three deploys minimum. Deploy N adds the column and writes both. Deploy N+1 reads the new one. Deploy N+2 drops the old. Prevents: a migration that makes the running version fail, which is the failure a second node replicates instead of preventing.
- (question) OPEN: how long must the server accept a payload from an older client? An offline device can stay offline across a deploy. A pilot's phone can hold a queued write for days. The API compatibility window must cover the longest realistic offline period plus the deploy cadence. Proposal: additive-only write API, and the server accepts any client released in the last 90 days. Needs Roman's decision.
- (decision) OFFLINE QUEUE TTL set by Roman: 24 hours. A queued write that does not reach the server within 24 hours stops applying automatically. The system logs the event and tells the user at the next sign-in. Binds the offline store, the sync path and the notification surface.
- (decision) PRD QUESTION 20 ANSWERED by Roman: a CONFLICT DIALOG. It names the two authors and shows the value each one chose. This rejects a silent last-write-wins for the fields the full form changes. Binds FR-38 and FR-39.
- (constraint) INHERENT LIMIT of an unsynced write, to be stated and not discovered: the write exists only in the store of the device that made it. The server never saw it. A person who signs in on a different device does not see the pending write, and a lost or wiped phone loses it. This is intrinsic to offline-first and it is accepted.
- (question) OPEN, raised against the 24-hour TTL: a timed-out write is data a person typed. Dropping it loses a flight the club bills for. Proposal: the write is PARKED, never deleted. After 24 hours it stops applying by itself and becomes a pending item that shows the values and the time of entry, with Apply and Discard. This satisfies time out, log and notify, and it never destroys typed data. It also reuses the conflict surface, so one mechanism does two jobs. Needs Roman.
- (question) OPEN: conflict GRANULARITY. Field-level or record-level. Field-level means two devices that change different fields of one flight both apply, and only the same field changed differently is a conflict. It costs per-field change tracking instead of a row version. Record-level is cheaper and raises a dialog for edits that do not disagree. Needs Roman.
- (question) OPEN: WHO resolves a conflict. Proposal: the person whose write arrives second, because they are present and they see both values. The alternative is a role precedence, where the duty flight leader wins on operational fields. Needs Roman.
- (question) OPEN: what the nightly engine uses while a conflict is unresolved. Proposal: the server value stands, the flight bills on it, and the conflict stays in an open list. Never block the money on a dialog nobody opened. This matches the migration mismatch pattern the PRD already chose. Needs Roman.
- (decision) STAMPS ARE EXEMPT from the conflict machinery, and this must be explicit or a builder will break UJ-1. The NOW press on the airborne row and the landing stamp are idempotent: the first stamp wins and a second is a no-op. Two people who press NOW do not disagree about a value; they state the same intent twice. A dialog there destroys the speed budget, which is one of the two capabilities that decide if the product succeeds. Binds FR-20 to FR-26 and the offline write path.
- (question) OPEN: API COMPATIBILITY WINDOW, now smaller because of the 24-hour TTL. The TTL bounds the age of a write, not the age of the client that made it. A phone that only opens the application at the airfield can run an old bundle for a season. Proposal: the client updates at application open when it has network, the server accepts any client released in the last 30 days, and an older client meets a reload gate at open, never in the middle of a form. Needs Roman.
- (decision) PARKED WRITE, refined by Roman. Apply and Discard are offered only while the API still accepts the shape the write was made in. If the shape is gone, the system shows an error with the recorded values, and the person re-enters them by hand. Nothing is applied blind.
- (decision) STAMP IDEMPOTENCY CONFIRMED by Roman. The NOW press and the landing stamp never raise a dialog. First stamp wins, the second is a no-op.
- (decision) CONFLICT GRANULARITY, decided by Roman: FIELD-LEVEL, with an exception. A change to a FUNDAMENTAL field escalates to a whole-record conflict, because it reshapes the record and invalidates other fields. EXPERIENCE.md line 177 states the shape: a winch launch shows no tow aircraft and no release altitude, and the legacy carries 88 conditional directives. Binds FR-38, FR-39 and the offline sync path.
- (question) OPEN: name the exact set of FUNDAMENTAL fields that escalate a field conflict to a record conflict. Candidates from the form-reshaping behaviour: LaunchMethod (legacy StartType), the flight type or flight category, the aircraft, and the training flag that makes an instructor mandatory. The set must be closed and named, or a builder guesses. Best resolved by the legacy-oracle agent against the 88 conditional directives.
- (decision) CONFLICT RESOLVER CONFIRMED: the person whose write arrives second resolves it. No role precedence.
- (decision) BILLING DURING AN OPEN CONFLICT CONFIRMED: the server value stands and the flight bills on it. The conflict stays in an open list. The money never waits for a dialog.
- (decision) VERSION WINDOW, decided by Roman: keep it to an absolute minimum. The client reloads when it detects a new server version, except while a person edits a form. Supersedes my 30-day and 90-day proposals.
- (decision) MECHANISM that delivers the minimum window, and it composes with the 24-hour parked write. (1) The offline queue is VERSIONED and the CLIENT migrates it. After an upgrade the new client rewrites its own queued writes into the current shape before it sends them. (2) Every write carries its client version. The server accepts the current version and one version back, and it rejects anything older with a typed error. (3) The client upgrades and retries the queue automatically. Consequence: version tolerance lives in ONE place, the client queue migration, instead of spreading through the API. A queued write the client cannot migrate is exactly Roman's error case: show the values, let the person re-enter them. Prevents: a permanent compatibility layer in the server.
- (direction) NEW SCOPE RAISED BY ROMAN, not yet decided: a bare-bones COMMUNITY EDITION beside the SaaS product, and a PLUGIN ARCHITECTURE for the pro capabilities - invoicing module, OGN integration, FLARM integration. This changes the product shape, the licence, the distribution and the support model. It is upstream of the PRD and of the brief, neither of which mentions an edition split. Must be settled before the module boundaries are fixed.
- (decision) LEGACY BEHAVIOUR EXTRACTION stays with the legacy-oracle agent. No BMad skill covers codebase archaeology. bmad-deep-recon researches the outside world, bmad-project-context documents a repository for agents, and bmad-spec distils intent. None of them reads flsserver and returns a testable oracle. CLAUDE.md already routes this lane to legacy-oracle. Pair it with bmad-review edge-case lens to pressure-test what it returns.
- (decision) The fundamental-field set is EPIC-level detail, not spine-level. The spine fixes that the set EXISTS, is CLOSED, and is enforced in one place. The membership comes from a legacy-oracle read against the 88 conditional directives, queued for the flight-form epic.
- (decision) EDITIONS, decided by Roman. THREE offerings, not two. (1) COMMUNITY EDITION: self-hosted, core only, no pro modules, no commercial support, no migration, no invoicing. (2) FREE PLAN: hosted by the supplier, every pro capability, limited to one club and two aircraft with the inactivity lifecycle. (3) PAID SaaS: hosted, everything, no limits. The community edition sits BESIDE the free plan and does not replace it.
- (decision) THE MIGRATION IS THE PAID MOAT, and this is deliberate. A club that self-hosts cannot bring its legacy FLS data, because the migration and the invoicing are pro. The product sells the migration first, so the migration never ships in the free-as-in-freedom edition.
- (decision) PRO MODULES ARE CLOSED SOURCE. Named so far: invoicing, migration, OGN integration, FLARM integration. Roman requires the boundary to be real, so no future pro feature can be built into the community edition.
- (decision) REPOSITORY SHAPE RECOMMENDED: ONE PRIVATE MONOREPO for development, and an automatic publication of the core subtree to a public repository. Rationale: atomic refactors across core and pro, one test suite, one CI. The public repository is a publication artifact, not the development repository. The alternative, two repositories with pro depending on a published core package, costs a version matrix and a two-step release for every core refactor. That is the expensive option for one person.
- (decision) THE REAL BOUNDARY ENFORCEMENT IS CI, NOT THE REPOSITORY SPLIT. Two rules. (1) Core must never reference pro; a CI check fails the build if it does. (2) CI builds AND TESTS the community edition standalone on every commit. A repository split only LOOKS like enforcement. A green standalone core build IS enforcement. Binds the module boundaries and the build.
- (decision) ONE TENANCY MECHANISM, ALWAYS ON, IN BOTH EDITIONS. A self-hosted community install holds exactly one club row and runs the same isolation code the SaaS runs. Prevents: two tenancy modes, where the isolation defect hides in the path that gets less testing. This is the largest correctness risk in the rewrite (R1, RK-3, RK-12), so it never gets a second code path.
- (constraint) THE COMMUNITY EDITION MUST BE INSTALLABLE BY A VOLUNTEER. One container plus PostgreSQL, documented. Never Kubernetes. This matches the single-node SaaS decision, so both editions deploy the same way.
- (override) CORRECTION, Roman was right. The Akaflieg fork of Startkladde IS maintained. Evidence from the GitHub API on 2026-08-29: last push 2026-07-07, version 2.5.3, commits in June and July 2026 and in March 2025. Not archived, 3 open issues, 6 stars, 6 forks, no licence declared. Only the ORIGINAL startkladde/startkladde repository is marked unmaintained. Drop the no-maintainer argument against Startkladde.
- (event) STARTKLADDE POSITIONING, corrected. It is a Qt and C++ DESKTOP program on a laptop at the launch point, with MySQL or MariaDB, plus a web interface for retrieval. Velocity is low: two commits in 2026 and three in March 2025. That is maintenance, not development. The AlpenFlight differentiator against it is therefore NOT a maintainer. It is phone-first operation, offline work, and the invoicing. State it that way.
- (decision) LICENCE: APACHE-2.0 for the public code, chosen by Roman. It permits closed pro modules with no argument, it carries a patent grant, and inbound contributions arrive under the same terms, so no contributor licence agreement is needed for a small patch.
- (decision) THREE-TIER MODULE TAXONOMY, forced by the FLARM decision. One word one meaning, so name them. (1) CORE - always present, Apache-2.0, both editions: flight logging, master data, logbook, statistics, tenancy, identity. (2) OPEN MODULE - Apache-2.0, optional, present in both editions: FLARM. The SaaS ships it pre-installed and pre-configured, which is a distribution difference, never a code difference. (3) PRO MODULE - closed source, SaaS only: invoicing, migration, OGN. Binds the directory layout, the licence headers and the CI rules.
- (decision) PUBLICATION BOUNDARY REFINED. The public repository carries CORE PLUS THE OPEN MODULES, not core alone. The CI rule updates to match: build and test core-plus-open-modules standalone on every commit.
- (decision) NEW SOURCE for the migration, raised by Roman: an automated path from STARTKLADDE to the SaaS, beside the FLS path. Forces an architecture decision NOW. THE MIGRATION IS A PIPELINE: source reader, then a canonical import model, then validation, then load. The source reader is the ONLY source-specific part. Prevents: the first builder writing an FLS-shaped importer, which makes the Startkladde source a rewrite instead of a new reader. Cheap to decide now, expensive later.
- (constraint) TWO FLAGS on the Startkladde migration. (1) Startkladde has NO invoicing, so there are no invoices to reproduce to the cent. The headline promise of the FLS migration does not apply, and a Startkladde import is flights, pilots and aircraft only. (2) Startkladde is a GERMAN product with German clubs. PRD section 2 lists a club outside Switzerland as not served in v1. The Startkladde importer therefore implies a market expansion with German legal and accounting differences. Record it as a v2 direction, never a v1 commitment.
- (decision) WORDING CHANGE by Roman: SELF-DEPLOYED, never self-hosted. The community edition is deployed by the club into a cloud service of its choice.
- (decision) DEPLOYMENT CONFIRMED by Roman: one container image, Docker Compose is the supported path, Kubernetes is permitted and never required. The image must be stateless so several containers run at once. This confirms the no-state decision and makes Option B, a second instance behind a load balancer, available from the first day.
- (decision) PROVISIONAL DIRECTIVE 2 IS RATIFIED, and row-level security does NOT violate it. Reading: directive 2 forbids a BUSINESS RULE in the database - a state machine, a range, a calculation. It already permits STRUCTURAL invariants: primary keys, foreign keys, structural NOT NULL, identity-bearing partial UNIQUE and performance indexes. Tenant isolation is an access boundary, the same category as a foreign key, not a business rule. Amend the directive to name tenant isolation in the list of structural invariants the schema may enforce.
- (decision) PROVISIONAL DIRECTIVE 1 IS RATIFIED unchanged. Working software over comprehensive documentation. The spine itself is terse for the same reason.
- (event) TENANCY COST ANALYSIS, answering Roman's cost question. FOUR mechanisms, not two. (1) Convention: free, and it is what already failed in the legacy. (2) ORM or repository default scoping: near free, covers the common path, bypassable on purpose. (3) Compile-time type enforcement: the HIGHEST cost and the WEAKEST guarantee, because it protects only the code that goes through it. Raw SQL, a bulk importer and a job that opens its own connection walk past it. (4) PostgreSQL row-level security: about 2 to 3 days of setup, and it covers everything including raw SQL, jobs and the importer.
- (decision) THE DECISIVE ARGUMENT against test-only enforcement. Roman offered to carry the risk with proper testing. Counted honestly, that means an isolation test for every query path - roughly 200 endpoints, so 200 tests a developer must REMEMBER to write. Forgetting the test has exactly the same failure mode as forgetting the filter. Row-level security needs ONE test: enumerate every table that carries a club column and assert the policy is enabled. That test CANNOT be forgotten, because it fails the day somebody adds a table without a policy. One test that cannot be forgotten beats 200 that can.
- (decision) TENANCY MECHANISM DECIDED, answering PRD questions 23 and 24. Two layers. FLOOR: PostgreSQL row-level security with a session variable set once, in the transaction opener. Every tenant table gets a generated policy, plus FORCE ROW LEVEL SECURITY, and the application connects with a non-owner role so it cannot bypass. A query that forgets the filter returns ZERO rows, never another club's rows. ERGONOMICS: default scoping in the repository layer, so the common path is obvious and readable. REJECTED: compile-time type enforcement, on cost against guarantee. Binds FR-1, FR-2, FR-8, SM-4.
- (decision) THE FAIL-CLOSED DEFAULT, which makes a declared shared list safe. A new table is club-scoped unless it appears in a DECLARED list of shared entities. A table that carries no policy and is not on the list fails the build. Prevents: a forgotten policy that looks like a deliberate sharing decision.
- (decision) PRD QUESTION 25 ANSWERED. OwnerId plus OwnershipType is REPLACED, not ported. Tenant-scoped tables get a plain club_id with a real foreign key, which is indexable and carries a policy. The polymorphic discriminator disappears; the legacy already carried it and ignored it, evidenced at FlightService.cs:483. A genuinely shared entity carries NO tenant column, and its access runs through an explicit membership or ownership join with a hand-written policy, one per entity, from the declared list.
- (question) OPEN, PRD question 23: the exact DECLARED LIST of shared entities. Proposal from domain-model.md: Person (shared by design, since a person belongs to any number of clubs and the ClubMembership carries the link), Aircraft (shared in the legacy, with an owning club), and Location or Airfield (shared, since two clubs use the same field). Everything else is club-scoped. Needs Roman to confirm, and it constrains question 28, which asks which shared entity a free club may create.
- (decision) ROMAN'S OBJECTION IS CORRECT and it dissolves the shared-entity problem. A shared writable aircraft leaks from the free tier, and one club deleting it breaks another club. The answer is a FOURTH category that the legacy never had: SYSTEM REFERENCE DATA. It is owned by the supplier, read-only to every club, and imported from an authoritative source. It is NOT shared between clubs, because no club may write it.
- (decision) THERE ARE NO SHARED WRITABLE ENTITIES. Three categories replace the legacy's three broken mechanisms. (1) SYSTEM REFERENCE: supplier-owned, readable by all, writable by none. (2) CLUB-SCOPED: one club, row-level security on club_id, fails closed. (3) CROSS-CLUB LINK: an explicit row the owning club creates, such as a ClubMembership. Prevents: RK-13 entirely, because a free club cannot write anything another club reads.
- (decision) AIRCRAFT SPLIT. ReferenceAircraft is system reference data from the national register: registration, type, category, manufacturer. ClubAircraft is club-scoped and optionally points at a ReferenceAircraft; it carries the counters, the status, the home airfield and everything the club owns. Club A deleting its ClubAircraft never touches club B. A visiting aircraft with no register match becomes a club-scoped ClubAircraft with a free-text registration. Answers Roman's deletion objection.
- (decision) LOCATION SPLIT, the same shape. ReferenceLocation is system reference data. ClubLocation is club-scoped and covers outlanding fields, private strips and waypoints. Coverage of small Swiss gliding fields in any public source is partial, so ClubLocation is the NORMAL case, not a rare fallback.
- (decision) PERSON IS NOT REFERENCE DATA, and it is the one genuinely hard case. A person is visible to a club ONLY through a ClubMembership that the club owns. THERE IS NO GLOBAL PERSON SEARCH. A cross-club crew member under FR-2 needs an explicit link, never a typeahead over every person in the system. This is required independently of the free tier: the system holds names, addresses, licences and medical expiry, so letting any club enumerate every person is a personal-data exposure under the Swiss DSG and the GDPR. The brief already names that breach risk.
- (version) REFERENCE SOURCES researched 2026-08-29, with licences. AIRFIELDS: OurAirports, PUBLIC DOMAIN, nightly CSV dumps of airports, countries and subdivisions, unrestricted commercial use. RECOMMENDED. openAIP is REJECTED as the primary source: it is CC BY-NC 4.0, and although its own wording permits shipping with a commercial application when the data is not sold exclusively, that ambiguity is a risk one person should not carry. AIRCRAFT: the BAZL Swiss aircraft register is the authoritative Swiss source; an online register exists and BAZL has an opendata.swiss organisation page, but I could NOT confirm a bulk dataset - opendata.swiss returned HTTP 403. OGN DDB offers CSV, JSON against ogn-ddb-schema-1.0.0, and a FLARMnet-compatible format, but it states NO LICENCE, and no stated licence means no granted permission.
- (question) OPEN, before the reference-data epic: (1) confirm whether BAZL publishes the aircraft register as a bulk dataset and under which terms; (2) ask the Open Glider Network for written terms for the DDB. Use the DDB for FLARM device matching inside the FLARM module, never as the authoritative aircraft list, until terms exist.
- (decision) REFERENCE DATA SHIPS BOTH WAYS. The SaaS syncs the reference tables on a schedule. The community edition ships a SNAPSHOT inside the container image and may sync if the club wants. Same tables, same code, one mechanism. Reference sync therefore belongs to CORE, never to a pro module, because the community edition needs airfields too.
- (decision) MIGRATION RULE that follows: the FLS importer maps a legacy aircraft to a ReferenceAircraft where the registration matches, and falls back to a club-scoped ClubAircraft where it does not. Same for locations. Belongs to the migration epic.
- (decision) PRD QUESTION 28 IS CLOSED by this model. It asked which shared entity a free club may create. The answer is NONE, because no shared writable entity exists. RK-13 is closed with it.
- (version) BAZL AIRCRAFT REGISTER CONFIRMED USABLE, tested 2026-08-29 with the endpoint Roman supplied. POST https://app02.bazl.admin.ch/web/bazl-backend/lfr/csv returns text/csv, HTTP 200, no authentication. page_result_limit is IGNORED for the CSV export: the call returns the WHOLE register. 3115 aircraft, 39 columns, UTF-16LE with a BOM, semicolon delimited, about 3.8 MB. No pagination needed.
- (event) REGISTER CONTENT. Status: 3018 Registered, 97 Registration in Progress. Aircraft Type: 1499 Aeroplane, 472 Glider, 355 Helicopter, 299 hot-air Balloon, 242 Powered Glider, 188 Homebuilt Airplane, 4 Homebuild Glider. Glider family total 718. This CROSS-CHECKS the sizing research: Statista reported 514 gliders in 2023 and 241 motor gliders; the register today holds 472 plus 242. The two agree.
- (event) FIELD COMPLETENESS on the 718 glider-family rows is effectively total. Registration 718/718, Aircraft Address HEX 718/718, MTOM 718/718, Manufacturer 718/718, Aircraft Model 718/718, ICAO Aircraft Type 711/718, Serial Number 713/718. This is a better aircraft master than any club keeps by hand.
- (decision) AIRCRAFT ADDRESS HEX IS THE ICAO 24-BIT ADDRESS, and it is present for every glider. That is the key FLARM, OGN and ADS-B match on. Consequence: the OGN DDB drops from LOAD-BEARING to OPTIONAL for Swiss aircraft, which removes the dependency on a source that states no licence. Caveat to carry: a FLARM device transmits an ICAO address, a FLARM ID, or a random ID, so the ICAO address covers only the aircraft configured to send it. The DDB stays useful for foreign visitors and for FLARM-ID devices.
- (decision) PRIVACY LANDMINE FOUND IN THE REGISTER, and it sets a hard rule. Columns 35 to 39 are Main Owner, Main Operator, Part Owners, Part Operators and Billing Address. Each carries a full name and a full postal address. Of the 718 glider-family aircraft, 446 - 62 percent - are owned by a PRIVATE PERSON, not an organisation. RULE: import the register and DROP columns 35 to 39. Never store an owner, an operator or a billing address in reference data. Prevents: republishing the home addresses of 446 private Swiss aircraft owners to every club, inside the product whose own brief names personal-data exposure as the reason the legacy is dangerous.
- (decision) REFERENCE AIRCRAFT FIELD SET, from the register, personal columns removed: Registration, Status, Manufacturer, Aircraft Model, ICAO Aircraft Type, Aircraft Type, Airworthiness Category, Aircraft Address HEX, Year of Manufacture, Serial Number, MTOM, Engine Category, Noise Class, plus the LfrID as the natural key for the sync.
- (constraint) SIZE. The whole Swiss register is 3115 rows and about 3.8 MB of CSV. ReferenceAircraft is therefore tiny, and it ships inside the community-edition container image with no difficulty.
- (question) OPEN: the terms of use for the BAZL register export. The Swiss aircraft register is a public register by law, but a public register is not automatically a licence to redistribute the PERSONAL data inside it. Dropping columns 35 to 39 makes the question much easier, because a registration, a type and an MTOM are facts about an aircraft and not about a person. Confirm the terms with BAZL before the reference-data epic ships.
- (event) OWNER-FREE IMPORT PROVED. Dropped the 5 personal columns, kept 34 of 39. All 3115 aircraft survive - the strip removes columns, never rows. Result 1.04 MB of UTF-8 CSV, under 1 MB. Checked the remaining columns for a leak: only Aircraft Address DEC, HEX, OCT and BIN match owner or address by name, and those are the ICAO 24-bit code, not a postal address. Confirmed clean.
- (version) STACK RESEARCH, verified 2026-08-29. JAVA: 25 is the current LTS, GA 2025-09-16, latest 25.0.4.1 on 2026-08-18, free updates to September 2028, next LTS is Java 29 in September 2027. SPRING BOOT: 4.0 released 2025-11-20, 4.1 on 2026-06-10, latest 4.1.1 on 2026-08-20, built on Spring Framework 7, needs Java 17 minimum and supports up to Java 26. POSTGRESQL: 18.6 current on 2026-08-13, 19 in beta. ANGULAR: 20 shipped stable signals and zoneless change detection in May 2025; zoneless is the default from version 21.
- (constraint) HONEST COST TO ACCEPT with Spring Boot: it designates NO release as LTS. Every minor gets 12 months of open-source support and a new minor ships every 6 months. Spring Boot 4.0 reaches end of life on 2026-12-31 and 4.1.1 is supported to 2027-07-31. For a ten-year product with one maintainer that means roughly one framework upgrade per year. Most are mechanical; a major is not. Roman must accept this cadence or choose a stack with longer windows.
- (decision) SERVER RECOMMENDED: Java 25 LTS with Spring Boot 4.1. Five reasons. (1) MONEY - BigDecimal maps exactly to PostgreSQL numeric, so NFR-1, no binary floating point in accounting, is the DEFAULT path and not a discipline. (2) LONGEVITY - a two-year LTS cadence suits one maintainer. (3) PORTING - the legacy is C#, and Java is the nearest structural translation for the nine-phase rules engine. (4) RLS - setting the session variable in a transaction listener is one small class. (5) Roman is already fast in it.
- (decision) THE DECISIVE ARGUMENT AGAINST TYPESCRIPT ON THE SERVER is money. JavaScript has no native decimal type. Every accounting path would need a decimal library plus discipline at every call site. The product's core promise is that invoices reproduce TO THE CENT. Java makes the safe path the default; Node makes it a rule somebody must remember. Same failure shape as the tenant filter.
- (decision) DATABASE: PostgreSQL 18. External validation of our tenancy call: the documented 2026 default for multi-tenant SaaS is a shared schema with row-level security as the safety net, which is exactly the two-layer model already decided.
- (decision) DATABASE ACCESS: Spring Data JPA for the CRUD domain, plus plain SQL through JdbcClient for the rules engine, the reports and the migration importer. Both share one DataSource, so row-level security covers both. jOOQ is DEFERRED, not rejected.
- (decision) CLIENT RECOMMENDED: TypeScript with Angular 21 or later, zoneless, signal-based. Reasons: batteries included - router, forms, dependency injection, HTTP and an official service-worker package - which matters most to a solo developer who benefits from fewer assembly decisions; the 88 legacy conditional directives and 21 typeaheads map naturally onto Angular forms; and this repository already carries angular-developer and angular-signals skills. Honest counterweight: React has about 5.7 times the weekly downloads and a larger answer pool. Roman works alone, so hiring does not apply and both have enough answers.
- (decision) OFFLINE STORE: IndexedDB through a thin wrapper such as Dexie, plus OUR OWN queue. REJECT the sync-engine category - RxDB, ElectricSQL, PowerSync and similar. Reason: our conflict model is bespoke - field-level with a fundamental-field escalation, a 24-hour park, and idempotent stamps. A sync framework imposes its own model, usually CRDTs or last-write-wins, and most bring their own server component, which fights the single-node and one-person decisions.
- (decision) API CONTRACT: the server publishes an OpenAPI specification and the client generates its TypeScript types from it. That gives end-to-end type safety WITHOUT forcing one language on both sides.
- (decision) BUILD: a Gradle multi-module build. A convention rule fails the build when core references pro. Flyway carries the migrations, expand-contract by convention, and it also generates the row-level-security policy per tenant table. One container image serves the API and the built client, so the community edition is one container plus PostgreSQL.
- (decision) API STYLE: REST, and it is not close for this product. FOUR reasons. (1) AN EXTERNAL CONSUMER ALREADY DEPENDS ON REST URLS - the PRD memlog records that the Proffix accounting synchroniser POLLS /api/v1/deliveries GET endpoints, and PRD question 4 keeps that wire path. We must serve REST there whatever else we do, and running two API styles doubles the surface for one maintainer. (2) ONE CLIENT, WHICH WE WRITE - GraphQL exists to let many diverse clients shape their own queries. We have a single client and a FIXED query set: the form prefetches 13 catalogs before it opens. There is no ad-hoc query need to serve. (3) CACHING - REST plus ETag and Cache-Control composes directly with the service worker and the offline store. A GraphQL POST is not HTTP-cacheable, so it needs a normalised client cache such as Apollo or urql, which is more machinery and it fights the bespoke offline queue. (4) THE PUBLIC SURFACES - NFR-7 requires the registration forms to work with no scripting beyond validation, which suits a plain REST POST and does not suit a GraphQL client.
- (decision) GRAPHQL COSTS WE AVOID: a server-side N+1 problem that needs dataloaders, and query-cost limiting to stop an expensive or hostile query on a public endpoint. Both are real work with no matching benefit here.
- (constraint) WHERE GRAPHQL WOULD HAVE WON, recorded so the decision can be re-opened honestly: many diverse clients, third-party consumers with varied query needs, or deeply nested variable graphs. AlpenFlight has none of those. If a third-party integration later needs flexible queries, add it over the same domain then - never now.
- (event) ROOT CAUSE OF ROMAN'S LAYERING PAIN FOUND in attempt-1. ADR-0023 chose hexagonal-lite per module and applied it UNIFORMLY: every module ships domain/, application/, web/ and infra/, with DTOs and domain-to-DTO mappers in application/. ADR-0018 put DDD aggregates on every bounded context, about 30 of them. ADR-0027 added domain read-models. The ceremony was correct for the flight state machine and the rules engine. It was pure overhead for about 45 lookup and master-data entities that carry no rule at all.
- (decision) THE ATTEMPT-1 ESCAPE HATCH POINTED THE WRONG WAY. ADR-0023 offered a promotion path to MORE purity - promote one aggregate to two-entity hexagonal. It offered no path to LESS. Rebuild 2 inverts the default: THIN BY DEFAULT, DEEP ONLY ON A TRIGGER. This supersedes ADR-0023, which CLAUDE.md already marks as history and not authority.
- (decision) DESIGN PARADIGM NAMED: a MODULAR MONOLITH OF VERTICAL SLICES AT TWO DEPTHS. Organise by feature, never by layer. A THIN SLICE is about 4 files: the JPA entity, a Spring Data repository interface, a DTO record materialised directly by a projection, and a controller. It has NO application package, NO mapper class, NO separate domain interface. A DEEP SLICE keeps the four-package shape from ADR-0023, because there the insulation earns its cost. Prevents: two builders choosing different depths for the same kind of work, which is what makes a codebase feel inconsistent.
- (decision) THE DEPTH TRIGGER, so nobody guesses. A slice is DEEP when it has ANY of: (1) a state machine, such as the flight air state, the flight process state or the club lifecycle; (2) an invariant that spans more than one record, such as the glider-to-tow self reference or the crew rules; (3) MONEY - charging rules, invoice drafts, billing expectations; (4) a legacy behaviour we must reproduce exactly, such as the nine-phase rules engine. If none applies the slice is THIN. A THIN SLICE MAY CARRY NO BUSINESS RULE AT ALL - the moment it needs one, it is promoted. That rule is the guard against logic hiding in a controller.
- (assumption) EXPECTED SPLIT across the 56 legacy entities: about 6 to 8 DEEP slices - Flight, the rules engine, InvoiceDraft, BillingExpectation, the migration, the club lifecycle, and probably Reservation for its overlap invariant - against about 45 THIN slices. All 13 catalogs the flight form prefetches are thin. Reference aircraft and reference locations are thinner still, because they are READ-ONLY and carry no write path.
- (event) MEASURED CEREMONY. Attempt-1 per CRUD entity: domain entity, domain repository interface, domain exception, application service, request DTO, response DTO, mapper, controller, JPA repository, and four package-info files - about 9 to 12 files. Proposed thin slice: entity, repository, DTO record, controller - about 4. Across roughly 45 thin entities that is the difference between about 450 files and about 180.
- (decision) CLIENT LADDER REMOVED TOO. Attempt-1 shape: generated TypeScript type, then a client service, then a component model, then a BehaviorSubject or signal, then the template. New rule: the generated OpenAPI type IS the component's type - never re-map it into a client-side model - and httpResource gives the signal with its loading and error state in ONE declaration. No client service layer for a thin slice.
- (version) CORRECTION to my earlier client version. ANGULAR 22 is the target, not 21. resource, rxResource and httpResource were promoted to STABLE in Angular 22 (angular/angular PR 68253); they were experimental in 19 to 21. httpResource runs through HttpClient, so interceptors, auth headers and XSRF still apply. Pin the exact minor when the repository is created.
- (decision) REJECTED: metadata-driven CRUD screen generation on the client. A generic form-and-table renderer becomes its own framework and it fights DESIGN.md, which is opinionated - dark only, dense, specific. Instead: a SHARED COMPONENT SET - data table, form field, typeahead - with HAND-WRITTEN screens. Screens are cheap when the components are right; a renderer is expensive and rigid.
- (constraint) RISK ACCEPTED on the thin lane: a generic CRUD controller base can grow into its own mini-framework. Mitigation: keep it small, and let any slice write its controller out longhand. Prefer explicit over clever.
- (decision) PROPAGATION TO DOWNSTREAM AGENTS, ranked by reliability. A document is the WEAKEST mechanism, so the spine's job is to specify the stronger three. (1) A CI RULE - a failing build teaches better than any prose. (2) A WORKED EXAMPLE in the repository - one thin slice and one deep slice, built first, that every later story copies. Attempt-1 proved this with the S-048 walking skeleton, which ADR-0023 records as the template later stories inherited. (3) AGENTS.md in alpenflight/, which every agent loads. (4) The spine itself, referenced from the CLAUDE.md triage table.
- (decision) THE CI RULE SET that carries the architecture, so no agent has to remember it: a thin slice must NOT contain an application package; a deep slice MUST contain the four packages; core must never reference pro; the community edition builds and tests standalone; every table carrying a club column has a row-level-security policy; no accounting type is a binary floating-point type. Each rule fails the build.
- (decision) FIRST EPIC IS THE TEMPLATE, not a feature. It ships the two walking skeletons - one thin slice and one deep slice - plus the CI rule set above. Every later story copies a skeleton instead of re-deciding the shape.
- (decision) AGENTS.md GAP CLOSED at finalize. No project-context.md exists today, which is why the bmad-architecture persistent_facts glob loaded nothing at activation. Run bmad-project-context against alpenflight/ once the spine is final, so every downstream agent loads the architecture facts automatically.
- (decision) NO TABLE ELEMENT FOR A DATA LIST, raised by Roman: a table is not responsive. Accepted, with the correct mechanism. The problem is the TABLE MARKUP, never the column alignment. ONE RecordList component renders CARDS on a narrow viewport and ALIGNED COLUMNS on a wide one, from ONE definition, using CSS grid. That gives the treasurer the aligned columns the comparison work needs AND gives the phone the cards. Binds every list surface.
- (constraint) ACCESSIBILITY CAVEAT on the RecordList, which must be solved once inside the component. NFR-6 and NFR-7 require programmatic labels and correct announcement. Dropping table SEMANTICS hurts a screen-reader user on genuinely tabular data, because row and column context disappears. The component therefore carries table roles when the data is tabular, and labels each field inside a card when it renders narrow. Solve it once in the component, never per screen.
- (event) WHERE A COLUMN RENDERING IS NOT OPTIONAL, and why the RecordList must support it: the logbook with about 15 columns, the invoice-line review, the migration mismatch report that shows legacy against reproduced line by line, and the reports. A treasurer scans DOWN one column across 200 lines. Cards destroy that scan. EXPERIENCE.md already calls the pointer device the dense variant and puts the treasurer there for the accounting rules and the invoice run.
- (decision) UPSTREAM UPDATE QUEUED. The RecordList rule is a UX decision and its home is DESIGN.md and EXPERIENCE.md, not the spine. The spine records it as a consistency convention - one component, every surface uses it. Offer to update the two UX documents at finalize, together with the PRD and brief updates already queued for the editions, the licence, the availability target and the module split.
- (decision) ACCESSIBILITY SCOPE REDUCED by Roman, deliberately, and it amends NFR-6. KEPT, because speed and sunlight already require it: keyboard reachable in visual order, a visible focus ring, the DESIGN.md contrast floor, no state carried by colour alone, and an error tied to its field. KEPT UNCHANGED: NFR-7 for the three public surfaces, because a stranger on an unknown device uses them and the legal exposure sits there. DROPPED for the signed-in application: screen-reader optimisation of RecordList, live-region politeness tuning, and any formal WCAG conformance claim. RecordList still carries its roles, because twenty lines now beats touching every screen later, and the memlog records that this becomes a real launch after the closed group is satisfied.
- (event) REVIEWER GATE run inline, not as subagents, because Roman has not asked for agent spawning. lint_spine.py: 0 findings. Then the two configured lenses plus the good-spine rubric, applied by hand.
- (version) ANGULAR VERIFIED: v22 released 2026-06-03, current patch 22.0.1, active support to December 2026 and LTS to May 2028. resource, rxResource and httpResource are STABLE in v22. SIGNAL FORMS are also stable in v22, which matters directly for a flight form carrying 88 conditional fields. Selectorless components and zoneless are the defaults.
- (event) ADVERSARIAL LENS FOUND SIX REAL HOLES - pairs of slices that obey every AD and still build incompatibly. (1) Two slices pick different id types while the strategy is only Deferred. (2) One slice returns a bare array and another returns an items-and-total envelope, so RecordList needs two shapes. (3) One slice soft-deletes and another hard-deletes, which breaks a foreign key from a deep slice and makes the retention duty unenforceable. (4) One slice calls Instant.now() and another injects a Clock, which breaks NFR-10, the requirement that both time gates are drivable in a test with no clock change. (5) One slice adds an optimistic-lock version column and another does not, which loses an update on a concurrent server edit and undermines the HOLD concept. (6) The client and the server each keep their own list of fundamental fields, and the two drift.
- (decision) SIX FIXES APPLIED. (1) The template epic fixes the id type and every slice copies the skeleton. (2) One list envelope and one pagination shape for every collection endpoint. (3) SOFT DELETE IS THE DEFAULT for every business record; a hard delete happens only in a named erasure path under FR-83 and FR-84. (4) A Clock is INJECTED; no code calls now() directly. (5) Every mutable entity carries a version column, and a concurrent update returns a typed conflict. (6) The fundamental-field set is SERVED to the client from the server, never duplicated in the client.
- (decision) AD-3 SHARPENED after the lens found an ambiguity in 'readable by all'. Reference data is readable by every AUTHENTICATED principal. A public surface reads only an explicitly PUBLISHED subset, never the whole reference table.
- (event) spine finalized
- (assumption) REFERENCE-SOURCE TERMS ASSUMED CLEAR by Roman, so they do not block implementation. Recorded as an ASSUMPTION and not a fact: neither BAZL nor the Open Glider Network has confirmed anything in writing yet. The technical risk is low because AD-5 already drops every personal column, which leaves facts about aircraft. The residual risk sits with the OGN DDB, which states no licence at all. Revisit condition: if either party declines, the FLARM module falls back to the BAZL ICAO 24-bit address alone, which AD-5 already carries for every Swiss glider, and the DDB is used only for a device the club registers itself.
- (decision) COMPONENT NAME DECIDED after three rounds: RecordList holds RecordItem. Rejected RowCard, because row is already taken - DESIGN.md defines spacing.row-h 44px as the DENSE height against item-h 60px - and because row is the table word the design explicitly rejects. Rejected RecordCard, because card is only true in the narrow rendering and false in dense mode at 32px. Rejected RecordSummary, raised by Roman, because a summary could read as a summary OF the list rather than one item IN it. RecordList plus RecordItem makes the containment obvious and stays true in both renderings.
- (decision) NEW PRIMARY DIRECTIVE 3 in CLAUDE.md, requested by Roman: naming is a first-class decision with NO round limit. Bring the alternatives and the reason for each. Never cap the rounds and never signal impatience. Test every candidate against domain-model.md section 1 and against the names already in use - a collision is a defect just as a synonym is. Never rename silently: state the reason, then apply the new name everywhere in ONE pass.
- (event) UPSTREAM UPDATES APPLIED. CLAUDE.md: both directives ratified, directive 2 amended for tenant isolation, directive 3 added, the spine linked from the triage table and from When in doubt. brief.md: the three offerings, Apache-2.0, the migration as the paid moat, superseding the no-free-tier text. prd.md: NFR-4, NFR-6, NFR-12 and NFR-13 rewritten; section 5 gains the editions and the module tiers; RK-3, RK-12 and RK-13 updated; questions 4, 17, 18, 20, 23, 24, 25 and 28 closed, 34 deferred; an architecture pointer in the frontmatter and the preamble. DESIGN.md and EXPERIENCE.md: record strip renamed to record item with its token and section, the operator vocabulary split into duty flight leader and supplier, and the offline ASSUMPTION marked resolved.
- (event) TWO DEFECTS I INTRODUCED AND FIXED during the rename. (1) A Python text-mode write re-translated the newlines, which put a double carriage return on every line of both files. (2) EXPERIENCE.md was LF-only in git and my write converted the whole file to CRLF, which showed as 392 changed lines. Both repaired by a binary rewrite; the diff is now 30 real content lines per file. Also fixed the article agreement the rename broke - a item became an item, six places.
