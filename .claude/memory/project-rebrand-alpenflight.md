---
name: project-rebrand-alpenflight
description: FLS rewrite is AlpenFlight (alpenflight.ch); shipped via S-128 on 2026-05-16. Domain reg + TM filing deferred.
metadata: 
  node_type: memory
  type: project
  originSessionId: 21c377e9-db57-4689-9f19-8c885e984805
---

The rewrite is **AlpenFlight** (`alpenflight.ch`). FLS legacy stack (`flsserver/`, `flsweb/`) keeps the old name as reference-only per CLAUDE.md.

**Status:** name + repo paths shipped via S-128 (2026-05-16). Domain registration + TM filing deliberately deferred — confirm with operator before assuming either is done.

**Rationale capsule (for "why this name" questions):** DACH-coded compound, identical DE/EN pronunciation, distinctive vs. crowded cloud iconography, no DE class-42 TM incumbent (unlike runners-up Vario / Bise). `.ch` chosen over `.aero` on cost (~CHF 12 vs ~CHF 80/yr).

**Apply when:** generating product copy, repo names, emails, URLs. Default to `alpenflight.ch`.
