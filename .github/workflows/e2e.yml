name: e2e

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
  workflow_dispatch:

# Cancel in-progress runs for the same ref when a new commit lands.
concurrency:
  group: e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright:
    name: Playwright on Mono + Node 8 (flsweb) + Node 20 (e2e)
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    permissions:
      contents: write

    env:
      FLS_LISTEN_URL: "http://*:25567/"
      FLS_MSSQL_SA_PASSWORD: "Demo#FLS#2026"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----- Kick off the slow independent steps in parallel ---------------------
      # These all run concurrently as background shell processes. They saturate
      # the runner's 2 cores by overlapping CPU-bound (xbuild, gyp), I/O-bound
      # (apt install Mono, NuGet restore, yarn install, docker pull) work.
      - name: Install Mono 6.12 (start in background)
        run: |
          (sudo apt-get update -qq && sudo apt-get install -y -qq mono-complete && touch /tmp/.done-mono) > /tmp/mono.log 2>&1 &
          echo "mono install PID=$!"

      - name: Start docker-compose stack (background)
        run: |
          # docker images: pull + start; healthcheck wait happens later
          # together with the rest of the join points.
          (docker compose -p fls-e2e up -d && touch /tmp/.done-stack) > /tmp/stack.log 2>&1 &
          echo "compose up PID=$!"

      # ----- Caches (synchronous; quick) ----------------------------------------
      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: flsserver/src/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('flsserver/src/**/packages.config') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Cache flsweb node_modules
        id: flsweb-cache
        uses: actions/cache@v4
        with:
          path: flsweb-node_modules
          key: flsweb-node-modules-${{ runner.os }}-${{ hashFiles('flsweb/yarn.lock', 'flsweb/package.json') }}
          restore-keys: |
            flsweb-node-modules-${{ runner.os }}-

      # ----- Prepare flsweb tree + start yarn install in background -------------
      - uses: actions/setup-node@v4
        with: { node-version: 8 }

      - name: Prepare flsweb tree + yarn install (background)
        run: |
          rm -rf /tmp/flsweb-build
          cp -r flsweb /tmp/flsweb-build
          sed -i "s|'./TryflightController'|'./TryFlightController'|" /tmp/flsweb-build/src/tryflight/TryFlightModule.js
          sed -i "s|'./PassengerflightController'|'./PassengerFlightController'|" /tmp/flsweb-build/src/passengerflight/PassengerFlightModule.js
          if [ -d flsweb-node_modules ]; then
            mv flsweb-node_modules /tmp/flsweb-build/node_modules
            echo "yarn install: cache hit, skipping" > /tmp/yarn.log
            touch /tmp/.done-yarn
          else
            (sudo npm install --global yarn@1 -s \
              && cd /tmp/flsweb-build \
              && yarn install --network-timeout 600000 || true \
              && touch /tmp/.done-yarn) > /tmp/yarn.log 2>&1 &
            echo "yarn install PID=$!"
          fi

      # ----- Join point #1: need Mono before we can NuGet-restore + xbuild ------
      - name: Wait for Mono install
        run: |
          while [ ! -f /tmp/.done-mono ]; do sleep 2; done
          tail -3 /tmp/mono.log

      - name: Install NuGet CLI
        run: |
          sudo curl -sSL -o /usr/local/bin/nuget.exe https://dist.nuget.org/win-x86-commandline/latest/nuget.exe

      - name: NuGet restore
        working-directory: flsserver/src
        run: mono /usr/local/bin/nuget.exe restore FLS.sln

      - name: Build flsserver projects (xbuild)
        working-directory: flsserver/src
        run: |
          for proj in \
              FLS.Common/FLS.Common.csproj \
              FLS.Server.Interfaces/FLS.Server.Interfaces.csproj \
              FLS.Data.WebApi/FLS.Data.WebApi.csproj \
              Alpinely.TownCrier/Alpinely.TownCrier.csproj \
              FLS.Server.Data/FLS.Server.Data.csproj \
              FLS.Server.Service/FLS.Server.Service.csproj \
              FLS.Server.Web/FLS.Server.WebApi.csproj \
              FLS.Server.Console/FLS.Server.Console.csproj ; do
            xbuild "$proj" /p:Configuration=Debug
          done

      - name: Drop EF SqlServer DLL into bin
        working-directory: flsserver/src
        run: |
          cp packages/EntityFramework.6.2.0/lib/net45/EntityFramework.SqlServer.dll \
             FLS.Server.Console/bin/Debug/

      # ----- Join point #2: need the docker stack healthy before seeding -------
      - name: Wait for docker stack healthchecks
        run: |
          while [ ! -f /tmp/.done-stack ]; do sleep 1; done
          for i in $(seq 1 60); do
            healthy=$(docker compose -p fls-e2e ps --format json | grep -c '"Health":"healthy"' || true)
            if [ "$healthy" -ge 2 ]; then break; fi
            sleep 2
          done
          docker compose -p fls-e2e ps

      - name: Apply FLSTest schema and seed
        env:
          FLS_MSSQL_CONTAINER: fls-e2e-mssql-1
        run: bash e2e/scripts/seed.sh

      - name: Start FLS.Server.Console
        working-directory: flsserver/src/FLS.Server.Console/bin/Debug
        run: |
          setsid nohup mono FLS.Server.Console.exe > /tmp/fls-server.log 2>&1 < /dev/null &
          for i in $(seq 1 60); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:25567/api/v1/countries || true)
            if [ "$code" = "200" ]; then echo "API ready"; break; fi
            sleep 2
          done

      # ----- Join point #3: need yarn install done before starting dev server --
      - name: Wait for yarn install
        run: |
          while [ ! -f /tmp/.done-yarn ]; do sleep 2; done
          # If a cache miss installed node_modules, push it back to the cache path.
          if [ ! -d flsweb-node_modules ] && [ -d /tmp/flsweb-build/node_modules ]; then
            cp -r /tmp/flsweb-build/node_modules flsweb-node_modules
          fi
          tail -3 /tmp/yarn.log

      - name: Start webpack-dev-server in background
        working-directory: /tmp/flsweb-build
        run: |
          setsid nohup ./node_modules/.bin/webpack-dev-server --TARGET=DEV --SERVER_URL=http://localhost:25567/ > /tmp/flsweb.log 2>&1 < /dev/null &
          for i in $(seq 1 90); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ || true)
            if [ "$code" = "200" ]; then echo "Dev server ready"; break; fi
            sleep 2
          done

      - name: Set up Node 20 for Playwright
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: e2e/package-lock.json

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('e2e/package-lock.json') }}
          restore-keys: |
            playwright-${{ runner.os }}-

      - name: Install Playwright deps
        working-directory: e2e
        run: |
          npm ci
          if [ "${{ steps.playwright-cache.outputs.cache-hit }}" = 'true' ]; then
            npx playwright install-deps chromium
          else
            npx playwright install --with-deps chromium
          fi

      # ----- Run tests ----------------------------------------------------------
      - name: Run Playwright (read project)
        id: read
        working-directory: e2e
        run: npx playwright test --project=read
        continue-on-error: true

      - name: Stash read project's HTML report
        if: always()
        # hashFiles() only inspects ${{ github.workspace }} — paths under /tmp
        # always return empty, so we used to silently skip this step on every
        # run. Check from shell instead.
        run: |
          if [ -f /tmp/fls-e2e-report/index.html ]; then
            mv /tmp/fls-e2e-report /tmp/fls-e2e-report-read
          fi

      - name: Run Playwright (mutate project)
        id: mutate
        working-directory: e2e
        run: npx playwright test --project=mutate
        continue-on-error: true

      - name: Stash mutate project's HTML report
        if: always()
        run: |
          if [ -f /tmp/fls-e2e-report/index.html ]; then
            mv /tmp/fls-e2e-report /tmp/fls-e2e-report-mutate
          fi

      # ----- Bundle report + screenshots + logs for gh-pages ---------------------
      - name: Assemble gh-pages artifact
        if: always()
        env:
          READ_OUTCOME: ${{ steps.read.outcome }}
          MUTATE_OUTCOME: ${{ steps.mutate.outcome }}
        run: |
          mkdir -p public
          if [ -d /tmp/fls-e2e-report-read   ]; then cp -r /tmp/fls-e2e-report-read   public/report-read;   fi
          if [ -d /tmp/fls-e2e-report-mutate ]; then cp -r /tmp/fls-e2e-report-mutate public/report-mutate; fi
          if [ -d e2e/screenshots ]; then cp -r e2e/screenshots public/screenshots; fi
          mkdir -p public/logs
          [ -f /tmp/fls-server.log ] && cp /tmp/fls-server.log public/logs/ || true
          [ -f /tmp/flsweb.log ]     && cp /tmp/flsweb.log     public/logs/ || true

          # Build the per-run dynamic fragments. The template at
          # .github/pages/index.html contains ${VAR} placeholders that
          # envsubst fills in below.
          REPORTS=""
          add_card() {
            REPORTS="${REPORTS}<a class=\"card\" href=\"$1\"><h3>$2</h3><p>$3</p></a>"
          }
          if [ -d public/report-read ];   then add_card "report-read/index.html"   "Playwright report — read"   "Read-only test project."; fi
          if [ -d public/report-mutate ]; then add_card "report-mutate/index.html" "Playwright report — mutate" "Mutating test project with failure traces &amp; screenshots inline."; fi
          if [ -f public/logs/fls-server.log ]; then add_card "logs/fls-server.log" "FLS server log"        "Mono / ASP.NET Web API output."; fi
          if [ -f public/logs/flsweb.log ];     then add_card "logs/flsweb.log"     "flsweb dev-server log" "Webpack dev-server stdout."; fi

          SCREENSHOTS=""
          if [ -d public/screenshots ] && compgen -G "public/screenshots/*.png" > /dev/null; then
            SCREENSHOTS='<section><h2>Full-page screenshots</h2><div class="gallery">'
            for f in public/screenshots/*.png; do
              name=$(basename "$f")
              SCREENSHOTS="${SCREENSHOTS}<a class=\"shot\" href=\"screenshots/${name}\"><img src=\"screenshots/${name}\" loading=\"lazy\" alt=\"${name}\"><span>${name}</span></a>"
            done
            SCREENSHOTS="${SCREENSHOTS}</div></section>"
          fi

          export TIMESTAMP="$(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          export COMMIT_SHORT="${GITHUB_SHA:0:7}"
          export REPORTS SCREENSHOTS

          envsubst '${TIMESTAMP} ${COMMIT_SHORT} ${READ_OUTCOME} ${MUTATE_OUTCOME} ${REPORTS} ${SCREENSHOTS}' \
            < .github/pages/index.html > public/index.html

      - name: Deploy to gh-pages
        if: github.ref == 'refs/heads/main' && always()
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: public
          publish_branch: gh-pages
          force_orphan: true
          enable_jekyll: false
          commit_message: "e2e: ${{ steps.read.outcome }}/${{ steps.mutate.outcome }} for ${{ github.sha }}"

      - name: Tear down docker-compose stack
        if: always()
        run: docker compose -p fls-e2e down -v

      - name: Mark job as failed (post-deploy)
        if: steps.read.outcome != 'success' || steps.mutate.outcome != 'success'
        run: exit 1
