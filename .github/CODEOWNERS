# CODEOWNERS — sensitive files that require an explicit security / tech-lead review.
#
# GitHub uses this file to auto-request review on PRs that touch the listed
# paths and (when branch protection is set up) to block merge until the
# matching owner has approved. Solo-dev today; the rule still serves as a
# visible "this file is sensitive" marker that surfaces in the PR diff.
#
# See https://docs.github.com/en/repositories/managing-your-repositorys-settings-and-security/customizing-your-repository/about-code-owners.

# Tenant-scope catalog files — every change to these alters the surface that
# S-024's CI leakage sweep + native-SQL register gate enforce.
/alpenflight/database/tenant-rules.yaml         @elekktrisch
/alpenflight/database/tenant-catalog.md         @elekktrisch
/alpenflight/database/native-sql-register.md    @elekktrisch
