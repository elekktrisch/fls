# 01 — Current State

Phase 1 output of the [modernization workflow](README.md). Authoritative snapshot of the system as it stands at the time of writing. Sacred cows and strategic anchors live in [`00-seed.md`](00-seed.md); this doc supplies the facts the seed assumes.

Generated by `/modernize-discover`. Regenerate via the same skill when the repo materially changes.

## 1. Executive snapshot

The Flight Logging System (FLS) is a multi-tenant SaaS used by Swiss glider clubs to run flight operations: aircraft reservations, flight logging, planning days, accounting, and invoice export to external systems. Two independently-versioned codebases live side-by-side in this repo: `flsserver/` (ASP.NET Web API 2 on .NET Framework 4.5, EF6 Code First, Unity DI, OWIN OAuth2 bearer auth, SQL Server) and `flsweb/` (AngularJS 1.4 SPA, Webpack 1, Babel ES2015, Karma + Jasmine, Node 8 toolchain). Two external integrations book-end the system: the [OGNAnalyser](https://github.com/sgacond/OGNAnalyser) project writes inbound flights directly to the DB, and the [PROFFIX-FLS-Sync](https://github.com/arminstutz/PROFFIX-FLS-Sync) project consumes outbound deliveries via the public API. Lifecycle stage: **mature legacy** — actively used in production, but the entire toolchain is end-of-life (Webpack 1, AngularJS 1.4, .NET Framework 4.5, Node 8). A Playwright e2e suite (34 specs) was added recently and is the most reliable feature inventory.

## 2. Feature inventory

Grouped by domain. Rows: feature → primary code paths → user persona → e2e spec. "Spec" column refers to files under [`e2e/tests/`](../../e2e/tests/).

### Identity, auth, and tenancy
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Bearer login (`/Token`) | `flsserver/.../Startup.Auth.cs`, `IdentityUserManager`, `core/AuthService.js` | all | `auth.spec.ts`, `02-authenticated.spec.ts` |
| Password reset (lost password) | `flsweb/src/lostpassword/`, `RegistrationService` | end-user | (none) |
| Email confirmation | `flsweb/src/confirm/`, `RegistrationService` | end-user | (none) |
| User CRUD | `UsersController`, `UserService`, `flsweb/src/masterdata/users/` | club admin | `27-user-crud.spec.ts` |
| Roles & user-role assignment | `UserRolesController`, `Role` enum, `RoleApplicationKeyStrings.cs` | system admin | (covered transitively) |
| Profile self-edit | `flsweb/src/profile/`, `UsersController.UpdateMy` | end-user | `18-profile-edit.spec.ts` |
| Multi-tenant isolation | `BaseService.CurrentAuthenticatedFLSUserClubId` (convention) | system | `25-multi-tenant-isolation.spec.ts` |

### Master data (CRUD-shaped admin surface)
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Clubs | `ClubsController`, `ClubService`, `flsweb/src/masterdata/clubs/` | system / club admin | `28-club-crud.spec.ts` |
| Aircraft | `AircraftsController`, `AircraftService`, `flsweb/src/masterdata/aircrafts/` | club admin | `26-aircraft-crud.spec.ts` |
| Locations | `LocationsController`, `LocationService`, `flsweb/src/masterdata/locations/` | club admin | `12-masterdata-crud.spec.ts`, `03-masterdata.spec.ts` |
| Flight types | `FlightTypesController`, `flsweb/src/masterdata/flightTypes/` | club admin | `29-flight-type-crud.spec.ts` |
| Member states | `MemberStatesController`, `flsweb/src/masterdata/memberStates/` | club admin | `30-member-state-crud.spec.ts` |
| Person categories | `PersonCategoriesController`, `flsweb/src/masterdata/personCategories/` | club admin | `31-person-category-crud.spec.ts` |
| Persons (+ add-person modal) | `PersonsController`, `PersonService`, `flsweb/src/masterdata/persons/` | club admin | `13-persons-add-modal.spec.ts` |
| Articles | `ArticlesController`, `ArticleService` | club admin | (none) |
| Email templates | `EmailTemplatesController`, `TemplateService` | club admin | (none) |
| Language translations | `LanguageTranslationsController`, `LanguageService` | system admin | (translation pipeline tested transitively) |
| System data | `SystemDatasController`, `SystemService` | system admin | (none) |
| System logs | `SystemLogsController`, `flsweb/src/system/logs/` | system admin | `19-audit-logs.spec.ts` |
| Reference tables (countries, lengths, elevations, counter units, …) | `CountriesController`, `ElevationUnitTypesController`, `LengthUnitTypesController`, `CounterUnitTypesController`, … | system | (consumed via dropdown caches) |

### Flight operations
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Glider/tow flight create | `FlightsController.Post`, `FlightService`, `flsweb/src/flights/` | pilot / operator | `04-flights-create.spec.ts` |
| Flight edit | `FlightsController.Put`, `FlightService` | pilot / operator | `05-flights-edit.spec.ts` |
| Flight state transitions | `FlightService.cs:1380-1440`, `FlightStates`/`FlightAirStates` enums | scheduled job + admin | `06-flights-state-transitions.spec.ts`, `22-flight-locking-workflow.spec.ts` |
| Flight types per aircraft type | `FlightCostBalanceTypesController`, `FlightTypesController` | system | (validated indirectly) |
| Air movements (motor aircraft) | `flsweb/src/flights/airmovements/`, same `FlightsController` | club admin | `07-airmovements-crud.spec.ts` |
| Flight reports | `FlightReportsController`, `FlightReportService`, `flsweb/src/reporting/` | club admin | `16-flight-reports-generation.spec.ts`, `17-custom-report-builder.spec.ts` |
| API contract surface | (covers all `FlightOverview` / `FlightDetails` DTOs) | system | `33-api-contract.spec.ts` |
| Audit log | `AuditLogsController`, `AuditLogService` (client) | club admin | `19-audit-logs.spec.ts` |

### Reservations & planning
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Reservation CRUD | `AircraftReservationsController`, `AircraftReservationService`, `flsweb/src/reservations/` | pilot | `10-reservations-crud.spec.ts` |
| Reservation scheduler (calendar view) | `flsweb/src/reservation-scheduler/` | pilot | `11-reservation-scheduler.spec.ts` |
| Planning day CRUD | `PlanningDaysController`, `PlanningDayService`, `flsweb/src/planning/` | club admin | `14-planning-day-crud.spec.ts` |
| Planning setup wizard | `flsweb/src/planning/` (setup screen) | club admin | `15-planning-setup-wizard.spec.ts` |
| Planning-day notifications | `PlanningDayNotificationJob` | scheduled job | (covered by email spec) |

### Accounting & invoicing pipeline (sacred cow — see [seed §Sacred cows](00-seed.md#sacred-cows-must-survive-the-rewrite))
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Accounting rule filter CRUD | `AccountingRuleFiltersController`, `AccountingRuleFilterTypesController`, `AccountingUnitTypesController`, `flsweb/src/masterdata/accountingRules/` | club admin | `21-accounting-rules-edit.spec.ts` |
| Delivery creation (rules engine) | `Accounting/DeliveryItemRulesEngine.cs`, `DeliveryService`, `RulesEngine/RulesEngine.cs`, `DeliveryCreationJob` | scheduled job | `23-delivery-creation-workflow.spec.ts`, `32-rules-engine-per-type.spec.ts` |
| Delivery view / edit / delete | `DeliveriesController`, `flsweb/src/masterdata/deliveries/` | club admin | `23-delivery-creation-workflow.spec.ts` |
| Delivery creation test (regression harness) | `DeliveryCreationTestsController`, `flsweb/src/masterdata/deliveryCreationTests/` | club admin | `20-delivery-creation-test.spec.ts` |
| Delivery mail export | `DeliveryMailExportJob`, `Exporting/` (EPPlus, Ionic.Zip) | scheduled job | (no e2e — depends on SMTP) |

### Public (no-auth) flows
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Trial-flight registration | `TrialFlightsRegistrationsController`, `flsweb/src/tryflight/` | public | `09-public-flows.spec.ts`, `01-public.spec.ts` |
| Passenger-flight registration | `PassengerFlightsRegistrationsController`, `flsweb/src/passengerflight/` | public | `09-public-flows.spec.ts` |
| Landing page | `flsweb/src/main/` | public | `landing.spec.ts` |

### Email & scheduled jobs
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Daily flight validation | `Jobs/DailyFlightValidationJob.cs` | scheduled | `22-flight-locking-workflow.spec.ts` |
| Daily report email | `Jobs/DailyReportJob.cs`, `Email/` | scheduled | `08-email.spec.ts` (mailpit) |
| Monthly aircraft statistic report | `Jobs/AircraftStatisticReportJob.cs` | scheduled | (none) |
| Planning-day notification email | `Jobs/PlanningDayNotificationJob.cs` | scheduled | `08-email.spec.ts` |
| Licence-expiry notification email | `Jobs/LicenceNotificationJob.cs` | scheduled | `08-email.spec.ts` |
| Delivery creation | `Jobs/DeliveryCreationJob.cs` | scheduled | `23-delivery-creation-workflow.spec.ts` |
| Delivery mail export | `Jobs/DeliveryMailExportJob.cs` | scheduled | (none) |
| Aircraft DB sync (OGN) | `Jobs/AircraftDatabaseSyncJob.cs` | scheduled | (none) |
| Workflow dispatcher | `WorkflowsController`, `WorkflowService.Run()` | external cron | (covered transitively) |

### Dashboard
| Feature | Code | Persona | Spec |
|---|---|---|---|
| Dashboard | `DashboardsController`, `DashboardService`, `flsweb/src/main/dashboard/` | all | (none direct) |

**Coverage gap callouts:** No e2e for monthly aircraft stat report, aircraft DB sync, delivery mail export, password reset, email confirmation, or articles / email templates CRUD. These are the highest-risk parity gaps if we cut over before adding tests.

## 3. Architecture digest

Bullet the load-bearing patterns. Detail lives in [`SERVER.md`](../../SERVER.md) and [`CLIENT.md`](../../CLIENT.md); not repeated here.

- **Workflow ≠ Quartz.** Background jobs are HTTP endpoints on `WorkflowsController`. External OS-level cron hits them via the `FLS.Workflow.Activator` console app. `WorkflowService.Run()` dispatches by UTC hour. The `job_scheduling_data_2_0.xsd` in the service tree is a **dead schema** — no in-process scheduler.
- **Two-dimensional flight state.** `FlightAirState` is *computed*, never stored; `FlightProcessState` is *stored* and workflow-driven. Time gates: ≥2 days to lock, ≥3 more days to bill. See [seed §Sacred cows](00-seed.md#sacred-cows-must-survive-the-rewrite).
- **Single-entity flight model.** Glider, tow, and motor flights are all one `Flight` row, discriminated by `FlightAircraftType`. Tow plane references its towed glider via `TowFlightId`. Validation recurses through the link.
- **Accounting rules engine is a decrement loop.** `DeliveryItemRulesEngine.cs` repeatedly applies matching `FlightTime` rules until none match; each iteration decrements `ActiveFlightTime` on a shared accumulator. This is the mechanism for tiered/chunked billing. Customer-configurable; behavior must be bit-exact in the rewrite.
- **Multi-tenancy is convention, not framework.** No EF global filter for `ClubId`. Every service is expected to call `CurrentAuthenticatedFLSUserClubId` on every query. Seed marks structural enforcement as **non-negotiable** for the rewrite.
- **User vs. Person split.** `User` is a login principal scoped to one `ClubId`. `Person` is a human, can be in multiple clubs via `PersonClub`. Collapsing them breaks multi-club pilot rosters.
- **OAuth bearer, 14-day, no refresh.** Tokens sit on `$http.defaults` in the SPA; sessionStorage only; no global 401 interceptor; recovery depends on next route change re-running `userAuth` resolve guard.
- **Per-action cache invalidation.** Client-side `$resource` services attach response interceptors that clear specific `$http` cache entries on mutating calls. No global cache strategy.
- **i18n is server-loaded.** `angular-translate` URL loader against `/api/v1/translations`. Translations live in DB (`LanguageTranslation` table). Changes don't require a client rebuild.
- **DB schema is hand-rolled.** `flsserver/database/FLS/Updates/DBUpdate_v*.sql` is the production schema source of truth. EF Code First migrations exist in `FLS.Server.Data/Migrations/` but are not the runtime driver.

## 4. Integration map

### Inbound
| Caller | Contract | Auth | Owning repo |
|---|---|---|---|
| `flsweb` SPA | REST under `/api/v1/*` (~46 controllers), bearer token | OAuth2 password grant, 14-day token | this repo |
| OGNAnalyser | **Direct SQL writes** to `Flight` + related tables | DB credentials | [sgacond/OGNAnalyser](https://github.com/sgacond/OGNAnalyser) |
| External cron (`FLS.Workflow.Activator`) | `GET /api/v1/workflows/*` | OAuth2 bearer (admin user) | this repo (`flsserver/src/FLS.Workflow.Activator`) |
| Public registration forms | `POST /api/v1/trialflightregistrations`, `POST /api/v1/passengerflightregistrations` | none | this repo (frontend embeds) |

### Outbound
| Target | Contract | Auth | Owning repo |
|---|---|---|---|
| SMTP server | RFC 5322 messages via `System.Net.Mail.SmtpClient` | smtp creds | n/a |
| Proffix accounting system | Polled by external sync via `/api/v1/deliveries/*` GETs | OAuth2 bearer | [arminstutz/PROFFIX-FLS-Sync](https://github.com/arminstutz/PROFFIX-FLS-Sync) |
| OGN aircraft DB | HTTP GET (public) — `AircraftDatabaseSyncJob` | none | OGN DDB |

**Note:** `FLS.Server.ProffixInvoiceService/` in this repo is a stub (only `Properties/AssemblyInfo.cs` and `packages.config` committed). All Proffix logic lives in the external sync repo.

## 5. Data model summary

EF Code First with ~60 entity types in `FLS.Server.Data/DbEntities/`. Schema mostly driven by hand-written SQL scripts (`database/FLS/Updates/DBUpdate_v1.8.0.sql` through `v1.8.11.sql`, 11 scripts on top of a 40-table base in `2 Alter Database.sql`).

Clusters and central entities:

- **Identity & access.** `User`, `Role`, `UserRole`, `Person`, `PersonClub`, `Club`, `ClubExtension`, `ClubState`. The `User`/`Person`/`PersonClub` triad is the load-bearing shape — see seed sacred cows.
- **Aircraft & resources.** `Aircraft`, `AircraftType`, `AircraftState`, `AircraftAircraftState`, `AircraftOperatingCounter`, `Article`. ~15 aircraft seeded in `FLSTest`.
- **Flight operations.** `Flight`, `FlightCrew`, `FlightCrewType`, `FlightType`, `FlightCostBalanceType`, `FlightAirState`, `FlightProcessState`, `InOutboundPoint`, `Location`, `LocationType`. `Flight` is the largest table; one row per glider/tow/motor flight.
- **Reservations & planning.** `AircraftReservation`, `AircraftReservationType`, planning-day entities (in `DbEntities/` but list truncated; planning-day data persists per `(location, date)` with assigned crew references).
- **Accounting.** `AccountingRuleFilter`, `AccountingRuleFilterType`, `AccountingUnitType`, `Delivery`, `DeliveryItem`, `DeliveryCreationTest`. Rule filter config drives `DeliveryItemRulesEngine`; deliveries flow `Prepared → Booked`.
- **Reference / dropdown data.** `Country`, `CounterUnitType`, `LengthUnitType`, `ElevationUnitType`, `MemberState`, `PersonCategory`, `LocationType`, `Language`, `LanguageTranslation`, `StartType`, `EmailTemplate`, `ExtensionType`, `ExtensionValue`, `SettingsEntity`-likes. Mostly small static tables; `LanguageTranslation` is the largest (one row per (locale, key)).

Cross-cluster constraints worth noting for any schema reshape:
- `Flight.TowFlightId → Flight.FlightId` self-reference (glider ↔ tow link).
- `Flight.GliderPilotPersonId / FlightInstructorPersonId / TowPilotPersonId → Person`, indirectly through `PersonClub` for tenancy. **A flight crew member can be a Person from a club other than the operating club** — splitting the schema by `ClubId` is non-trivial.
- `Delivery.RecipientPersonId → Person`, same cross-tenant possibility.
- `AccountingRuleFilter` carries per-club config but references master data (`Aircraft`, `FlightType`) that is also per-club — clean per-tenant export requires walking these graphs.

## 6. Build, test, and ops surface

### Server
- **Build:** MSBuild + NuGet on Windows; `xbuild` on Linux/Mono (see [`TESTING.md`](../../TESTING.md) for the Linux playbook). Target: .NET Framework 4.5.
- **Runtime:** IIS in production; Mono 6.12 console host (`FLS.Server.Console`) on Linux for the demo path.
- **DI:** Unity (`UnityConfig.cs`).
- **EF:** version 6.2.0, `EntityFramework.SqlServer.dll` must be copied next to bin under Mono.
- **Vendored libs:** `Alpinely.TownCrier` (email templating), `Foundation.ObjectHydrator` (test data) — both checked into source rather than NuGet.
- **Logging:** NLog (`NLog.config` per host).
- **Tests:** MSTest in `FLS.Server.Tests`. Test settings file at `src/FLS.Server.Tests.runsettings`. Coverage is uneven — service tests dominate, controller tests sparse.

### Client
- **Build:** Yarn 1 + Webpack 1.12 + Babel `preset-es2015`. Node **8.17.0** (pinned via the Yarn ecosystem; never been ported forward).
- **Tests:** Karma 0.13 + Jasmine 2.4, headless Chrome. Single spec gating done by editing `src/index.spec.js`.
- **Dependencies:** AngularJS 1.4.1, `angular-ui-bootstrap` 0.13, `ng-table`, `selectize`, `pikaday`, `highcharts` 0.0.11 (an oddly old wrapper), `moment` ^2.22, `lodash` 3.10. Many pre-1.0 packages.

### Database
- **Engine:** SQL Server 2022 Developer for the demo / e2e; production engine assumed SQL Server but not asserted in this repo.
- **Migration:** hand-rolled SQL scripts in `database/FLS/Updates/` (versioned) + `database/FLSTest/{1 create, 2 alter, 3 insert}/` reference tree. 11 update scripts on top of the base alter file. Seed in `3 insert/`.
- **E2E fixture:** `database/FLSTest/3 insert/_test-fixture.sql` anchors timestamps to a fixed `2026-01-01` base so time-gated states are reachable without clock manipulation.

### CI / dev infra
- **GitHub Actions:** one workflow, `.github/workflows/e2e.yml`. Runs the Playwright suite. No server / client unit-test workflow detected.
- **Docker:** `docker-compose.yml` brings up SQL Server + Mailpit (for the e2e). Server and client are still started manually outside compose — see comments in `docker-compose.yml`.
- **`.travis.yml`** (per `CLAUDE.md`) at one point used Node 8 — Travis migration status unknown.

### Acceptance smoke
The "T3 sequence" in `TESTING.md` is the minimum bar for "the system runs": POST `/Token`, GET `/users/my`, GET / PUT a flight, re-read to confirm persistence. If T3 passes, server + DB + auth + EF are all wired. The new system's first parity check should be a T3 equivalent.

## 7. Risk hotspots

### R1 — Multi-tenancy enforced by convention
Every query goes through a service that *should* filter by `CurrentAuthenticatedFLSUserClubId`. Nothing structural prevents a forgotten filter from leaking another club's data. Test `25-multi-tenant-isolation.spec.ts` is the only line of defense and only covers a sampled subset. **Seed mandates structural enforcement in the new system** — this is a sacred cow but worth restating because it's the single largest correctness risk in the rewrite.

### R2 — Time-gated state machine in unit-of-business-days
`FlightProcessState.Locked` requires `≥ 2 days` of age; `DeliveryPrepared` requires `≥ 3 days` past lock. A fresh DB appears broken end-to-end. The e2e fixture works around this with backdated seed data. New system must preserve the gates *and* provide a way to test them without clock manipulation.

### R3 — Accounting rules engine: parity-critical, customer-configurable
`DeliveryItemRulesEngine` runs a stateful decrement loop over rule objects instantiated from per-club DB config. Re-implementing this from scratch in a new language is the single highest-risk story in the modernization. `DeliveryCreationTest` is the regression harness; we will need a *much* larger corpus of tests before cutover than is committed today.

### R4 — EPPlus license boundary
EPPlus changed from LGPL to Polyform Noncommercial post v4.5. Current pinned version not verified in this pass — the new Excel exporter ADR must either confirm we are on a free version or pick a different library (OpenXML SDK, ClosedXML). Output format compatibility with Proffix sync is the parity constraint.

### R5 — `FlightStateMapper` enum duplication
`flsweb/src/flights/FlightsServices.js:117-199` hardcodes server enum values as JS strings. Any drift is a silent UI bug. New system must derive client-side state names from a single source (codegen from OpenAPI, shared TS types, etc.).

### R6 — CORS wide open
`origins: "*"` in `WebApiConfig`. New system must scope CORS to the actual deployment origins. Cheap to fix in the rewrite; flagged so an ADR captures the policy.

### R7 — Hand-rolled SQL migration baseline
11 `DBUpdate_v*.sql` scripts layered on top of a 40-table base alter file. Any new migration tool (EF Core, FluentMigrator, DbUp, Flyway, Liquibase) needs a parity baseline derived from the existing schema. The baseline-snapshot step is its own story.

### R8 — Stubbed Proffix integration in-repo
`FLS.Server.ProffixInvoiceService/` exists but contains only assembly metadata. The actual sync is the external `PROFFIX-FLS-Sync` repo. New system must serve a Proffix-compatible API or coordinate a rebuild on the sync side. Out of scope per seed, but a compatibility ADR is required.

### R9 — OGN inbound contract is direct DB writes
OGNAnalyser writes flights straight to the SQL schema. Replacing the SQL schema (R7) implies either preserving the schema for OGN or providing a new ingestion endpoint and updating OGNAnalyser. The latter is owned by a different person/repo.

### R10 — OAuth bearer with no refresh, no 401 interceptor
Tokens are 14 days, sessionStorage only, no proactive refresh; the client has no global 401 handler. UX-wise this is invisible drift between client and server. New auth scheme (OIDC, sliding refresh, or short-lived JWT + refresh token) must address this in the auth ADR.

### R11 — Vendored email-templating lib (`Alpinely.TownCrier`)
Checked into source instead of NuGet. License and provenance unverified in this pass. Replace with a maintained equivalent during the rewrite (or extract templates first to make the email-system rewrite tractable).

### R12 — `||` tautology bug in nav-bar visibility (`index.js:50`)
Cosmetic but real. New system must hide the nav bar on public routes by an actual mechanism (route flag, layout slot, etc.) instead of the broken expression.

### R13 — Test coverage gaps in scheduled jobs
No e2e covers `DeliveryMailExportJob`, `AircraftStatisticReportJob`, or `AircraftDatabaseSyncJob`. These are exactly the jobs that are hardest to verify post-cutover. Pre-cutover, add either e2e or integration tests; otherwise we ship blind.

## 8. Open questions for phase 2

Drive elicitation in `/modernize-vision` with these. Don't ask the user to confirm anchors already locked by [`00-seed.md`](00-seed.md).

- **Performance targets.** What page-load and request-latency budgets must the new system hit? (Current system has none recorded.)
- **Availability target.** What uptime SLO are we committing to? What's the current observed availability?
- **Acceptable cutover downtime.** Seed locks "hard cutover" — but what window is OK? Minutes? Hours? A weekend?
- **Hosting target.** Production today is presumably IIS on Windows; is the new system constrained to Windows, allowed on Linux, or required to run in a specific cloud?
- **Budget / team shape.** Solo developer? Team? What languages does the team know — does that pin the backend ADR?
- **Compliance.** Any regulatory obligation (GDPR retention, Swiss data residency, FOCA aviation rules) the new system must demonstrably satisfy?
- **Internationalization.** Server-loaded translations stay, or move to client-bundled? Add languages?
- **Reporting / Excel parity.** Are existing Excel exports byte-for-byte parity-required (e.g., because Proffix sync parses them), or just feature-equivalent?
- **OGN integration.** Keep direct-DB ingestion, or are we willing to negotiate an ingestion API with the OGNAnalyser maintainer?
- **DB scope.** Seed makes the DB conditionally in scope. Confirm: is reshape allowed if we have a migration path, or must the new system run against the existing schema unchanged for a parallel period?
- **Auth migration.** Are existing user sessions / tokens expected to survive cutover, or are users re-prompted to log in on day 1?
- **Test-corpus expansion for the rules engine.** What's the budget (time / data) for building the `DeliveryCreationTest` corpus before cutover?
- **Out-of-scope features.** Are there features in §2 that we're explicitly **not** porting (deprecating)?

---

## Skill self-report

- Features inventoried: **~50** across 8 domains (identity, master data, flight ops, reservations & planning, accounting, public flows, email/jobs, dashboard).
- Integrations: **4 inbound, 3 outbound**.
- Risks recorded: **13** (R1–R13).
- Open questions for phase 2: **13**.
- Suggested next command: `/modernize-vision`.
