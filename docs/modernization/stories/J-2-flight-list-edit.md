---
id: J-2
title: Flight list + edit forms (airfield hot path)
epic: E-07
status: done  # PR #204 — ci GREEN (18 specs) + real-export fanout GREEN; full 6+ paired legacy↔AlpenFlight screenshots (list/form/airmovements/wizard/motor) in a collapsible accordion gallery; producer SELECT vs MSSQL validated. gap-hunter SHIP. Mocked seams: none. done_at 2026-06-04.
started_at: 2026-06-03
done_at: 2026-06-04
journey0: false
carved: true
depends_on: [J-1]   # J-1 = aircraft register (flights FK aircraft); inherits J-0/J-0b migration-fan-out infra
rolls_up: [S-061, S-062d, S-062e, S-062f, S-062h, S-062i, S-064]  # reuses impl S-062a/b/c, S-063, S-067
acceptance:
  - Glider flight create via the 3-step wizard (Launch → Glider → Tow when start-type = aerotow); the new flight appears in the /flights list. [happy]
  - When start-type is aerotow, the tow flight is created + linked in the SAME save (paired glider↔tow): the glider row carries `towFlightId` to a distinct TOW-type row in the same club (S-063 parity). [happy]
  - Edit a flight; changes persist and re-render in the list. [happy]
  - Motor flight create/edit/list in the **unified /flights list** (a flight with a motor aircraft + no tow; legacy's separate /airmovements screen is NOT carried forward) (S-064, reinterpreted T-36). [happy]
  - Delete a flight; it leaves the list (CLUB_ADMINISTRATOR gate). [happy]
  - Tenant scope: a caller sees only their club's flights; a cross-tenant flight GET 404s (J-0 pattern). [key-error]
  - Time-gate (S-061, sacred cow): a flight too recent cannot transition Valid→Locked; one past the threshold can, and a Locked flight is read-only in the edit form. Boundary exact at the day gate. [key-error]
  - Optimistic concurrency (net-new affordance): a stale PUT (If-Match with an old version) returns 412 and opens the inline per-field conflict diff (keep-mine / keep-theirs, no auto-retry); a 409 state-gate reject shows a "reload latest" toast (S-062h). [key-error]
  - Real legacy Flight + FlightCrew data migrates into AlpenFlight and a migrated flight (with crew + tow link) renders in the owning club's /flights list — full legacy→migrate→Keycloak→UI chain green (reuses J-0c's harness; the real export validates the producer SELECT). [happy, real-data]
screen: /flights (+ /flights/new, /copy/:id, /:id/edit) — feature folder features/flights/ — replacing legacy flsweb/src/flights/ + flsweb/src/flights/airmovements/ (the legacy /airmovements split is NOT carried forward: motor flights are unified into /flights, T-36)
headless_pulled_in: FlightGatePolicy time-gate beans + injected Clock (S-061) — pulled in by this screen, asserted per-flight via the process-state transition + locked-flight read-only. The BULK LockFlights / DeliveryCreation jobs do NOT home here — they ride J-15 (jobs console) / J-10 (deliveries) per the roadmap headless table.
migration: Flight, FlightCrew (tow self-FK two-pass UPDATE; air-state lossy — only the FlightPlanOpen timestamp survives, accepted per S-060)
parity_test: alpenflight/web/e2e/tests/real-idp/flight-migration-parity.spec.ts
adr_refs: [0005, 0007, 0008, 0014, 0015, 0022, 0024]
---

## Context

The flight list + edit screen is **the** airfield hot-path screen (vision §C21–C24,
2026-05-15b amendment) — the one pilots and ops touch every flying day. A club records
glider flights (with an optional linked tow), motor "air movements", and edits/locks/bills
them through the process-state machine. Every downstream journey leans on it: J-3 dashboard,
J-5 reservations, J-7 reports, J-9/J-10 accounting all read flights. It's the first journey
after the masterdata pair (J-0 locations, J-1 aircraft), so it runs the **full real
legacy→migrate→Keycloak→UI chain** for `Flight` + `FlightCrew` as its done bar — the first
*transactional* (non-masterdata) entity to migrate end-to-end.

## Spec must assert

Grounded in legacy `flsweb/src/flights/` (list `flights.html`, glider form
`flight-edit-glider-form.html`, tow form `flight-edit-tow-form.html`) +
`flsweb/src/flights/airmovements/` (motor `air-movements.html`), the server gates in
`flsserver` (`FlightService.cs`, `Accounting/DeliveryService.cs`), and the existing rewrite
backend `FlightsController` (`/api/v1/flights`: GET list keyset-cursor / `/{id}` /
`/new-template` / `/{id}/copy-template` / `/last-context`, POST, PUT/{id} with If-Match→412,
DELETE/{id}, PATCH `/{id}/process-state`) + the `Flight` aggregate (`@Version`, `linkTow`,
computed `airState()`, `transition()` over `FlightTransitionMatrix`):

- **Glider + tow + motor CRUD** (happy): 3-step wizard (Launch / Glider / conditional Tow);
  paired glider↔tow save when start-type = aerotow (`needsTowplane`); motor flight
  create/edit/list in the **unified /flights list** (a flight with a motor aircraft + no tow;
  the MOTOR discriminator is inferred from the selected aircraft — legacy's separate
  /airmovements screen is NOT carried forward, S-064 reinterpreted T-36); edit persists; delete
  (CLUB_ADMINISTRATOR). List columns mirror legacy `flights.html` (air-state + process-state
  badges, date, immat, pilot/second-crew, start location, start/landing/duration, tow cols).
- **Tenant scope** (key-error): own-club only; cross-tenant flight GET 404 (J-0 pattern).
- **Time-gate** (key-error, S-061 sacred cow): too-recent flight can't lock; past-threshold
  flight locks; Locked flight read-only (legacy `CanUpdateRecord=false`). Boundary exact at
  the day gate, "today" driven by the injected `Clock` (e2e fixture anchors 2026-01-01).
- **412 conflict diff** (key-error, S-062h): stale `If-Match` PUT → 412 → inline per-field
  keep-mine/keep-theirs dialog, no auto-retry; 409 state-gate reject → "reload latest" toast.
- **Real-data parity** (happy, real-data): a migrated legacy flight (crew + tow link) renders
  in the owning club's `/flights` list, post full chain.

**Parity to confirm at ship time (do NOT guess — dispatch `legacy-oracle`):**
- **Time-gate KEY column (load-bearing).** S-061's ACs say lock on `flight.flight_date ≤ today-2d`
  and bill on `flight.locked_at ≤ today-3d`. **Legacy disagrees:** `FlightService.cs:1157,1164`
  locks on `CreatedOn ≤ Today-2d` (record-creation date, `TruncateTime`) gated on
  `ProcessStateId == VALID(30)`; `DeliveryService.cs:65,97` bills on `CreatedOn ≤ Today-3d`
  gated on `LOCKED(40)`. `CreatedOn` (entry date) ≠ `flight_date` (flying date) ≠ `locked_at`.
  Resolve which key is correct before writing the boundary spec — `CreatedOn` is the legacy
  behavior; S-061's wording is likely a refinement simplification. This decides the gate.
- **Lock prerequisite.** Legacy lock also requires `ProcessStateId == VALID`; confirm the
  Valid prerequisite + whether J-2 proves the gate per-flight (PATCH process-state) vs needing
  the bulk job (homed on J-15).
- **412 is NET-NEW, not parity.** Legacy `Flight.cs` has **no** RowVersion/concurrency token —
  last-write-wins (oracle-confirmed). The `@Version`/If-Match/412 path + the conflict-diff UX
  (S-062h) are a new-stack affordance the rewrite adds; assert the *new* behavior, don't seek
  legacy parity for it.
- **af-date-picker range mode (S-062e).** S-062b reportedly worked around a zoneless deadlock
  by splitting the filter into two single pickers; the current code appears to use range mode.
  Verify whether the deadlock is fixed/reverted or still worked around before asserting the
  range filter (and whether `/dev/primitives` range showcase loads).

## Notes

- **Backend + frontend largely exist** (S-062a/b/c, S-063, S-067 are `implemented/`): `Flight`
  aggregate w/ `@Version` + If-Match→412 + `linkTow`/`unlinkTow` + computed `airState()` +
  `transition()`; `FlightsController` (all CRUD + templates + last-context + process-state);
  the NgRx signal store (`flight.store.ts`) already tracks `saveConflict` (412); the 3-step
  wizard + paired-create coordinator + `flight-form.model.ts`. **Net-new for J-2:**
  - **S-061** — `FlightGatePolicy` bean (`canLock`/`canBill`) + injected `Clock`, wired into
    the transition service; boundary unit/IT (1s before = reject, 1s after = allow).
  - **S-062h** — the **412 conflict-diff dialog** (`flight-conflict-prompt.component.ts`) +
    `FlightStore.save` wiring (store *tracks* the conflict; the diff UI isn't built) + 409 toast.
  - **S-064** — `/airmovements` route + page reusing the shared list/form parameterized by the
    MOTOR filter (legacy duplicated; rewrite must NOT — one parameterized list/form, two routes).
  - **S-062e** — af-date-picker `mode="range"` zoneless fix; **S-062f** — `flights-list.spec.ts`
    air-state dropdown reliability (green without retries).
  - **Flight + FlightCrew migration proof** (below).
- **Done bar = real chain** (retro Q1, [[feedback_demonstrable_proof_prefer_ui]]). The
  `FlightMapper` + `FlightCrewMapper` (Group.FLIGHT) **exist** from J-0b's per-entity authoring
  but are **UNPROVEN end-to-end** ([[verify_infra_is_run_not_just_authored]]) — size as
  build/verify, not wire. The **real legacy export validates the producer SELECT** that synth
  bundles can't ([[project_synth_bundle_doesnt_validate_producer_select]] — exactly the J-1 T-16
  catch; expect ≥1 Flight producer-column mismatch). Reuse J-0c's legacy→migrate→Keycloak→
  AlpenFlight harness + per-club video; synth-at-PR / real-export-at-nightly (J-0c/J-1 split).
- **Migration shape (parity-sensitive):** tow self-FK is NOT in `foreignKeys()` (would break
  the ArchUnit ingest-order rule) — it's a **two-pass UPDATE** (first pass writes the legacy
  GUID, second resolves after the FLIGHT pass walks `legacy_id_map_flight`; soft-deleted tow-ref
  keeps both rows tombstoned). Air-state is **lossy** — legacy `AirStateId` is not migrated
  (air-state is computed); only the `FlightPlanOpen(5)` timestamp survives → `flight_plan_opened_on`
  (accepted, S-060). Operating-club is **per-flight** (not denormalized from aircraft — charter
  case). Aircraft FK resolves cross-tenant (TENANT_BYPASS); Location FK is tenant-scoped (fan-out
  replica per operating club). **Carry forward the J-1 flag:** the J-1 `OwnerId`-vs-`AircraftOwnerClubId`
  fidelity note — weigh it where a migrated flight resolves its aircraft FK replica.
- **Headless homing:** the time-gate **policy beans + Clock** home on THIS screen (gate proven
  per-flight via the process-state transition + locked-flight read-only). The **bulk** LockFlights
  / DeliveryCreation **jobs** do NOT — they ride J-15 (jobs "run now") / J-10 (deliveries) per the
  roadmap headless table. Don't build a bulk-lock job here.
- **Deferred breadth — size at ship** ([[feedback_vertical_slices_first]]): the J-2 **gate** is
  glider+tow+motor CRUD + time-gate + 412 diff + paired save + migration real-chain. The
  pure-breadth ACs inside the rolled-up polish stories ride a later journey / boyscout, NOT the
  J-2 gate: **offline IndexedDB cache + service-worker sync + marginal-3G** (S-062d AC-DIR-6/7,
  S-062h SW-queue replay / re-auth / 3G), **saved-filter recency chips** (S-062d AC-DIR-8),
  **mobile-first card layout + sticky chrome + density variants** (S-062d AC-DIR-1..5),
  **cross-browser Ctrl+D matrix + 1–5 quick-select + slide-in focus jump + Shift+? help**
  (S-062i). `/do-ship` decides which slices ride the gate vs defer to a named follow-up.
- **Boyscout riders to fold** (from `_BOYSCOUT.md` — they ride this journey, not own stories;
  `/do-ship` sizes + clears the bullets):
  - **ci.yml docs-only path-filter** — skip the heavy `alpenflight build` + `alpenflight-proof`
    on docs/skill/story-only pushes (`docs/**`, `.claude/**`, root `*.md`), keeping the `required`
    aggregator green via the skipped-to-success pattern. (J-1 burned many cycles re-running the
    ~7-min proof on doc-only pushes.) *(seam: ci.yml path filter + required aggregator)*
  - **modernize-\* sunset** — J-2 is the **first non-migration-flavored feature journey**, which
    is the rider's "after do-* ships one non-migration journey" condition. Delete the 9
    `modernize-*` skills + ~12 modernize agents + prune the `rolled_up_into:` horizontal stories
    (47 `implemented/` stay as history). Mechanical, however many files. *(seam: .claude/skills/modernize-*,
    .claude/agents/*, rolled_up_into stories)* — fold only if it doesn't bloat the gate PR; else
    leave for the next feature journey.
- **Seam hints for `/do-ship`** (non-binding, one seam each): `FlightGatePolicy` bean + `Clock` +
  transition wiring (S-061) · 412 conflict-diff dialog component + `FlightStore.save` wiring +
  409 toast (S-062h) · `/airmovements` route + motor-filter param on the shared list/form (S-064) ·
  af-date-picker range zoneless fix (S-062e) · `flights-list.spec.ts` air-state dropdown
  reliability (S-062f) · Flight + FlightCrew migration bindings + producer SELECT + round-trip IT ·
  real-idp `flight-migration-parity.spec.ts` + flight parity bundle seeder · each boyscout rider.

## Parity decisions (ship-time — legacy-oracle 2026-06-03 + operator)

Resolved against legacy (`flsserver` `FlightService.cs`, `Accounting/DeliveryService.cs`,
`MappingExtensions.cs`) + operator adjudication:

- **Time-gate KEY — DELIBERATE DIVERGENCE from legacy (operator).** Legacy keys **both** gates on
  `CreatedOn` (record-entry date, `TruncateTime`'d, `DateTime.Today` local) — `FlightService.cs:1157,1164`
  (lock ≥2d) + `DeliveryService.cs:65,97` (bill ≥3d); there is **no `locked_at` column** in legacy.
  Operator chose S-061's wording instead: **lock on `flight_date ≤ today-2d`, bill on `locked_at ≤
  today-3d`** (lock 2 days after the flight flew, bill 3 days after locking). This **changes which
  flights become billable and when** → needs a **`locked_at` column (net-new, set on Valid→Locked)**
  + an **ADR amendment recording the divergence** (flag for `/do-retro`; do-ship does not auto-edit ADRs).
  `created_at` stays migratable for audit but no longer drives the gates.
- **412 / optimistic concurrency — NET-NEW, not parity.** Legacy `Flight` has no row-version/concurrency
  token (last-write-wins, oracle #25). The `@Version`/If-Match/412 + conflict-diff (S-062h) is an
  intended improvement; assert the *new* behavior.
- **Read-only rule — only `DeliveryBooked(60)`, NOT `Locked`** (`FlightService.cs:1276`, oracle #12).
  A Locked flight is still editable (the manual state-transition path relies on it). The
  `docs/legacy/server.md` "no edits when locked" prose is wrong — code wins. **Correct the J-2 AC:**
  the read-only assertion targets a `DeliveryBooked` flight (edit/delete → 4xx), not a Locked one.
- **Single-flight tenant scope — close the legacy leak (oracle #24).** Legacy `GetFlight(id)` /
  update / delete load by id with **no ClubId filter** (same hole J-1 found for Aircraft) → cross-tenant
  edit/delete possible. New stack scopes structurally (`@TenantId`, ADR 0008); assert cross-tenant 404.
- **Legacy bugs to NOT reproduce (assert corrected):** glider↔tow start-time/location mismatch →
  legacy `ValidationException`→HTTP **500** (oracle #15) should be a 4xx field error; tow-orphan on
  edit-away-from-towing (`UpdateFlightDetails` nulls the nav but skips the cascade `DeleteFlight` does,
  oracle #17) → new stack must delete/clean the orphan (assert no dangling tow row); empty-guid FKs
  (`00000000-…`) → null/absent (oracle #18).
- **Validate flow (reference, J-15 owns the bulk action):** `ValidateFlights` is club-wide,
  best-effort, always returns 200 (oracle #6,#9). J-2 proves the gate per-flight via the existing
  `FlightStateTransitionService` (PATCH `/{id}/process-state`); the bulk validate/lock **jobs** ride
  J-15. Manual transition matrix (oracle #11): only the legal source→target pairs transition; others 400.

## Tasks

Verify-wire-prove journey — backend CRUD/concurrency/tow-link/transition (S-062a/b/c, S-063, S-067,
S-059) + the 3-step wizard + the Flight/FlightCrew mappers all **exist**; net work is the time-gate
(net-new `locked_at` + policy), the 412 conflict UI, `/airmovements`, the migration **proof** (expect a
producer-SELECT catch like J-1 T-16), the real chain + gallery, and folded riders. Ordered:

- [x] **T-01** — Real-idp Playwright spec **stub**: author `alpenflight/web/e2e/tests/real-idp/flight-migration-parity.spec.ts` structure + selectors + flow steps with **thin** assertions (clean-seed: glider create via 3-step wizard, paired tow, motor at `/airmovements`, edit, delete, cross-tenant 404, time-gate lock + `DeliveryBooked` read-only, 412 diff; then migrated-flight render). Commits the screen shape. *(seam: one spec file)*
- [x] **T-02** — S-061 **time-gate backend** (operator divergence): `FlightGatePolicy` bean (`canLock`: `flight_date ≤ clock.today()-2d`; `canBill`: `locked_at ≤ clock.today()-3d`) + injected `java.time.Clock` bean; net-new **`locked_at` column** (Flyway V-migration + `Flight` field set on the Valid→Locked transition); wire the gate into the existing `FlightStateTransitionService` (reject too-recent lock/bill); boundary unit/IT (1d before reject, on/after allow, `Clock.fixed`). Record the divergence in this file's Parity decisions (done) + leave the ADR-amendment flag for `/do-retro`. *(seam: FlightGatePolicy + Clock + locked_at migration + transition wiring + boundary IT)*
- [x] **T-03** — **Single-flight tenant scoping**: verify/confirm GET/PUT/DELETE `/{id}` scope by `@TenantId` (close the oracle #24 legacy leak); add a cross-tenant **404** IT (`FlightsAuthorizationIT` or equivalent). If the repo already filters via `@TenantId`, this is verify + test only. *(seam: single-id read/write path + tenant IT)* — **Already scoped, no production change.** All three single-id paths route through `FlightRepository.findByIdWithCrew` (JPQL `select f from Flight f where f.id = :id`), and `@TenantId` on `Flight.operatingClubId` makes Hibernate add the tenant predicate to every read → cross-tenant `findById` returns empty → `FlightNotFoundException` → 404. The existing `FlightsTenantIsolationIT` already asserted GET-404; extended it with PUT-404 + DELETE-404 (each also asserts the owning club's row is left untouched / not soft-deleted).
- [x] **T-04** — S-062h **412 conflict-diff dialog + 409 toast** (frontend, gate slice only — NO drafts/SW/3G breadth): `flight-conflict-prompt.component.ts` (`<af-dialog>` per-field keep-mine/keep-theirs, first conflict focused, Enter activates, **no auto-retry**) + wire `FlightStore.save` 412 → dialog (replace the placeholder toast), 409 → "reload latest" toast; Vitest for the conflict resolver. *(seam: conflict-prompt component + FlightStore.save wiring)* — **Done.** New `conflict-resolver.ts` (pure): the 412 ProblemDetail carries only `serverVersion` (no field values — `FlightsExceptionHandler.handleVersionMismatch`), so the store re-GETs the server detail and `computeFieldDiffs(mine, theirs)` diffs the submitted update vs the stored row, returning only changed fields. `FlightStore.updatePair` now distinguishes **412 → `saveConflict: FlightConflict`** (opens the inline diff; also refreshes `current`/`currentVersion` so the user's explicit resubmit If-Matches the fresh version — NO auto-retry) from **409 → `reloadConflict: true`** (state-gate/optimistic-lock → non-blocking toast, never the diff); re-GET failure degrades 412→reload toast. New `flight-conflict-prompt.component.ts` (`<af-dialog>`-backed, transloco `flight.conflict` scope, per-field keep-mine/keep-theirs buttons, first field's keep-mine focused on open via `viewChild` + `effect`, Enter activates the focused native button, default keep-mine, emits `resolved`/`dismissed` — host applies "theirs" picks onto the glider form then resubmits). `af-dialog` gained an optional projected body (`<ng-content>`) + `wide` input (kept backward-compatible). `flights-edit.page.ts` replaced the placeholder `flight-conflict-toast` `<p>` with the prompt component (412) + a 409 reload toast wired to `onReloadLatest()`. i18n `flight.conflict.*` + `flight.reload.*` in de/en/fr/it. **Testids:** `flight-conflict-dialog`, `flight-conflict-field-<name>`, `flight-conflict-keep-mine-<name>`, `flight-conflict-keep-theirs-<name>`, `flight-409-toast`, `flight-409-reload`. Vitest: 3 store specs (412→diff+no-auto-retry, 409→reload-not-dialog, dismiss clears both) + 3 resolver specs (diff-only-changed, omit-unchanged, buildConflict shape). Full `pnpm test` green (264 app + 26 script), `ng lint` + prettier clean. The T-01 stub's `test.fixme`'d 412 case can adopt these testids in T-09.
- [x] **T-05** — S-064 **`/airmovements`** route + motor-filtered **shared** list/form (no legacy-style duplication): one parameterized list/form, the `/airmovements` route binds the MOTOR filter + hides the tow step; nav entry + i18n (de/en/fr/it). *(seam: airmovements route + motor-filter param on the shared components)* — **Done.** Parameterized by **route `data.flightVariant`** (consistent with the existing `data: { showNavBar }` pattern). New `flight-variant.ts` config (pure): `FLIGHT_VARIANTS.{default,motor}` carrying `basePath` (`/flights` | `/airmovements`), `title`, `newLabel`, `aircraftTypeFilter` (MOTOR pin), `createAircraftType` (MOTOR discriminator), `suppressTow`. The shared `FlightsListPage` + `FlightsEditPage` read `resolveFlightVariant(route.snapshot.data)` and specialise: list pins the MOTOR client-filter (effect), hides the aircraft-type dropdown, retargets new/edit/copy/row links to the variant base path, and re-pins MOTOR on "Clear filters" (the pin isn't a user-clearable filter); the wizard hides the tow step via `variantNeedsTow` (motor → always false regardless of start-type), titles "… air movement", and threads `primaryAircraftType: MOTOR` into the snapshot → `snapshotToCreateRequests` emits a MOTOR create with no tow. New `airmovements.routes.ts` mirrors the flights route family (`''`/`new`/`copy/:id`/`:id/edit`) with `data.flightVariant: 'motor'`, registered as a top-level sibling in `app.routes.ts`. Nav entry "Air movements" (`plane-takeoff` icon, registered in `icon-registry.ts`). **Heading i18n key:** `flight.airmovements.title` (de "Motorflüge" / en **"Air movements"** / fr "Mouvements moteur" / it "Movimenti a motore"), plus `.nav` + `.newFlight`. NB: the components render the variant-config literal "Air movements" for the `<h1>` (matching the existing hardcoded-title pattern on this screen — the T-01 stub's `h1` assertion passes); the i18n bucket is added per the AC and stands ready for transloco wiring. Vitest: `flight-variant.spec.ts` (9 cases — variant resolution, MOTOR list pin, MOTOR create discriminator, motor-never-tows vs default-defers-to-aerotow). Full `ng test` green (274 app tests, +9), `ng lint` clean, prettier + tsc parity clean. The T-01 stub's `/airmovements` e2e case is adopted in T-09.
- [x] **T-06** — S-062e **af-date-picker `mode="range"` zoneless fix**: verify whether the deadlock persists; if the list still uses the two-single-picker workaround, fix the primitive + revert to a single range picker; `/dev/primitives` range showcase loads < 2s. *(seam: af-date-picker primitive + flights-list filter revert)* — **REAL FIX (workaround was live).** Verified the flights filter still used the two-single-picker workaround (`flights-list.page.ts` had an explicit "range deadlocks under zoneless" comment + `flights-date-from`/`flights-date-to`). **Root cause:** `af-date-picker`'s `rangeValue()` getter returned a *fresh array reference each CD pass* (`Array.isArray(v) ? v : []`); under zoneless Angular nz-range-picker re-read the "new" `[ngModel]` input every pass, re-normalised, re-scheduled CD → tight busy-loop that froze the main thread (single mode was fine — stable `Date|null` identity). **Fix:** new pure `date-value-bridge.ts` (`rangeArray` returns the SAME array across passes when epochs are unchanged; `toRangeValue` narrows emissions) + component now memoises the range projection via a `computed` + carried `#prevRange` so the picker input identity is stable. Reverted the flights filter to a single `mode="range"` picker (`flights-date-range` testid, one `setDateRange` handler). Added testids to the `/dev/primitives` range showcase. **Tests:** `date-value-bridge.spec.ts` (9 Vitest cases — reference stability, epoch-change mint, null collapse) + a Playwright deadlock-guard smoke `e2e/tests/dev/primitives-date-picker.spec.ts` (mounts < 2s, picks from+to, emits a 2-tuple, page stays responsive) + updated `flights-list.spec.ts` to assert the single range picker. Unit suite 283 green, `ng build --configuration=mock-auth` green, lint + prettier clean. **Boyscout (folded):** the mock-auth AOT build was red on a **pre-existing** T-04 leftover (`flights-edit.page.ts:658` TS2352 — direct cast of the typed control map needs `as unknown as`; the looser test tsconfig hid it, `ng test`/`ng lint` never caught it — [[verify_infra_is_run_not_just_authored]]); fixed forward (one line) so the dev server boots + the Playwright gate can run. **Env note:** the Playwright specs themselves couldn't run on this Alpine/musl box (chromium-headless-shell `atk_*: symbol not found`, exit 127 — no browser launches here); they run in the CI mock-auth chromium project (the journey gate, `/do-ship`'s job).
- [x] **T-07** — **Flight + FlightCrew migration** bindings + producer SELECT: register/confirm FLIGHT (+ FLIGHT_CREW) in `MapperLegacyBindings` with the producer SELECT **reconciled against the real legacy `Flight`/`FlightCrew` MSSQL schema** (expect ≥1 column mismatch like J-1 T-16) + `FlightMigrationRoundTripIT` + `FlightRealProducerRoundTripIT` (tow self-FK two-pass, empty-guid→null, crew nesting, `flight_date`/`locked_at`/`created_at`, lossy air-state→`flight_plan_opened_on`). *(seam: MapperLegacyBindings FLIGHT producer + 2 round-trip ITs)* — **Done.** Producer-SELECT catch (J-1 T-16 class): `FlightMapper` read `OwnerClubId` for `operating_club_id`, but the real legacy `Flights` table has **no such column** — the operating-club source is `OwnerId` (Flight.cs:130, same column the LOCATION fan-out keys); fixed mapper + binding SELECT. Closure: binding FLIGHT forces START_TYPE (SYSTEM_GLOBAL) + FLIGHT_TYPE (FULL_PORT) + FLIGHT_CREW to be bound too (closure guard). Also wired the authored-but-missing resolver declarations: `FlightMapper.foreignKeyColumns()` (`operating_club_id`→CLUB, `start/ldg_location_id`→LOCATION fan-out disambiguated by `operating_club_id`) + `referenceLookups()` (`process_state_id`, `flight_cost_balance_type_id`); `FlightCrewMapper.referenceLookups()` (`flight_crew_type_id`); `FlightTypeMapper.foreignKeyColumns()` (`operating_club_id`→CLUB — second producer-FK catch, surfaced by the migrate-link IT's `fk_flight_type_operating_club_id` violation). Empty-guid (`00000000-…`) FKs → null via new `Coercions.writeOptionalGuidString`. `locked_at` (T-02 net-new): set from `ModifiedOn` for migrated flights `ProcessStateId >= LOCKED(40)`, null otherwise (bill gate never fires on import for still-Valid flights). Tow self-FK resolves for free (FLIGHT non-fan-out → `legacy_guid` preserved as `id`, so a real tow GUID == the migrated tow row's id; no separate two-pass UPDATE needed while id is preserved). **Both ITs FIRST-LIVE-GREEN on real Postgres**; the producer SELECT vs real MSSQL stays the nightly real-export gate (no MSSQL on this box — J-1 caveat).
- [x] **T-08** — **Flight parity bundle seeder + legacy MSSQL flight seed** (mirror `AircraftParityBundleSeeder`): synth migrated-flight bundle byte-aligned with T-07's IT (glider+tow paired, motor, a too-recent + a lockable flight, a `DeliveryBooked` read-only flight, a cross-tenant flight) + Gradle task; the legacy seed driving the real export/video. *(seam: parity bundle seeder + legacy seed)* — deps T-07. — **Done.** `FlightParityBundleSeeder` (test source, byte-aligned with `FlightMigrationRoundTripIT`) builds a synth bundle with **two declared clubs** in one deployment: the owner club carries the glider+tow paired pair (shared flight_date/location, glider.tow_flight_id → tow, FlightPlanOpen→`flight_plan_opened_on`), a MOTOR(4) flight, and a `DeliveryBooked(60)` read-only flight; a **second cross-tenant club** carries one flight (its admin's GET of the owner's flight → 404). Time-gate seed nuance (clock 2026-01-01): glider/tow `flight_date=2025-12-29` (≥2d → lockable); DeliveryBooked `locked_at=2025-12-28` via ModifiedOn (≥3d → billable window) per the mapper's `ProcessStateId>=40` rule. Gradle `seedFlightParityBundle` (mirrors `seedAircraftParityBundle`; args `<pem> <uploadId> <freshnessToken> <clubKey> <out>`, emits `{clubKey,crossTenantClubKey,freshnessToken,bundlePath}`). **Legacy seed: REUSED the existing glider+tow paired flights + crew in `6 Insert Test Flights.sql`** (already glider+tow paired with FlightCrew, FK closure); **ADDED only a MOTOR air-movement** (`HB-WAT`, FlightCode '60' IsForMotorFlights, FlightAircraftType=4 + motor-pilot crew) — the one missing flight kind. FIRST-LIVE = nightly real export/UI render (no MSSQL/full-chain on this Alpine/musl box). Structurally validated: `compileTestJava` green, Gradle task resolves, and the seeder runs end-to-end against a real RSA-4096 handshake key (full NDJSON+tar+manifest+encrypt path, emits the JSON line). The TS `flight-parity-fixture.ts` + spec wiring stays T-09 per the stub's TODO.
- [x] **T-09** — **Thicken** the real-idp spec to full oracle assertions (glider+tow+motor CRUD, cross-tenant 404, time-gate `flight_date` lock + `locked_at` bill + `DeliveryBooked` read-only, 412 diff, migrated-flight render) + fold **S-062f** (`flights-list.spec.ts` air-state dropdown reliability — green without retries). *(seam: the real-idp spec + flights-list spec)* — deps T-02..T-08. — **Done.** Thickened `flight-migration-parity.spec.ts` from the T-01 stub (thin + 3 `test.fixme`) to FULL oracle assertions, all `test.fixme`'d cases un-fixme'd. **Clean-seed describe** (`provisionTwoClubs` + new `seedFlightMasterdata`): the clean realm's club A has NO masterdata (V5 seeds only the bare club row), so the fixture seeds a location + glider flight-type + 2 pilots + glider/tow/motor aircraft through the REAL create APIs (no mocking) in `beforeAll`. The glider create drives **start-type = AEROTOW explicitly** (via the new af-select helper) so the paired-tow branch is deterministic; asserts the glider row renders its immat AND (via the detail API) carries a `towFlightId` to a DISTINCT `TOW`-type row flying the tow aircraft. Motor create/edit/list at `/airmovements` (tow step absent, MOTOR pill). Edit persists + round-trips on reload. Delete leaves the list. Cross-tenant GET → 404. **412 diff:** a same-principal out-of-band PUT bumps the server version between form-load and submit → the stale If-Match PUT 412s → asserts `flight-conflict-dialog` + per-field keep-mine/keep-theirs, first keep-mine focused, the 409 toast absent, and **exactly one PUT before the dialog** (no auto-retry, counted via `page.route`). **Migrated describe** (new `seedFlightParity` fixture → `seedFlightParityBundle` Gradle task, real ingest + KC provision): the migrated glider (crew ≥2 + tow link) renders in the owning club's `/flights`; a SECOND case asserts the seeded **DeliveryBooked** flight is read-only (PUT + DELETE → 4xx, and the list kebab shows `flights-delete-disabled-<id>` not an actionable Delete). **Time-gate handled per T-02 finding:** the Valid→Locked transition is LOCK_JOB-driven (NOT an OPERATOR endpoint) and the day-boundary is proven at `FlightTimeGateIT`, so the e2e does NOT drive a lock — it asserts the read-only CONSEQUENCE on DeliveryBooked (stated in a spec comment so it doesn't read as a missing assertion). **S-062f folded:** extracted `e2e/tests/_helpers/af-select.ts` (`selectAfOption` — opens the nz-select, waits for the freshly-opened option to be VISIBLE, clicks, then waits for the overlay to DETACH; no `waitForTimeout`), which closes the `goBack()`→stale-portal race; `flights-list.spec.ts` now drives both air-state dropdowns through it (green without retries; chromium project is already `retries: 0`). **NO app testids added** — every referenced testid (`flight-conflict-dialog`/`-field-`/`-keep-mine-`/`-keep-theirs-`, `flight-409-toast`, `flights-delete-disabled-<id>`, `flight-edit-startType`) already exists from T-04/T-05. FIRST-LIVE-GREEN is the CI `alpenflight-proof` real-idp run. Structurally validated on this Alpine/musl box (no browser launch): `playwright test --list` discovers all 8 flight cases with zero fixme, the new `af-select`/fixture/spec files are tsc-clean + eslint-clean + prettier-clean (the pre-existing strict-tsc warnings on `flights-list.spec.ts`/`playwright.config.ts`/mock specs are unchanged and Playwright transpiles via esbuild regardless).
- [x] **T-10** — **Legacy flsweb flight video + paired screenshots** (done-bar, e2e-driver): legacy flight list+form video + paired legacy↔AlpenFlight list/form screenshots in the gallery, auto-posted PR link (mirror J-1 T-14/T-19/T-20); wired into `alpenflight-proof-fanout.yml`. *(seam: fanout legacy flight capture + gallery declaration)* — e2e-driver.
- [x] **T-11** — **Boyscout: ci.yml docs-only path-filter** — skip `alpenflight build` + `alpenflight-proof` on docs/skill/story-only pushes (`docs/**`, `.claude/**`, root `*.md`), required aggregator green via skipped-to-success. *(seam: ci.yml path filter + required aggregator)* — clears the `_BOYSCOUT.md` bullet. Root cause was deeper than "filter too broad": on `integration/**` the `pull_request` trigger made dorny diff the WHOLE PR vs `main`, so `changes.next` was always true (branch carries earlier alpenflight/ commits) → doc-only pushes still ran build+proof. Fix: new `changes.docs_only` output from the INCREMENTAL push diff (`github.event.before..after`); heavy jobs gate on `next == 'true' && docs_only != 'true'`, fail-safe toward running.
- [x] **T-13** — **Gate-revealed (T-07):** `ExportCommandSmokeTest.registeredEntitiesMatchTheBoundLegacyEntities()` (migration-tool) fails — T-07 added FLIGHT/FLIGHT_CREW/FLIGHT_TYPE/START_TYPE to `MapperLegacyBindings`, so `ExportCommand.registeredEntities()` now returns them and the test's hardcoded expected list is stale. Update the expectation to include the FLIGHT group + verify `ExportCommand`'s export/producer ordering covers them. *(seam: ExportCommandSmokeTest + ExportCommand entity list)* — **Done.** Test-only fix: `ExportCommand.registeredEntities()` derives the set from `KnownMappers.all()` ∩ `MapperLegacyBindings.isRegistered` (no hardcoded list — Javadoc explicitly says it grows automatically), so T-07's four new bindings flow in with zero code change. Updated the `containsExactlyInAnyOrder(...)` expectation from 11 to 15 entities, adding FLIGHT, FLIGHT_CREW, FLIGHT_TYPE, START_TYPE (the exact set T-07 bound, confirmed by reading `MapperLegacyBindings.BINDINGS`), and the comment now cites J-2 T-07. `ManifestBuilder`/`BundleWriter` carry no stale entity list (both iterate the derived list or `EntityType.values()`). `migration-tool:test` green.
- [x] **T-14** — **Gate-revealed (T-06):** the `primitives-date-picker.spec.ts` deadlock-guard asserts `< 2000ms` mount but a cold CI dev-server load took 2141ms (NOT a deadlock — it settled; a busy-loop never settles). Make the guard distinguish *settles-in-bounded-time* from *loads-fast*: raise the budget to a generous bound that still catches a busy-loop regression (or assert responsiveness via the main-thread round-trip), so it is non-flaky on a loaded CI runner. *(seam: one spec threshold)*
- [x] **T-15** — **Gate-revealed (the gate gap):** ci.yml `alpenflight-proof` runs only the J-0 Locations + J-1 Aircraft real-idp specs (`ci.yml:405-409`); the J-2 `flight-migration-parity.spec.ts` is in the tree but NOT in the proof invocation, so the green `ci` never exercised J-2's real chain (green-for-the-wrong-reason). Wire the flight spec into the same `playwright test` invocation (synth-at-PR mode, mirroring J-1); ensure the flight parity fixture seeds in that job. *(seam: ci.yml proof invocation)* — e2e-driver. **This is the real PR gate for J-2.**
- [x] **T-16** — **Gate-revealed BLOCKER (gap-hunter, high-confidence):** `FlightParityBundleSeeder` hardcodes migrated flight dates to Dec-2025 (chosen for a notional 2026-01-01 e2e clock), but the real-idp backend runs `Clock.systemUTC()` (no fixed clock in `dev`) and `/flights` defaults to a `[today-90d, today]` window — so at the real run date the migrated flights fall OUTSIDE the window and the migrated-render + DeliveryBooked-read-only assertions (the `[happy, real-data]` done-bar) cannot render. Fix: compute the migrated flight `flight_date`/`locked_at` relative to `LocalDate.now()` (lockable ~now-5d, DeliveryBooked flight ~now-10d w/ locked_at ~now-5d, motor ~now-3d) so they are always in-window AND satisfy the gate semantics; keep byte-aligned with `FlightMigrationRoundTripIT`; confirm the migrated spec renders them (default window or an explicit filter). *(seam: FlightParityBundleSeeder dates + IT alignment + spec)* — e2e-driver. **Done.** Replaced the three `2025-12-*` literals + the two date constants with `LocalDate.now(ZoneOffset.UTC)`-relative offsets: lockable glider/tow `now-5d` (≥today-2d lock gate, in 90d window), motor `now-3d`, DeliveryBooked flight `now-10d` w/ `locked_at = now-5d` (≥today-3d bill gate), cross-tenant `now-5d`. UTC matches the backend clock (no TZ skew; silences the `JavaTimeDefaultTimeZone` Error Prone warning). **No spec change needed** — the flights store sends no from/to by default (`flight.store.ts:98-99,131-132`), so the backend's default 90-day window now surfaces every migrated row. **No IT change needed** — `FlightMigrationRoundTripIT` is window-agnostic (direct DB query, asserts its own fixed `2024-06-01`); byte-alignment is on NDJSON field *shape*, which is unchanged. `compileTestJava` green; `FlightMigrationRoundTripIT` green locally (Docker). FIRST-LIVE-GREEN of the migrated-render + DeliveryBooked assertions is the CI `alpenflight-proof` run (T-15 wires the spec in).
- [x] **T-17** — **Gate-revealed (first-live):** clean-seed `:190` fails — the pilot `af-select-option-pn-…` never appears because `seedFlightMasterdata`'s `POST /api/v1/flight-types` surfaced a 500. *(seam: MutationAuditEventListener tenant-resolution + ClubsAuditEventIT)* — **Done.** ROOT CAUSE = **backend audit-tenant bug, NOT a fixture provisioning gap.** The backend log (`backend.log:75-77`) showed the 500 is in `ClubsController.createClub` (the fixture's `provisionTwoClubs`→`createClubB` step, run by the seeded **sysadmin**), not flight-types — Playwright reported the next `postJson` (flight-types) as the visible failure because club B provisioning had 500'd. A real `SYSTEM_ADMINISTRATOR` carries **no `clubId` claim** (`UserPrincipalLookup` logged `column=club_id matches=0` for the sysadmin sub `f1558768`), so the success-path AFTER_COMMIT audit listener (`onCommittedMutation`) let Hibernate's `@TenantId` resolver fill `tenant_club_id` and got back `NO_TENANT` (nil UUID `00000000-…`). The nil has no `t_club` parent → `fk_mutation_audit_event_tenant_club_id` violation → `TransactionSynchronization.afterCompletion threw exception` → the 201 became a 500. `ClubsAuditEventIT` masked it by minting the test sysadmin **with** a `clubId` claim pointing at seed-club-1. Fix: `MutationAuditEventListener.safeWrite` now re-resolves the tenant explicitly in the authenticated-but-empty branch; when it resolves to null/`NO_TENANT` (tenant-less sysadmin — club creation is the cross-tenant system event V9 reserves the nullable column for) it writes the **unscoped row (NULL `tenant_club_id`)** via the existing `writeUnscopedRow` path instead of persisting the nil UUID. Added red→green `ClubsAuditEventIT#tenantless_sysadmin_post_emits_event_with_null_tenant_club_id` (sysadmin token with no `clubId` → 201 + one audit row with NULL tenant). Whole audit IT suite + `ApplicationModulesTest` + `TenantBypassGuardTest` green locally (Docker). FIRST-LIVE-GREEN of `:190` is the CI `alpenflight-proof` real-idp run.
- [x] **T-18** — **Gate-revealed (first-live):** migrated `:560` bundle ingest 409s `DEPLOYMENT_EXISTS` (+ `ux_user_username_lower_alive` dup) — the flight parity fixture ingests as a migration principal that collides with J-0c/J-1's deployment when all three real-idp specs run in one invocation (exact J-1 T-17 class). Give the flight fixture its OWN distinct migration principal (mirror J-1's `clubadmin2` → a new `clubadmin3`: realm-export user + `V__dev_user_seed` row), so disjoint principals = disjoint deployment owners. *(seam: flight-parity-fixture principal + realm-export + dev-user seed migration)* — **blocks the migrated done-bar (with T-16).**
- [x] **T-19** — **Gate-revealed (2nd live run, after T-16/17/18):** the flight real-idp proof still reds on two ADVANCED causes — (a) `:190` clean-seed: `POST /api/v1/flight-types` still 500s at `seedFlightMasterdata` (T-17 fixed club-creation; flight-type create now 500s for its OWN reason — pull the backend stack); (b) `:560` migrated: ingest provisions 2 clubs but `seedFlightParity` owner-detection finds NO club owning the migrated glider on HB-3000, with a flood of `ux_user_username_lower_alive` dups during ingest (the bundle's migrated USER provisioning collides → flight data does not land). Diagnose from the run-26893195357 backend log + trace artifacts; fix both root causes (backend audit/constraint and/or bundle-user uniqueness + owner-detection). *(seam: flight-types create audit/constraint + flight bundle user-uniqueness/owner-detection)* — **blocks done.** — **Done. BOTH causes were in the e2e fixture, not the backend — diagnosed from run-26893195357's full `backend.log` + the `:560` trace.zip API bodies (no guessing).** **(A)** Backend stack (`backend.log:224-290`): `DataIntegrityViolationException` on `ux_flight_type_club_code`, `Detail: Key (operating_club_id, flight_code)=(…001, J2L) already exists` thrown at `FlightTypesService.persist:148`. `seedFlightMasterdata` tags location/persons/aircraft names with a run-scoped suffix but **hardcoded `flightCode: 'J2L'`** — so a Playwright retry's `beforeAll` re-seed (or any 2nd seed against club A) collided on the `(operating_club_id, flight_code)` partial-UNIQUE → 500 → the pilot af-select-option never rendered. Fix: tag the flight-code (`J2L${tag}`, ≤30) like the name. **(B)** The `ux_user_username_lower_alive` "flood" is a **RED HERRING** — it's the JIT materializer's benign insert-or-find race (5 concurrent first-login requests → losers WARN 23505, winner's row found; every cluster ends with a `JIT materialised user row` INFO; present for J-0c/J-1/J-2 alike). The migrated ingest SUCCEEDED (`deployment.provisioned clubCount=2`, both admins logged in) and the owner club's `/api/v1/flights` DID return the glider+tow+motor+DeliveryBooked rows (trace `:560` body). REAL cause: `tenantOwnsMigratedPair` checked `f.towFlightId` off the **LIST** projection, but `FlightListItem` (server `FlightDtos:43` + generated `flightListItem.ts`) carries **NO `towFlightId`** — the tow self-FK is DETAIL-only (`FlightDetail`, `GET /flights/{id}`). So owner-detection always saw `undefined` → no owner, even though the pair ingested + rendered correctly. Fix: resolve the link via the DETAIL endpoint (find GLIDER flights on the migrated glider aircraft → GET each detail → confirm `towFlightId` → a distinct TOW-type list row), exactly as the `:560` migrated-render spec already does. **NO spec/assertion weakened; NO backend change** (both list+detail DTOs are correct as-is — only the fixture mis-read the list). `FlightMigrationRoundTripIT` green on real Postgres (bundle data lands); changed file tsc+eslint+prettier-clean; specs list with zero fixme. FIRST-LIVE-GREEN is the next CI `alpenflight-proof` real-idp run.
- [x] **T-20** — **Gate-revealed (3rd live run — last red):** `:560` migrated render is GREEN; `:190` clean-seed now fails one step PAST the (now-fixed) flight-types 500 — `selectAfOption` cannot see the pilot option `af-select-option-pn-<uuid>` in the wizard crew dropdown (`af-select.ts:49`). The person/crew picker is likely a server-backed typeahead (unlike the small aircraft/flight-type selects), so the option only renders after a typed query, OR the seeded pilot is not returned by the picker endpoint. Pull run-26894448778 trace/error-context for the dropdown state; fix the person-picker selection (type-to-filter helper path and/or ensure the seeded pilot is pickable). *(seam: af-select person-picker interaction + the clean-seed pilot seed)* — **last blocker to green.**
- [x] **T-21** — **Gate-revealed (4th run):** `:560` migrated GREEN; `:190` advanced past the pilot picker (T-20) to `:137` — after `flight-submit-header` click the URL stays `/flights/new` (the paired glider+tow save never completes/navigates). Pull run-26895688605 trace (network + DOM): is the submit blocked by an invalid/missing required field on the glider OR tow side (AEROTOW needs tow aircraft+pilot+type+landing), or did the POST 4xx/500? Walk the ENTIRE `createGliderFlightAerotow` helper (spec:90-138) against the real wizard component + coordinator to find ALL remaining gaps in ONE pass (don't fix only line 137's symptom) so the next run is green. *(seam: createGliderFlightAerotow flow vs wizard required fields/save)* — **last clean-seed blocker.**
- [x] **T-22** — **Gate-revealed (5th run):** `:190` glider GREEN (T-21), `:560` migrated GREEN — now `:260` (motor at /airmovements) fails: 60s test timeout around `:313` `expect(flights-aircraft-type-${motorId}).toContainText('Motor')`. Either the motor create flow (spec `createMotorFlight`, ~260-313) hangs (a missing/ different field on the MOTOR variant, or /airmovements routing/save) or the list type-cell label mismatches ('Motor' vs 'MOTOR'). Pull run-26896883555 trace; walk the whole motor flow vs the /airmovements variant component (T-05) + the list type rendering in ONE pass. *(seam: createMotorFlight flow vs /airmovements variant + list type cell)* — **last clean-seed blocker.**
- [x] **T-23** — **Gate-revealed (6th run):** `:282` motor fails at T-22's fail-fast guard (`:299` `waitForResponse GET /api/v1/flights` 15s) on a Playwright RETRY (×°°°°· pattern). Root suspicion: motorAdmin re-login on retry gets a fresh Keycloak sub but the same username, colliding with the leftover `t_user` row from attempt 1 (`ux_user_username_lower_alive`) → JIT materialize returns empty → tenant-less → list GET never returns. Pull run-26898678147 BOTH attempt traces + backend log; fix DURABLY so the motor test passes on first attempt AND retries — either make `JitUserMaterializerImpl` recover-by-username (idempotent), or the fixture reuse a STABLE sub / purge the stale `t_user` on dispose, whichever is the correct robustness fix. *(seam: motor-test principal/JIT lifecycle — fixture + possibly JitUserMaterializerImpl)* — **last clean-seed blocker.**
- [x] **T-24** — **Gate-revealed (7th run, 3rd on the motor seam):** `:282` still times out at the guard. Backend shows the motor page authenticates as sub `f1558768` which is **stable across THREE separate CI runs**, NEVER materializes a t_user, and T-23 reconcile never fires for it — i.e. the page carries a STALE/wrong sub, not the freshly-provisioned `motorAdmin` (a login session/storageState bleed, or motorAdmin's real sub never hits JIT). **Decisive fix:** stop dynamic-provisioning for the motor clean-seed test; give it a PRE-SEEDED STABLE principal mirroring the proven-green `clubadmin3` (migrated test) — a realm-export user + a `V__dev_user_seed` t_user row in seed-club-1 (where `seedFlightMasterdata` seeds the motor masterdata) — so its tenant resolves deterministically with no JIT race. Confirm from run-26899830691 trace whether the page sub == the provisioned motorAdmin sub. *(seam: motor-test principal → pre-seeded stable user, realm-export + V-migration)* — **last clean-seed blocker; if this still reds, escalate (harness shape).**
- [x] **T-25** — **Gate-revealed (operator: one more harness fix):** the motor `:261` clean-seed test inherits the **sysadmin Keycloak SSO session** (sub `f1558768`, left live by `captureSysadminBearer` in `provisionTwoClubs`) in its Playwright context, so its tenant-scoped `GET /api/v1/flights` fires tenant-less → the 2xx guard times out. Confirmed: clubadmin4 (`c1ab4d40…004`) resolves its tenant fine (T-24 correct), but f1558768 is ALSO active in the window. Fix the session isolation: clear the Keycloak SSO cookies / force a fresh OIDC login for the motor context before logging in as clubadmin4 (or stop `captureSysadminBearer` leaving a live browser SSO session that later contexts inherit). Product is correct — this is harness-only. *(seam: real-idp session isolation — newRecordedContext / loginAsSeededMotorClubadmin / captureSysadminBearer)* — **last blocker.**
- [x] **T-26** — **Gate-revealed (operator: fix the harness, no shortcuts):** T-25 `clearCookies` did NOT fix motor `:261` — mixed signal (clubadmin4 resolves 34×, sysadmin f1558768 misses 53× during the run). ROOT-FIX the real-idp session handling so the motor test deterministically runs as clubadmin4 and its `GET /api/v1/flights` returns 2xx. First pull the trace to confirm the EXACT auth on the failing request (is the motor page on sysadmin, or on clubadmin4 but the GET never fires/2xx?). Then fix at source — e.g. `captureSysadminBearer` mints the bearer via the KC token endpoint (no browser SSO session) OR runs in a disposable context + explicit KC end-session logout, so no later context inherits the sysadmin session; adjust data seeds / principal strategy / storageState as needed. Operator: adjust whatever is needed; do not stop until all 14 flight cases are green. *(seam: real-idp session/principal harness — captureSysadminBearer + context isolation)* — **last blocker, no shortcuts.**
- [x] **T-27** — **ROOT CAUSE FOUND (manager trace analysis, run 26903461521):** the motor `:261` red is the `waitForResponse(GET /api/v1/flights 2xx)` GUARD itself (a T-22 debug aid), NOT the product. Trace proves: `GET /api/v1/flights?limit=50` fires EXACTLY ONCE (during login/landing, before the guard registers at spec:300), the page RENDERS "Air movements" (h1 present), and the in-app /airmovements nav reuses the WARM store (motor = client-side filter per T-05, no re-fetch) so no second GET ever matches → 15s timeout though the page works. **Fix:** remove the brittle waitForResponse guard; rely on the real behavioral assertions (toHaveURL /airmovements, h1 "Air movements", flights-table visible, then create the motor flight + assert the list row shows type "Motor"). Those already fail-fast on a real tenant regression (no table / create fails). Keep ALL create+list+type assertions — do not weaken the AC. *(seam: motor test guard removal + real assertions)* — **last blocker.**
- [x] **T-28** — **Gate-revealed (T-27 unblocked the real AC; genuine motor-create gap):** with the guard gone, motor `:261` reaches the real assertions but 60s-times-out at `:359` (`flights-aircraft-type-${motorId}` toContainText "Motor"). Trace proves the motor create **POST /api/v1/flights NEVER fires** (only the one login GET) — the `createMotorFlight` flow stalls before submit (page DID reach /airmovements + "Air movements"); most likely a MOTOR-variant wizard field/af-select option not fillable (the glider :190 class, for motor: flight-type must be motor-applicable, aircraft=motor, pilot pickable, start/landing/location), so submit stays disabled/no-op. Open the trace.zip action timeline (playwright show-trace / the trace JSON) to find the EXACT pending action; fix the createMotorFlight flow (and the motor wizard if a field does not render for the motor variant) so the POST fires and the new motor flight lists with type "Motor". *(seam: createMotorFlight flow + motor wizard required fields)* — **last blocker, no shortcuts.**
- [x] **T-29** — **ROOT CAUSE (manager analysis, run 26905560096):** clubadmin4 resolves its tenant fine server-side (`user-lookup hit … column=club_id`). The motor `:261` red is that it is the ONLY test using IN-APP nav (`af-nav-section-/airmovements` click + `toPass`, introduced by T-26) — in-app nav does NOT re-run the SPA auth-init, so `SessionStore.currentClubId` is not yet populated and `tenantRequiredGuard` bounces /airmovements→/start for the full 30s. ALL 13 green tests (incl. the migrated clubadmin3 test) use `page.goto(...)`, which re-runs auth-init (checkAuth→loadMe→currentClubId) BEFORE the guarded route resolves. **Fix:** revert the motor test to `page.goto(/airmovements)` + `page.goto(/airmovements/new)` (match the proven green pattern); drop the T-26/T-28 in-app-nav + toPass complexity; keep T-28 wait-for-/me in login (harmless). T-26 goto-stall theory was the (since-fixed) SSO-bleed/tenant-not-resolved, not goto itself. *(seam: motor test navigation → goto)* — **last blocker.**
- [x] **T-30** — **Gate-revealed (T-29 goto landed; genuine create-correctness):** motor `:261` now reaches `:351` `expect(row …motorImmat).toBeVisible()` — the create POST FIRES and navigates to /airmovements (`:339-340` pass), but the new motor flight row never appears in the MOTOR-filtered list. Diagnose: GET the created flight's detail (or inspect the list response) — is it `flightAircraftType=MOTOR` or GLIDER? Likely the motor-variant create via `goto(/airmovements/new)` does NOT stamp MOTOR (the wizard reuses the "Glider" single-aircraft step + the form model defaults primaryAircraftType=GLIDER), so the /airmovements client-side MOTOR filter excludes the just-created flight → row absent. Fix the REAL cause: ensure a flight created at /airmovements/new is stamped `flightAircraftType=MOTOR` (product — flight-variant must carry through goto/route-data create), OR if it IS motor but the warm list just is not refreshed after create, fix the refresh. Also cap the spec's `waitForResponse` waits at `{timeout:5_000}`. The new 20s/5s timeouts make this fail-fast + precise. *(seam: motor-variant create stamping + the spec waitForResponse caps)* — **last blocker, no shortcuts.**
- [x] **T-31** — **Gate-revealed (T-30 narrowed it):** the motor `:262` create POSTs a flight that IS `flightAircraftType=MOTOR` (T-30's API assertion passes), reaches /airmovements, but fails at `:373` `flights-aircraft-type-${motorId}` toContainText "Motor". So the discriminator is correct — the remaining issue is the LIST after create: either the warm /airmovements store is not refreshed post-create (the new row/type not shown — match the green glider /flights create→list refresh at `:210`), or the list type-cell renders the MOTOR label differently than "Motor". Pull run-26907518852 trace (now with 45s budget + 5s assertions the failure reports precisely): inspect the /airmovements list GET response after create + the rendered `flights-aircraft-type` cell. Fix the real cause (post-create list refresh on the motor variant, or the type-cell label). Also confirms the per-test 45s recalibration restored the J-1 aircraft S-163 test (20s collateral). *(seam: motor create→list refresh / type-cell)* — **last blocker.**
- [x] **T-32** — **ROOT CAUSE (manager trace, run 26908607097):** after `goto(/airmovements)` the page lands on the PUBLIC LANDING ("Sign in" / `landing-topbar-sign-in` present, ROUTER-OUTLET empty of the list) — auth-init did NOT restore the seeded principal's session on the reboot, so `flights-table` never mounts (45s timeout). The GREEN migrated test uses the SAME seeded-principal pattern (`loginAsMigratedAdmin` clubadmin3 + `page.goto(/flights)`) and passes — the motor login (`loginAsSeededMotorClubadmin` clubadmin4) DIVERGED via T-25 `clearCookies()` (prime suspect: clears the OIDC session cookies so the goto reboot can't restore auth → public landing) + T-28 wait-for-/me. **Fix:** make `loginAsSeededMotorClubadmin` mirror the proven-green `loginAsMigratedAdmin` EXACTLY (drop the divergent `clearCookies`/extra waits unless proven necessary), and have the motor test reach /airmovements via the proven path — `goto` to a known-good authed route first if needed, then the screen — i.e. match what makes clubadmin3 green. Verify clubadmin4 auth restores after goto. Strip the accumulated T-25/T-28/T-31 cruft that diverged it. *(seam: loginAsSeededMotorClubadmin ≈ loginAsMigratedAdmin + motor nav)* — **last blocker.**
- [x] **T-33** — **Gate-revealed (T-32 removed clearCookies, still red — isolate principal vs route):** trace 26909589684 shows clubadmin4 + `goto(/airmovements)` STILL lands on the public sign-in landing (`landing-topbar-sign-in` ×10), so the session is not restored on the reboot — but the GREEN migrated test (clubadmin3 + `goto(/flights)`) survives the identical reboot. Run the DECISIVE ISOLATION: in the motor test, temporarily `goto(/flights)` as clubadmin4 — (a) if it authenticates + renders the flights list → the **/airmovements route** is the culprit (examine its registration/guard/lazy-chunk under the real-idp `development` build vs /flights; fix it), (b) if it ALSO lands on sign-in → the **clubadmin4 principal** is the culprit (compare its realm-export user + V29 t_user seed against the green clubadmin3; fix the seed/realm divergence). Then make the motor test reach /airmovements green via the proven path. Pull the fresh trace; state which it was. *(seam: clubadmin4-vs-/airmovements isolation + the real fix)* — **last blocker.**
- [x] **T-34** — **ISOLATION RESULT (run 26910615810): it is the /airmovements ROUTE on hard `goto`, NOT the principal.** clubadmin4 authenticates fine — backend `user-lookup hit sub=c1ab4d40…004 column=club_id` ×many, trace shows kc-form-login + "Flights" h1 + flights-table RENDERED at /flights. The test still 45s-timed-out because the motor create flow still hard-`goto`s `/airmovements/new`, which reboots the SPA and hits the route issue that /flights does not. **Fix:** (1) reach /airmovements via the PROVEN-warm path — `goto(/flights)` [auth-init runs, renders] → wait flights-table → IN-APP nav-link click to /airmovements (no reboot, warm session) → create via in-app new-button click to /airmovements/new (no reboot). Restore the full motor assertions (h1, table, create POST, row, type "Motor"). (2) ALSO investigate WHY `goto(/airmovements)` fails where `goto(/flights)` works on a cold load (lazy-chunk 404? console error? guard/route-data diff at runtime under the `development` build) — if a real product bug (a user bookmarking /airmovements would hit it), fix it; if genuinely test-env timing, document it. Operator: no shortcuts. Pull the fresh trace. *(seam: motor test warm-nav to /airmovements + the /airmovements-cold-load investigation)* — **last blocker.**
- [x] **T-35** — **ROOT CAUSE (real product bug, confirmed in `session.guard.ts`):** `authGuard` returns `false` while `session.isLoadingSession()` — which CANCELS the navigation (the comment says "defer" but Angular `CanActivateFn`→false = cancel, not wait); `tenantRequiredGuard` also bounces to `/start` when `currentClubId()` is transiently null. So ANY navigation hitting the brief session-loading/token-renew window is silently cancelled/bounced (the motor /airmovements nav loses the race that /flights wins). This is a genuine UX bug (a user navigating during a renew gets a dead nav), not a test artifact — the no-shortcuts fix. **Fix:** make `authGuard` (and `tenantRequiredGuard`) return an `Observable<boolean|UrlTree>` that WAITS for `isLoadingSession` to settle (e.g. `toObservable(isLoadingSession)`/the OIDC checkAuth completion → `filter(!loading)` → `take(1)`), THEN evaluates auth + tenant — instead of returning false/bouncing during the transient window. Settled-session behavior unchanged. Verify ALL 13 green real-idp tests + the mock chromium suite stay green (the change only helps the loading-window race). Then the motor test's nav to /airmovements succeeds. Keep ADR 0007 hard-401 redirect intact for the genuinely-unauthenticated case. *(seam: authGuard + tenantRequiredGuard wait-for-settle)* — **the real fix.**
- [x] **T-36** — **DESIGN CORRECTION (operator, the real fix):** AlpenFlight does NOT have a separate /airmovements UI — motor flights live in the SAME unified /flights list as glider flights (only LEGACY separated them into a /airmovements screen). The 15 motor failures were all fighting a design mismatch: S-064/T-05 wrongly carried the legacy split forward as a separate /airmovements route. Correct it: (1) **rewrite the motor test (`:262`)** to create a motor flight via the SAME /flights wizard (select a motor aircraft, no tow step) and assert it appears in the unified /flights list with aircraft-type "Motor" — NO /airmovements navigation (mirror the GREEN glider test exactly, motor aircraft instead). (2) **Remove the `/airmovements` route + nav entry** (T-05's `airmovements.routes.ts` + app.routes registration + the `flightVariant:'motor'` nav section) since the unified design has no separate motor screen; clean up `/airmovements` references in specs (e.g. root `airmovements-crud.spec.ts` → fold into /flights or remove). (3) **Update the J-2 AC + S-064 framing**: "motor flight create/edit/list in the unified /flights list (legacy's separate /airmovements screen is NOT carried forward)". Keep all real assertions (create POST MOTOR-typed, lists in /flights, type "Motor"). *(seam: motor test → unified /flights + remove /airmovements route + AC)* — **the actual fix; supersedes T-05/T-22..T-35 motor-nav churn.**
- [x] **T-37** — **Gate-revealed (T-36 side-effect; motor case now GREEN):** the 412 test (`:489`) fails at `:526` — the out-of-band version-bump PUT `{ ...detail, comment }` returns **400** (was 200/passing at T-35). The OOB write spreads the full `FlightDetail` (GET shape) into a PUT (`FlightUpdateRequest` shape) — inherently fragile (DTOs differ by design); T-36 changed the glider create `snapshot()`/`flight-form.model.ts` so the created flight's detail likely no longer PUT-backs cleanly. Pull run-26922775968 trace for the PUT 400 response body (which field is rejected). Fix robustly: build a proper minimal `FlightUpdateRequest` for the OOB write (only the fields PUT accepts, comment changed) instead of spreading the whole detail — OR fix whatever T-36 actually changed in the created-flight shape. Keep the 412 assertions intact (stale If-Match → 412 → conflict dialog, exactly one PUT, no auto-retry). *(seam: 412 test OOB-write payload + any T-36 create-shape regression)* — **last blocker.**
- [x] **T-38** — **Gate-revealed (fanout, T-10 first live run):** the J-2 legacy flight parity VIDEO spec (`e2e/tests/flights/flights-parity-J2.spec.ts:96`) fails on guessed legacy-flsweb selectors (authored blind: `fls-flight-edit-tow-form` stays HIDDEN; list-row `td.immatriculation[ng-bind="flight.Immatriculation"]` never resolves at 30s). Worse, its failure FAIL-FASTS the fanout job → the REAL migration-proof steps (`Run alpenflight-export against legacy MSSQL` [the producer-SELECT validation — the migration done-bar], `Run AlpenFlight parity specs` [migrated render], gallery deploy) were ALL SKIPPED. **Fix BOTH:** (1) make the J-2 legacy parity-video step **non-blocking** (`continue-on-error: true`, mirror J-1 T-14: "AlpenFlight green is the gate; legacy video is a parity aid, surfaced in final-status not hard-fail") so the export + render run regardless; (2) fix the T-10 legacy spec selectors against the REAL legacy flsweb DOM (tow form only shows for tow-start-type; correct the list-row immat selector — pull the run trace/error-context for the actual DOM, or mirror the green J-1 `aircrafts-parity-J1.spec.ts` selector style). Re-dispatch the fanout. The migration done-bar needs the **real export (producer SELECT vs MSSQL) to actually RUN + pass** (expect a possible J-1 T-16-class column catch) + the AlpenFlight migrated render green; the legacy video is the parity aid. *(seam: fanout legacy-step gating + flights-parity-J2 selectors)* — e2e-driver. **last gate piece.**
- [x] **T-39** — **Real-export catch (fanout, J-1 T-16 class — the migration done-bar working):** export+producer-SELECT PASSED, but the real-bundle ingest 400s `BUNDLE_CROSS_TENANT_FK_LEAK`: a migrated FLIGHT carries `start_type_id` legacy guid `00000000-…-000000000003` (= legacy `AircraftStartType.SelfStart=3`) but `legacy_id_map_START_TYPE` has no resolution — the SYSTEM_GLOBAL START_TYPE reference closure does not enumerate value 3. Real FLSTest has a SelfStart flight; the synth bundle (T-08) only used start types 1/2/5, so only the real export hit it. Legacy start types are 1=TowingByAircraft, 2=WinchLaunch, **3=SelfStart**, 4=ExternalStart, 5=MotorFlightStart — the closure must enumerate ALL 5. **Fix:** ensure the START_TYPE SYSTEM_GLOBAL bundle entry / `StartTypeMapper` producer + the reference seed (`V2` t_start_type + the legacy_id_map_START_TYPE enumeration) resolves all 5 legacy values incl. SelfStart(3); add a guard/test that the START_TYPE closure covers the full legacy enum (so a future missing value fails loudly, not at ingest). Re-dispatch the fanout. *(seam: StartTypeMapper SYSTEM_GLOBAL enumeration + start-type reference seed + completeness test)* — **last migration-fidelity blocker.** ([[project_synth_bundle_doesnt_validate_producer_select]]) — **Done.** Root cause was NOT a missing V2 seed (V2 `t_start_type` already seeds all 5 codes incl. `SELF_START`, and `StartTypeMapper.LEGACY_ID_TO_V2_CODE` already mapped 1..5): the **real export path `BundleWriter` emitted NO `legacy_id_map/START_TYPE.pgcopy` at all** — `hasSeedResolver()`/`resolveSeedPk()` only wired COUNTRY/CLUB_STATE/LANGUAGE, so `legacy_id_map_START_TYPE` was EMPTY at ingest and the first start_type FK the real flight hit (SelfStart 3) fail-closed (synth ITs hand-seeded their own AEROTOW(2)-only map, so they never exposed it — the `project_synth_bundle_doesnt_validate_producer_select` class). Compounding gap: the producer SELECT `SELECT StartTypeId FROM StartTypes` reads only the legacy *table* rows, but `AircraftStartType` is a compile-time enum so a FLIGHT can reference any value 1..5 regardless of which `StartTypes` rows exist. **Fix (one seam, enum-driven closure):** added `StartTypeMapper.legacyEnumIdToSeedPk()` — the enum-complete `UUID(0, legacyId)→t_start_type seed PK` map for ALL 5 values, sourced from `SeedReferenceUuids.startTypeByCode()` (new START_TYPE entry, offset 4_000, in lockstep with the V2 seed via the extended `SeedReferenceUuidsSeedParityTest`). `BundleWriter` now emits `legacy_id_map/START_TYPE.pgcopy` from that closure (not the table-row-limited NDJSON stream), so every enum value resolves at ingest. **No new `t_start_type` row needed — SELF_START already existed in V2; it was a missing producer id-map, not a missing seed.** Completeness guards (TDD, RED-proven): `MapperLegacyBindingsTest.startTypeClosureEnumeratesTheFullLegacyAircraftStartTypeEnum` (closure covers 1..5 → 5 distinct seed PKs, pins SelfStart(3); fails at build if a value is dropped) + two real-Postgres ITs in `ForeignKeyResolverFanOutIT` (`start_type_self_start_resolves_against_the_enum_complete_closure` proves all 5 incl. 3 resolve through the real `ForeignKeyResolver`; `start_type_missing_from_the_closure_fails_closed` pins the FK-leak guard intact — fix the DATA, never weaken the guard). All green: migration-bundle + migration-tool suites, server `SeedReferenceUuidsSeedParityTest` (4) + `ForeignKeyResolverFanOutIT` (5) FIRST-LIVE on real Postgres. Re-dispatched fanout is the real-export first-live-green. **Flagged for `/do-retro` (NOT touched — out of seam):** `StartTypeMapper.LEGACY_ID_TO_V2_CODE` maps legacy `1→WINCH_LAUNCH, 2→AEROTOW`, but the legacy enum is `1=TowingByAircraft (aerotow), 2=WinchLaunch` — 1↔2 appear semantically SWAPPED. Synth seeders + ITs treat legacy 2 as AEROTOW consistently (round-trips), but migrated 1/2 start-type semantics may be inverted vs legacy. Needs a `legacy-oracle` confirm + coordinated fix across mapper + synth seeders; left for a follow-up to keep T-39 one seam.
- [x] **T-40** — **Real parity bug (T-39 worker flagged, manager confirmed):** `StartTypeMapper.LEGACY_ID_TO_V2_CODE` is INVERTED for 1↔2 — legacy `AircraftStartType` is authoritatively `1=TowingByAircraft (aerotow)`, `2=WinchLaunch` (`flsserver/.../AircraftStartType.cs`), but the mapper maps `1→WINCH_LAUNCH`, `2→AEROTOW`. Every migrated aerotow flight is silently mis-typed as winch-launched (and vice-versa). It resolves (both codes seeded → no FK leak → does NOT block the fanout), so only a semantic audit catches it. **Fix:** confirm the correct legacy→V2-code mapping (1=TowingByAircraft→AEROTOW, 2=WinchLaunch→WINCH_LAUNCH) against legacy usage (`needsTowplane`=StartType==1, the glider↔tow link keys on towing-start) + the new-stack start-type semantics (ensure the new code keys aerotow consistently — `FlightTransitionMatrix`/tow-link/`needsTowplane`); fix `LEGACY_ID_TO_V2_CODE` AND any synth seeders/ITs (T-08 `FlightParityBundleSeeder`, the round-trip ITs) that encoded the swap (they emit legacy 2 expecting AEROTOW — flip to legacy 1), so migrated start types are semantically correct and round-trip green. Add/adjust a test pinning legacy 1→AEROTOW. Coherent across mapper + seeders. *(seam: StartTypeMapper code mapping + synth seeders/ITs that encoded it)* — no shortcuts.
- [x] **T-41** — **Real-export catch #2 (fanout, tow self-FK ordering):** export GREEN, START_TYPE GREEN (T-39), but the real-bundle ingest 500s `sqlstate=23503` on `fk_flight_tow_flight_id` — a migrated glider INSERT carries `tow_flight_id=a64011f1…` but that tow `t_flight` row is not present at insert time. T-07 assumed the tow self-FK "resolves for free (id preserved), no two-pass needed" — true for the synth IT's favorably-ordered single pair, FALSE for the real bundle where the glider batches BEFORE its tow. The `FlightMapper` Javadoc itself documents the S-141 two-pass strategy T-07 skipped. **Fix:** implement the tow self-FK as a TWO-PASS (insert all FLIGHT rows with `tow_flight_id` NULL, then a second-pass UPDATE sets `tow_flight_id` after all flights exist) — OR a deferrable FK / topological intra-batch ordering; pick the approach that fits `EntityStreamIngestor`. Verify the migrated glider ends with its `tow_flight_id` → the migrated tow row (the round-trip IT must assert the LINK resolves, not just that ids match by luck — strengthen it to a batch where the glider precedes the tow). Then the J-0c real-bundle ingest (which now carries FLIGHT) succeeds. Also re-check the fanout 412-test 404 (`flight-migration-parity.spec.ts:552`) — likely a cascade of this 500 in the shared backend; confirm it clears once ingest is green, else separate. *(seam: tow self-FK two-pass in the FLIGHT ingest + round-trip IT strengthen)* — no shortcuts. ([[project_synth_bundle_doesnt_validate_producer_select]]) — **Done.** Implemented the S-141 **two-pass** (the approach the FlightMapper Javadoc already promised; chosen over a deferrable FK because it is robust to ANY batch order AND fits `EntityStreamIngestor`'s streaming single-INSERT-pass architecture without a schema change). New opt-in `Mapper.deferredSelfFkColumns()` (default empty; only `FlightMapper` overrides → `["tow_flight_id"]`). `EntityStreamIngestor.ingestEntityNdjson` now, for an entity that declares a deferred self-FK, captures `(id, value)` per non-null self-FK while NULLing the column on the row so the INSERT writes NULL, then after the full INSERT pass replays an `UPDATE t_flight SET tow_flight_id = ? WHERE id = ?` batch in the SAME txn. The value is already the preserved/derived target id (FLIGHT non-fan-out FULL_PORT), so the second pass needs no further resolution. Empty-guid/no-tow → null value → no edge, column stays NULL; soft-deleted tow rows stay tombstoned (mapper unchanged). Deferred column names join the `[A-Za-z0-9_]+` identifier allow-list. **Tow was in-bundle-but-mis-ordered, NOT missing** — confirmed: the producer SELECTs all club flights, so the tow row IS exported; the 23503 was purely intra-batch ordering (glider INSERTed before its tow). **IT strengthen (RED-proven):** both `FlightRealProducerRoundTripIT` + `FlightMigrationRoundTripIT` now emit the GLIDER **before** its tow (was tow-first = the lucky order) and assert the LINK resolves to a real migrated TOW-type row (`flight_aircraft_type_id=2`), not just id-equality. Disabling the two-pass reproduces the exact prod failure — `FlightRealProducerRoundTripIT` 500s `sqlstate=23503` on `fk_flight_tow_flight_id`. All green on real Postgres: both flight ITs + Aircraft/Location real-producer + `MigrationBundleIngestIT` (no regression to non-self-FK entities, which hit the null-fast-path) + `MapperVsSchemaCompatibilityTest`/`MigrationIngestNoDiskSinkTest`/`ApplicationModulesTest` + the full migration-bundle suite. **412-test 404 (`flight-migration-parity.spec.ts:552`):** did NOT touch it — it is a CASCADE of this ingest 500 corrupting shared-backend state in that one playwright invocation (passed clean in the PR-gate ci run); this fix should clear it. The re-dispatched fanout is the first-live-green confirmation.
- [x] **T-42** — **Operator-caught (demonstrability gap):** the legacy↔AlpenFlight **FORM** parity screenshot pairing is incomplete — `legacy-flight-form.png` was SKIPPED in the fanout staging ("not found"), so the gallery form pairing has only the AlpenFlight half. Root cause: `flights-parity-J2.spec.ts` writes `legacy-flight-form.png` at `:197` but ONLY AFTER the brittle tow-sub-form assertions (`:181-188`, `fls-flight-edit-tow-form div[ng-if=needsTowplane]` + `[name=TowAircraftId]` visible) — those abort the spec before the form PNG is written, and T-38's `continue-on-error` (correct — legacy video is non-blocking) let the gate go green while silently skipping the capture. The gallery link-check only fails on a *declared-but-missing* PNG, not a *skipped* one (guard gap). **Fix:** capture `legacy-flight-form.png` ROBUSTLY — move it to right after the edit form opens (~`:159`, the full form), INDEPENDENT of the tow-sub-form drill-down (keep the tow assertion as a soft/secondary check, or fix its selector, but the form SCREENSHOT must not depend on it). Pull the run-26926684710 trace to confirm the abort point. Also: **J-1 legacy aircraft list+form PNGs are similarly skipped** (`aircrafts-parity-J1.spec.ts`) — fix the same pattern there so the J-1 gallery is complete too. Re-dispatch the fanout; confirm all 4 J-2 (+ J-1) paired PNGs render. Consider a gallery guard that fails if an EXPECTED screenshot is skipped (not just declared-missing). *(seam: flights-parity-J2 + aircrafts-parity-J1 form-capture ordering + optional gallery skip-guard)* — e2e-driver.
- [x] **T-43** — **Operator: parity-complete screenshot set (motor + wizard).** The 2-pair set (list/form) under-tells J-2's story. Capture a richer paired set that shows the two distinctive shapes: **(a) the motor unification** — legacy has a SEPARATE `/airmovements` screen, AlpenFlight unifies into `/flights`; **(b) the glider+tow 3-step wizard** (Launch→Glider→Tow) vs legacy's two-column glider+tow form. Target ≥6 paired (3 legacy surfaces ↔ 3 AlpenFlight): LEGACY — glider+tow flight list (have), glider+tow edit FORM (T-42), motor `/airmovements` (list and/or form); ALPENFLIGHT — unified `/flights` list showing glider+tow+motor rows (have), the **glider+tow wizard FULL example** (a populated capture showing the TOW step — not an empty/first-step form; ideally 2 shots: glider step + tow step so the paired-create UX is visible), motor create in the unified wizard. Declare each in `screenshots.json` (journey J-2, side legacy|alpenflight, a `view` that pairs them: list / form / motor / wizard-tow), and extend the gallery generator if needed so >2 views per journey render as paired rows. Keep specs read-only. *(seam: flights-parity-J2 + flight-migration-parity capture points + screenshots.json views + gallery generator multi-view)* — e2e-driver. **builds on T-42; one fanout validates both.**
- [x] **T-44** — **Operator: accordion in the proof gallery (overview as it grows).** The gallery (`alpenflight/web/e2e/proof-gallery/generate-gallery.mjs`) now spans J-0/J-0c/J-1/J-2 + will keep growing; collapse each JOURNEY section into an accordion for overview. Use native `<details>/<summary>` (no JS, accessible, keyboard-reachable) — each journey a collapsible `<details>` with the journey id + AC/caption summary in `<summary>`, videos + paired screenshots inside. Aesthetics per ADR 0024: flat (1px slate border, sharp radius 0, no shadow), sentence case, restrained. Sensible default-open policy (e.g. newest journey open, older collapsed — or all open with a collapse-all). Keep the existing video + paired-screenshot rendering + the AC5 link-check inside each section. Add/extend the `generate-gallery.spec.ts` fixture to assert the `<details>` accordion structure (one per journey, summary present). *(seam: generate-gallery.mjs accordion render + spec)* — e2e-driver. **rides this fanout (validate with T-42+T-43).**
- [x] **T-45** — **Gate-revealed (T-43 capture-timing regression):** T-43 added wizard/motor screenshot captures inside `createGliderFlightAerotow`/`createMotorFlight` and regressed the previously-green flight spec. `:254` `createdResp.json()` throws "Protocol error (Network.getResponseBody): No data found for resource" — a screenshot/navigation was inserted BETWEEN the POST `waitForResponse` and reading its body, so Playwright evicted the body. And the 412 test `:639` `before.status()` is 404 (the create's flightId broke) — same class, glider-create capture disrupted the flow. **Fix:** in BOTH create helpers, read `createdResp.json()` (capture the body/id) IMMEDIATELY after `await createdResp`, BEFORE any screenshot or navigation; do the wizard/motor screenshot captures at safe points (before submit for the wizard-step shots, or after the list renders) that don't sit between a response and its body-read and don't change the returned flightId. Re-verify the 6+ screenshots still capture (T-43 set) AND the create/list/412 assertions pass. *(seam: createGliderFlightAerotow + createMotorFlight capture ordering vs response-body read)* — e2e-driver. **builds on T-42/T-43/T-44; one fanout validates all.**
- [x] **T-46** — **Gate-revealed (T-45 incomplete — the body read is fundamentally wrong):** `createdResp.json()` STILL throws "Network.getResponseBody: No data found for resource" at `:182` (glider) / `:270` (motor) even read right after `await created`. Root: the SPA navigates to `/flights` on POST-success, and that navigation EVICTS the POST response body — so `.json()` on the captured `waitForResponse` can never be reliable (T-45 moving it earlier didn't help; it flaky-passed in the fanout by retry, failed both ci attempts). T-45's switch to `body.id` was the regression. **Fix (definitive):** do NOT read `createdResp.json()`/body. Get the flight id from the **201 `Location` header** (`createdResp.headers()['location']` → parse `/api/v1/flights/{id}`; headers survive navigation) — or, if no Location, re-derive from the rendered `/flights` row (the pre-T-43 approach) / a `ctx.request.get` list query. Get `flightAircraftType` (the MOTOR assertion) via a separate `ctx.request.get(/flights/{id})` (its own buffered response, not page-navigation-evicted). Keep `await created` for the navigation-complete wait + the 412 no-auto-retry PUT count. Apply to BOTH `createGliderFlightAerotow` + `createMotorFlight`; keep the 6+ screenshots (T-43) + skip-guard (T-42). *(seam: both create helpers — id via Location header / re-GET, never the navigated-away POST body)* — e2e-driver. **definitive fix for the body-eviction flake.**
- [ ] **T-12** — _(DEFERRED to a boyscout rider — did not bloat the J-2 gate PR; see _BOYSCOUT.md)_ **Boyscout: modernize-\* sunset** — delete the 9 `modernize-*` skills + ~12 modernize agents + prune the `rolled_up_into:` horizontal stories (keep 47 `implemented/`). Mechanical; fold only if it doesn't bloat the gate PR, else defer (note for the next feature journey). *(seam: .claude/skills/modernize-*, .claude/agents/*, rolled_up_into stories)* — clears the `_BOYSCOUT.md` bullet.

## Assumptions made

1. ~~`/airmovements` is the **same** SPA screen parameterized by the MOTOR filter~~ **SUPERSEDED
   (operator, T-36):** AlpenFlight has **no** separate `/airmovements` screen — motor flights are
   unified into the SAME `/flights` list (a flight with a motor aircraft + no tow). S-064 is
   reinterpreted: "no legacy-style duplication" = **unified into /flights, not a second route**.
   Only the legacy flsweb app separated them. The MOTOR discriminator is inferred from the selected
   aircraft (`motor-aircraft.ts`); the T-05 `/airmovements` route + variant plumbing was removed.
2. The Flight + FlightCrew migration mappers exist from J-0b authoring but are **unproven
   end-to-end** ([[verify_infra_is_run_not_just_authored]]) — sized build/verify, expecting a
   real-export producer-SELECT catch like J-1 T-16.
3. ~~S-061's `flight_date`/`locked_at` gate wording is provisional~~ **RESOLVED (operator 2026-06-03):**
   gate on `flight_date`/`locked_at` (S-061 wording) — a deliberate divergence from legacy `CreatedOn`;
   adds a net-new `locked_at` column + an ADR amendment. See Parity decisions.
4. The 412/optimistic-concurrency path is a **new-stack affordance** (legacy is last-write-wins),
   so its spec assertions prove new behavior, not legacy parity.
