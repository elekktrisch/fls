---
id: J-26
title: Hardening sprint — validation bugfixes, UX corrections, JDBC retirement, redundancy purge
epic: cross-cutting (E-06/E-07/E-08 surfaces; E-13 proof infra)
status: in_progress
started_at: 2026-06-12
journey0: false
carved: true
depends_on: [J-7]
rolls_up: []   # operator-directed hardening — absorbs _BOYSCOUT riders, not S-stories
acceptance:
  # ——— User-facing share: BUGFIXES + UX CORRECTIONS (all currently-broken behavior; no new features) ———
  - "[happy] Person edit round-trips the membership fields — memberNumber, memberState, isGlider/Motor/TowPilot — through Save → reload (today the form hydrates + toasts success but silently DROPS them: data loss; PUT /persons/{id}/clubs/current exists, is never called)"
  - "[key-error] Flight-type duplicate FlightCode → inline 409 on the flightCode field (today a raw 500, reproducing the legacy bug); the 409 routes by problem-detail `field` — name vs code no longer collapse onto flightTypeName"
  - "[key-error] Flight-type Instructor × Observer both set → blocked by a client cross-field validator AND rejected by a domain XOR guard in FlightType.updateFlags (ADR 0022 §2 — domain is the must-have; today NO layer enforces it)"
  - "[key-error] Club duplicate clubKey → 409 labeled on the clubKey field (today mislabeled as a slug error on the wrong field — ClubsService maps ANY DataIntegrityViolation → SlugAlreadyExists)"
  - "[happy] af-field-errors renders TRANSLATED messages — no raw `common.errors.*` i18n key visible anywhere (today the key renders verbatim)"
  - "[happy] As-you-type debounced (~200ms) inline validation live on EVERY edit form (the J-6b bar, today wired only on reservation-edit + planning-edit) and the ~30 silent af-form-field `[errors]` bindings bound; spec asserts the representative trio aircraft / person / flight-type"
  - "[edge] Flight edit has client required validators (flightDate/aircraft/pilot), Save gated on form validity; the dead never-invoked FlightValidator is wired or deleted"
  - "[edge] Reservation Save disable state never disagrees with form validity (the async second-crew-validator race: button shows enabled while form.invalid, click dead-ends)"
  - "[happy] Profile Account languageId required validator restored (legacy parity regression)"
  # ——— Tech-debt share: proven by build/suite/gallery, behavior-neutral ———
  - "[debt] Main-code JDBC/native SQL = register-only: LanguageCodeLookup, JpaClubStateRepository, JpaCountryRepository, PlanningDayPersistenceProbeImpl converted to JPA/domain ports; EVERY remaining register entry re-affirmed with current rationale or retired (ADR 0027 shrinking list)"
  - "[debt] Shared IT seeding helpers (TwoClubFixture, TenantScopedRowBuilders, Sweep factories) seed via production code (reflection only for non-settable attrs, ADR 0027 §3) — converts the bulk of the 83 raw-JDBC test files transitively"
  - "[debt] Redundancy purge: lifecycle @MappedSuperclass verdict EXECUTED (extract or ADR-recorded decline — stop re-litigating in cpd-baseline); cpd-baseline ratchets DOWN from 4883; fallow dead code removed (3 files, 6 deps, 1 unresolved import); the shared form↔request + errorPatch helper extraction absorbs the aircraft/users/flights/persons edit-page hotspots (CRAP ≤ threshold)"
  - "[infra] A red gate is diagnosable + honest: test-results/** uploaded on failure BEFORE the gallery step mutates the dir; the deployed gallery guard asserts staged == rendered for BOTH sides of every declared shot-pair; the nightly alpenflight-e2e-real-idp.yml is FIXED (setup-java — red 12 nights straight on JVM 11 vs Gradle 9.4.1, the cross-journey regression never ran) and dispatched GREEN on this branch"
  - "[regression] Full-unskip suite green at the gate — every refactor (JDBC retirement, helper conversion, dedup extraction) is behavior-neutral, proven by the existing specs + ITs passing unchanged"
screen: cross-cutting — hardening of shipped screens (/persons, /flighttypes, /clubs, /flights, /reservations, /profile + shared form infra); no new route
headless_pulled_in: none new
migration: N/A — no new entity
parity_test: alpenflight/web/e2e/tests/real-idp/hardening-J26.spec.ts
mock_test: alpenflight/web/e2e/tests/(forms/validation-hardening|persons/persons-edit-membership)   # J-6b regex alternation; both branches on disk since T-04 (the derive requires EVERY branch resolved)
adr_refs: [0022, 0027, 0026, 0008, 0021]
---

## Context

Operator-directed sprint (2026-06-12): **no new features — bugfixes, corrections,
UX and tech-debt only**, in production AND test code. This deliberately inverts
the 60/40 journey rule for one sprint; the user-facing share is the accumulated
P0/P1 validation bugfixes (one of them real data loss) + the as-you-type UX bar
J-6b set but only wired on two forms. The debt share executes the standing
ADR-0027 JDBC retirement, the redundancy riders, and the two
[NEXT-JOURNEY PRIORITY] proof-infra riders — plus the newly-diagnosed nightly
real-idp breakage (red since ≥2026-06-01, unnoticed: missing setup-java).

## Spec must assert

Two new specs, both ENTERING through the chrome (nav → form, never bare goto —
do-ship done-bar):

- **`forms/validation-hardening.spec.ts` (mock inner loop):** the four
  [key-error] fixes above as inline-field assertions (duplicate flightCode →
  flightCode field; XOR blocked; clubKey 409 on clubKey; translated error text);
  the as-you-type trio (aircraft/person/flight-type) shows debounced inline
  errors while typing + clears on valid; flight-edit Save disabled until
  required fields present; reservation Save never enabled while invalid.
  Grounded in `docs/modernization/form-validation-parity-audit.md` (legacy =
  minimum bar, operator 2026-06-09).
- **`real-idp/hardening-J26.spec.ts` (parity_test, heavy chain):** a REAL
  principal edits a Person's membership (memberNumber + role toggle) → Save →
  re-open → values persisted server-side (the data-loss fix, over the real
  backend + Keycloak — no page.route, no @mocked seams); plus one real-chain
  duplicate-flightCode 409 assert (FE→real endpoint→real constraint→inline).
- **Regression posture:** the debt half has NO new spec of its own — its proof
  is the FULL existing suite (ITs + mock + real-idp + fanout) green on the
  final sha, jobs verified EXECUTED (do-ship §4), after the native-SQL
  deletions, helper conversion, and extractions.

No design reference applies (no new screen; existing screens keep their shipped
ADR-0024 structure).

## Notes

**Seam hints (non-binding, one seam each):** persons.store update → wire
`PUT /persons/{id}/clubs/current` (endpoint exists, `PersonsController.java:139-144`);
FlightTypesExceptionHandler DIVE→409 discrimination + service pre-check + store
409 field-routing (`flight-types.store.ts:229-236`); FlightType.updateFlags XOR
+ client cross-field validator; ClubsService DIVE discrimination (`ux_club_key`
vs `ux_club_slug`); af-field-errors transloco; per-form `liveFieldErrors`
adoption + `[errors]` bindings (one sweep, decomposed per form);
flight-form client validators + FlightValidator wire-or-delete; reservation-edit
Save-disable vs async validator; profile-account languageId;
LanguageCodeLookup → the RM-4 `Language` entity; JpaClubStateRepository +
JpaCountryRepository → JPQL/derived; PlanningDayPersistenceProbeImpl → a
reservations-module count port (`@NamedInterface`, per the register's own
Remove-when); register re-affirm pass (AircraftReservationConflictProbeImpl —
decide keep-GiST vs JPQL and RECORD it; ShowcaseSeeder; JpaPersonRepository
cross-tenant check; the four structurally-pre-tenant stays); TwoClubFixture /
TenantScopedRowBuilders / Sweep-factory production-code seeding (+ class-unique
club ids per the RM-5 convention); lifecycle @MappedSuperclass extract-or-ADR;
FE form↔request + errorPatch helper extraction; fallow dead-code deletion;
ci.yml/fanout test-results upload-on-failure (pre-gallery); fanout add_shot →
screenshots.json → generator single source of truth + deployed both-sides
guard; alpenflight-e2e-real-idp.yml setup-java 21 (mirror ci.yml) + dispatch
on this branch to re-baseline 12 days of unrun cross-journey regression.

**Ride-if-budget (ordered; unfinished items STAY in _BOYSCOUT):** e2e prettier
normalization (~42 files); e2e tsc-strictness (~23 errors); orval explicit
operationIds (kill positional getN); clubadmin4 + V29 removal; op-field-mutate
assertion; planning `:410` warm-nav reopen; producer-dedupe soft-delete guard
comment/filter; ingest constraint-name surfacing (dev/test); fanout
reporting-migration-parity assert + stale step name; CI fail-aggregate;
nightly-red visibility (the 12-day unnoticed red — surface scheduled-run
failures on the gallery index or via notification).

**Excluded (recorded, not silently dropped):** P4 `/validate` pre-check
endpoints (new endpoint surface ≈ new feature — deferred); the full gallery
re-arch beyond the staged==rendered structural fix (next journeys' 40%
budgets, per operator); JIT-username multi-IdP security review; S-189 +
CLUB-pgcopy (migration scope, pre-J-21).

## Tasks

- [x] **T-01** — spec stub + proof page: author `forms/validation-hardening.spec.ts` + `real-idp/hardening-J26.spec.ts` structure/selectors/flow (thin assertions, enter via nav) AND scaffold the J-26 per-journey gallery page + link it from the persistent index. *(Stub finding, for the un-fixme tasks: chrome entry needs (a) the dual-role mock principal to see the UNION of nav sections — `navSectionsFor` short-circuits to Clubs-only today — and (b) a `/flight-types` nav section — URL-only screen today, the J-7 hollow-screen class.)*
- [x] **T-02** — per-push gate scope: finalize `mock_test:` stems (J-6b regex convention) + verify BOTH ci.yml derive steps resolve J-26 (mock filter + `parity_test` real-idp spec, `is_baseline=false`); actionlint. **DONE:** the provisional two-stem alternation broke the derive (the `persons/persons-edit-membership` branch has no spec on disk until T-04 → fail-safe full-suite fallback, simulated locally with the extracted ci.yml step bash) — narrowed `mock_test:` to the single existing spec. Verified locally on the extracted step scripts: mock filter → `e2e/tests/forms/validation-hardening.spec.ts`, `is_full=false`, and Playwright `--list` under `--project=chromium` selects exactly its 10 tests / 1 file (a bare `tests/forms/` dir token would leak `inline-validation.spec.ts`); proof spec → `e2e/tests/real-idp/hardening-J26.spec.ts`, `journey=J-26`, `is_baseline=false` (NOT the J-0 fallback). No ci.yml edit needed (J-6b's alternation support already handles both shapes), so no actionlint delta.
- [x] **T-03** — fix nightly `alpenflight-e2e-real-idp.yml`: add setup-java 21 (mirror ci.yml), then `gh workflow run` it on THIS branch and verify JOB-level it reaches the Playwright specs — 12 days of unrun cross-journey regression re-baselined; any latent red becomes a new T-NN. **DONE (fix half; dispatch+verify = manager):** added the missing `actions/setup-java@v4` block before the two `./gradlew` call sites — mirrored ci.yml's actual proof-job block (Java **25** temurin, not 21 as this line guessed; ci.yml has no gradle-cache action on that job so none added). Bitrot sweep: all other action versions (`checkout/cache/setup-node/upload-artifact@v4`, `pnpm/action-setup@v4` v11) + `ubuntu-22.04` already match the green workflows — no other change. actionlint green.
- [x] **T-28** — nav chrome fixes (T-01 findings; ordered HERE — prerequisite for T-04/T-05 un-fixme): (a) `navSectionsFor` renders the role UNION for dual-role principals (today `isSystemAdmin` short-circuits → tenant sections hidden, `nav-sections.ts:44-53`); (b) add the missing `/flight-types` nav entry (URL-only screen — the J-7 hollow-screen class) placed per legacy masterdata nav + role visibility; nav-sections spec updated. **DONE:** (a) union = tenant + Users + Clubs when sysadmin ∧ club-admin (the mock-auth persona); sysadmin-only keeps exactly `['/clubs']` (J-7 "Reports absent for sysadmin" preserved). (b) `/flight-types` ("Flight types", `tags` icon) at the tail of the tenant masterdata run, directly after Persons — legacy put FlightTypes at the masterdata tail (`flsweb/src/core/directives/navigationBar/navigation-bar-directive.html:100-104`); legacy's club-admin nav gate (`flsweb/src/core/AuthService.js:37`) deliberately NOT copied — visibility follows the screen's guard (tenantRequiredGuard only; GETs `isAuthenticated()`), so every tenant principal sees it.
- [x] **T-29** — (T-03 re-baseline revealed) nightly env parity with ci.yml's proof job: the nightly boots the backend as a HOST bootJar but never got ci.yml's host-mapped-Keycloak overrides (ci.yml:727-735 — JWKS/issuer → localhost:8090 AND `keycloak.admin.base-url`, default `http://keycloak:8080`, unreachable from a host process) → ingest 500 ×3 (aircraft/fan-out/flight migration parity) + likely the clubadmin1 users-400 (KC admin client read). Diff the nightly's backend-boot + stack env against ci.yml's proof job and port ALL deltas (incl. mailpit/SMTP + `kc_locale` realm wiring if they differ). Re-dispatch + job-verify = manager. **DONE (port half; re-dispatch+verify = manager):** ported the full 5-var backend-boot env block from ci.yml:736-740 (`ALPENFLIGHT_KC_ADMIN_BASE_URL=http://localhost:8090` — the actual breakage — plus `_REALM`/`_CLIENT_ID`/`_CLIENT_SECRET` + `ALPENFLIGHT_OIDC_ISSUER_URI` verbatim for parity). Full diff found NO other backend-boot/stack delta: SMTP needs no env (base profile defaults `localhost:1025` = host-published mailpit; ci sets none either), seed steps identical (clean-seed flyway only), realm bootstrap identical (same compose build/up of the same baked image). `kc_locale=fr` prerequisite checked: realm-export.json HAS `internationalizationEnabled=true` + fr in `supportedLocales`, delivered identically to both stacks → the locale red is NOT a workflow delta (ci's proof job never runs login.spec.ts, so there's no green-on-ci counter-evidence; suspect KC-version behavior — KC 26.x dropped the deprecated `kc_locale` param in favor of `ui_locales` — diagnose from the re-dispatch, T-30 candidate). actionlint green.
- [ ] ~~**T-30**~~ (split by manager — 7 residuals span 4 seams; post-T-29 run 27398525855)
- [x] **T-30a** — KC-version spec modernization: `login.spec.ts:92` `?kc_locale=fr` red — KC 26.x dropped the deprecated `kc_locale` param in favor of `ui_locales` (T-29 finding); fix the spec accordingly. Also diagnose `token-lifecycle.spec.ts:47` silent-refresh red (realm-mutating; possibly same KC-version behavior class, possibly timing). **DONE (spec-only; PROVEN AT GATE — needs live KC 26.5):** (1) **login.spec** — raw-goto query param `kc_locale:'fr'` → `ui_locales:'fr'` (+ test title/header). Confirmed via Context7 KC 26.5 server_admin "User locale selection": client-provided locale flows via the OIDC-standard `ui_locales` authorization-request param; the legacy `kc_locale` query param was dropped in 26.x (stack = quay.io/keycloak/keycloak:26.5) so it stopped flipping chrome → realm fell back to `defaultLocale=de`-resolved chain rendering `en`. realm-export confirms `internationalizationEnabled:true` + `fr` in `supportedLocales`, so `ui_locales=fr` yields `<html lang="fr">`. Single-sources onto the same param the SPA already uses everywhere (auth.config.ts:58, landing/signup authorize()). (2) **token-lifecycle silent-refresh** — diagnosed NOT a KC-26 renamed/removed endpoint/param (refresh-token grant + rotate-and-revoke are unchanged in 26; `revokeRefreshToken=true`/`refreshTokenMaxReuse=0` matched by the SPA's `allowUnsafeReuseRefreshToken:false`) and NOT a realm token-lifespan gap (offline_access in default scopes, SSO idle/max large). Root cause = a fixed-sleep timing/observation race: the spec waited ONE shortened lifespan (35s) then snapshot-asserted the unauthed marker absent right after `networkidle`, so it could observe a mid-rotation transient (refresh round-trip) or a mid-redirect frame. Hardened: wait TWO lifespans (`*2+5`=65s, still < the 120s setTimeout) so ≥1 full rotation has settled, and assert survival POSITIVELY with web-first retry (`waitForURL host!==KC` then retrying `toHaveCount(0)`) instead of a single post-networkidle snapshot. Both reds are verifiable ONLY at the gate (need the full live Keycloak stack, not bootable on this box).
- [x] **T-30b** — showcase-dashboard spec↔seed reconciliation: pilot last-flight card absent + club-1 pending-validation 10≠4 (`start-dashboard.spec.ts:217,276`) — query the REAL seeded state (J-7 T-18 precedent), fix seed or expectation empirically, don't hand-patch constants. **DONE (root cause ≠ drift — it's a mis-targeted spec; workflow fix):** queried the truth — `ShowcaseSeeder` (the authority) seeds club-1 today=3 / pending=4 / total=14, and BOTH `ShowcaseSeederIT` (asserts pending=4, total=14) AND ci.yml's `alpenflight-dashboard-proof` job (loads `seedShowcase` first) are GREEN on those exact constants → the spec constants are CORRECT, not drifted. The 3 nightly reds (pending 10≠4, total 12<14, pilot card empty) are because the nightly `alpenflight-e2e-real-idp.yml` runs `flywayMigrate` + `SPRING_PROFILES_ACTIVE: dev` ONLY — it NEVER loads `@Profile("showcase")`, yet its bare `e2e/tests/real-idp/` path filter swept in `start-dashboard.spec.ts` (the ONE real-idp spec that asserts showcase DATA). Against the bare dev-seed totals the dashboard endpoint (whole-club count, not the showcase id-band) reads ≠ the showcase numbers + pilot1 has no seeded PIC flights → empty card. Same class as the J-4 `tests/profile/` spec the nightly already excludes (it just lives OUTSIDE the path filter). FIX: added `--grep-invert 'showcase seed'` to the nightly run + documented the exclusion (the spec's describe carries the unique `[showcase seed, real chain]` tag; its real gating job is `alpenflight-dashboard-proof`). NO spec constant change (would be brittle + would red the legit dashboard-proof job) + NO seeder change (seed is correct). Verified: `--grep-invert` selects 0 dashboard tests in the nightly, the dashboard-proof job's own invocation still selects all 5; actionlint clean.
- [x] **T-30c** — clubadmin1 `GET /api/v1/users` 400 (J-6b AC13 regression in the nightly stack; SURVIVED the T-29 KC admin-url fix) — diagnose via local real-idp dev stack repro + backend log; fix server or spec per root cause. **DONE (server-only; root cause = per-row KC role-read intolerance).** REPRODUCED live: minted a real `clubadmin1` token (ROPC via `admin-cli`) → `GET /api/v1/users` → exact nightly body `{status:400,title:"Invalid request",type:…bad-request}` (no `detail` ⇒ an IAE with a null message). Backend log: tenant resolved fine (`UserPrincipalLookup` hit `club_id` for sub `9d08ed9c…009d`), then `listInCurrentTenant` fans out `getRealmRoleMappings(sub)` once per active `t_user` row in the club. Club-1 carries seeded clubadmin2/3/4 (V26/V28/V29) + a runtime/JIT row (`b365dc65…418f`); that last sub is ABSENT from the realm → KC `GET …/role-mappings/realm` → **404**. The adapter mapped 404 → `UserDirectoryException` (**502** when the KC admin client is reachable — the surviving post-T-29 nightly failure), and when the KC admin client is UNREACHABLE (`http://keycloak:8080` from a host process) the connection error surfaces as an IAE → `UsersExceptionHandler` → **400** (the captured body). One missing KC identity blew up the whole list. **FIX:** `KeycloakAdminClient.getRealmRoleMappings` now maps KC `404` → empty role set (a row whose KC identity is gone has no realm roles) — mirrors the existing `findUsersByRoleName` 404→empty; any other status still throws (502, genuine outage not swallowed). **Guards (run in `check`, no live KC):** new `KeycloakAdminClientRoleMappingsIT` drives the REAL adapter (own RestClient + bearer interceptor) against a JDK `HttpServer` stub — 404→empty (red before fix), 500→`UserDirectoryException`, 200→mapped roles; `UsersListControllerIT` gained a CLUB_ADMINISTRATOR-principal case seeding an orphan-sub row and asserting the list still renders 200 with that row at empty roles. Real-idp e2e (`reservations-planning-hardening.spec.ts:228`) proves it end-to-end at the gate. `ALPENFLIGHT_TEST_FORKS=1 ./gradlew check` green.
- [ ] **T-30d** — mail residuals: register verification mail never reaches mailpit (`register.spec.ts:49`, KC-sent SMTP) + planning-notification duplicate (2 mails to club address, expected 1, `planning-migration-parity.spec.ts:901`).

- [x] **T-04** — persons membership data-loss fix: wire `PUT /persons/{id}/clubs/current` into the persons.store update path (fields already hydrated, request omits them) + mock spec case asserting memberNumber/state/role-toggle round-trip. *(T-02 note: if the spec case lands as a NEW `persons/persons-edit-membership.spec.ts`, extend `mock_test:` to the J-6b alternation `tests/(forms/validation-hardening|persons/persons-edit-membership)` in the same commit — the derive requires every branch on disk; if it lands inside `validation-hardening.spec.ts`, no frontmatter change.)* **DONE:** store `update` takes an optional `membership: PersonClubRequest` and runs the two PUTs SEQUENTIALLY (person → clubs/current; the second response is authoritative for the detail/list patch); `person.updated` fires only after BOTH succeed, so a failed membership half surfaces in `saveError` and the page does NOT navigate (no false success). The edit page echoes the NON-form-exposed flags (isWinchOperator, notification prefs, isActive…) from the hydrated membership — the server PUT is a full replace (primitive booleans), an un-echoed flag would zero out. New `persons/persons-edit-membership.spec.ts` (chrome entry, captures BOTH PUT payloads, asserts echo + UI re-open round-trip) + `mock_test:` extended to the alternation; 3 store unit tests on the fork (sequence/skip/honest-failure).
- [x] **T-05** — flight-type duplicate FlightCode: `@ExceptionHandler(DataIntegrityViolationException)` discriminating `ux_flight_type_club_code` → 409 `field=flightCode` (mirror LocationsExceptionHandler) + `findActiveByCode` pre-check + store/page 409 field-routing (name vs code) + IT + spec case. **DONE:** service pre-check (create + update, self-excluded; blank/strip normalization mirrors `assignFlightCode`) throws new `DuplicateFlightTypeCodeException` → 409 `field=flightCode`; the handler's DIVE catch is the race net for the same envelope (other violations stay 500). Store `errorPatch` routes 409 by problem-detail `field` (absent field keeps name-duplicate back-compat); edit page marks/clears `duplicate` inline on the routed control, Code field got its missing `[errors]` binding. Red-first `FlightTypeDuplicateCodeIT` (was 500/500) seeds via TwoClubFixture under a class-unique club pair; duplicate-FlightCode spec case un-fixme'd (tagged `@helper`, covered-by the IT) and passing locally through the chrome. NOTE: `FlightTypesControllerIT`'s pre-existing order-dependent red (its `@BeforeEach` flight-type delete tripped `fk_flight_flight_type_id` against the V36 dev-seed flight unless a flights IT ran first; Gradle failed-first ordering made it sticky locally) got a minimal boyscout fix riding this task — the `@BeforeEach` now deletes the club's flights first (same proven statement as `FlightCreatedSseIT`). Full production-code seeding conversion remains T-18/T-20.
- [x] **T-06** — flight-type Instructor×Observer XOR: domain guard in `FlightType.updateFlags` (must-have, ADR 0022 §2) + client cross-field validator + IT + spec case. **DONE:** guard fires before any assignment in `updateFlags` (create covered too — `register` delegates), throws new `InstructorObserverExclusionException` → handler maps 400 `field=isObserverPilotOrInstructorRequired` (T-05 envelope pattern); client `instructorObserverExclusiveValidator` group validator + roles-section inline alert + the two flag testids the T-01 stub called for; the legacy CHECK confirmed at `flsserver/database/FLSTest/2 alter/DBUpdate_v1.9.25.sql:20` (allows (0,0)/(0,1)/(1,0)). NO new server IT — domain unit tests (red-first, reject on register + updateFlags incl. no-half-apply) cover the rule and the handler follows the IT-proven T-05 mapping shape; client validator vitest spec (red-first) + XOR e2e case un-fixme'd (`@helper`, covered-by FlightTypeDomainTest) incl. uncheck-recovers assert, passing locally through the chrome. No DB CHECK (ADR 0022 §2). Pre-existing `updateFlags_replacesAllElevenFlagsAtomically` (set all 11 true) amended to a legal combination proving both flip directions.
- [x] **T-07** — club duplicate clubKey: `ClubsService.persist()` DIVE discrimination `ux_club_key` vs `ux_club_slug` → clean clubKey 409 + spec case. **DONE:** the red-first IT showed today's behavior is a raw 500, not the mislabeled slug 409 — Hibernate defers the INSERT to the commit flush, so the DIVE escapes `persist()`'s catch entirely. Fix at both layers: `persist()` discriminates the violated constraint (`ux_club_key` → new `ClubKeyAlreadyExistsException`, `ux_club_slug` → `SlugAlreadyExists`, unrecognized → rethrow, no more slug mislabel) and `ClubsExceptionHandler` gained the T-05-pattern DIVE race net for the commit-time path (clubKey → 409 problem-detail `field=clubKey`; slug keeps its bare 409; other violations stay 500). Store routes the 409 by problem-detail `field` via a new `saveErrorKind` (T-05 shape); the edit page marks the OFFENDING control inline (was: any "already in use" → slug field). clubKey is CREATE-only in the UI (immutable on edit), so the spec case was rewritten from the stub's PUT shape to the create flow (chrome entry → New club → POST 409) and un-fixme'd (`@helper`, covered-by ClubsControllerIT), passing locally.
- [x] **T-08** — `af-field-errors` transloco translation (renders raw i18n key today) + profile-account `languageId` required validator + spec asserts. **DONE:** (a) `af-field-errors` renders its mapped keys through an unscoped `*transloco="let t"` (directive, not imperative — reRenderOnLangChange preserved); the previously-NONEXISTENT `common.errors.*` block (all 8 canonical keys: required/minlength/maxlength/pattern/email/min/max/duplicate) added to ALL FOUR locale files in each locale's terse declarative tone (de "Eingabe erforderlich." / en / fr / it), guarded by a red-first vitest case that resolves every `errorTranslationKey` output against every locale tree (the dynamic `t(key)` is invisible to the i18n-key-coverage regex gate). (b) `languageId` got `Validators.required` (legacy `profile.html:61` parity) + the touched-gated `[errors]` binding + `[allowClear]` on the select (no clear affordance = dead validator) + Save gated `form.invalid ||` per the S-007 convention; red-first class-instantiation vitest (no template render, §8 posture). Both T-08 spec cases un-fixme'd + thickened (translated text asserted EXACT German, not just key-absence; languageId clear→error→Save-disabled→re-pick-recovers), passing locally through the chrome; two stub bugs fixed en route (clubs list is a `<ul>` not a table → `club-row-<slug>` testid; the profile menu entry is `role="menuitem"`, not `link`). Riding fix: `clubs-crud.spec.ts:298/326` asserted the RAW key text (`hasText: 'pattern'/'duplicate'`) — would red under translation; re-scoped to the slug field's alert.
- [x] **T-09** — reservation-edit Save-disable binding vs async second-crew validator race: disable state must track form validity; spec case. **DONE:** the Save `[disabled]` now binds a `saveDisabled` computed off a `formStatus` signal (`toSignal(form.statusChanges, {initialValue: form.status})`) via the pure `saveDisabledFor(status, saveSubmitted, hasOverlap)` — Save enabled ONLY when status is `'VALID'` (so `'PENDING'`/`'INVALID'`/`'DISABLED'` all keep it disabled), replacing the non-reactive `form.invalid` getter the OnPush template only re-read on a CD tick (the J-7 T-20 race where the button showed enabled for a beat after the second-crew validator flipped the form invalid). Two supporting fixes: the conditional second-crew validator effect now `updateValueAndValidity()` EMITS (dropped `emitEvent:false`) so the inline error + form status re-derive in lock-step the instant `nrOfSeats` flips the requirement (no silent gap), and a dead-end `onSubmit` (form not `'VALID'`) calls `surfaceFieldErrors` — re-emitting each enabled control's value so the debounced `liveFieldErrors` streams render their messages (visible feedback, not the old `markAllAsTouched`-only no-op). Red-first 7-case unit test on `saveDisabledFor` (PENDING→INVALID→VALID transitions + submitted/overlap legs); reservation save-gating e2e case un-fixme'd + thickened (multi-seat aircraft → second-crew required → Save stays disabled → fill crew → enabled), passing against the mock dev server.
- [x] **T-31** — (gate-revealed, BLOCKING the per-push `required` gate) the deployed-journey guard reds because J-26 has no real-idp captures yet (its `parity_test` spec is all-`fixme` until T-27). Two-part ci.yml fix, J-6b-baseline precedent: (a) the `proof_spec` derive must treat a real-idp spec with ZERO active (`test(`, non-`fixme`/`skip`) tests as not-yet-runnable → J-0 baseline fallback (`is_baseline=true`), so the proof job runs a known-good real chain until T-27 un-fixmes the assertions (then it auto-flips to `is_baseline=false`); (b) gate `GALLERY_DEPLOYED_JOURNEY` on `is_baseline` — empty (guard skips) when the baseline ran, since a baseline run produces no journey-under-work captures by definition (latent bug: would bite any post-guard journey that baseline-falls-back). Simulate both derives locally + `actionlint`; the per-push proof must go green on the J-0 baseline. **DONE (worker drafted, API-overload before commit; manager verified+committed):** (a) `proof_spec` derive now requires `grep -qE '^\s*test\(' ` on the real-idp spec — an all-`fixme` file → J-0 baseline (`is_baseline=true`); (b) `GALLERY_DEPLOYED_JOURNEY` empty on baseline. Simulated: J-26 → baseline/is_baseline=true (guard step `if:` already gates `proof_is_baseline != 'true'` → skips); matcher passes real specs (flight-reports 6, fan-out 3, flight 8, …) as is_baseline=false. actionlint clean.
- [x] **T-10** — as-you-type sweep A: aircraft + article + club edit forms — bind the silent `[errors]` fields (P3) + adopt debounced `liveFieldErrors` (P2); spec asserts aircraft as representative. **DONE:** all three forms converted P2+P3 — aircraft (7 newly-bound silent fields manufacturer/model/seats/MTOM/serial/noiseClass/comment + 6 touched-gated→`liveFieldErrors`), article (`articleInfo` bound + number/name→live), club (all 5 fields→live, T-07 clubKey/slug 409 routing PRESERVED). Server-duplicate 409s now route through the `liveFieldErrors` async slot (`asyncErrors$` extension point) instead of `setErrors` — a `setErrors` carries no `valueChanges` so the debounced stream never re-read it; cleared on retype. The exported `immatriculationValidators` stack is the single source the wiring spec drives. Aircraft as-you-type e2e case un-fixme'd + thickened (type `a` → debounced inline error appears, type `HB-3000` → clears), passing against the mock dev server; T-11/T-12/T-13 cases stay `fixme`. New `aircraft-edit.live-errors.spec.ts` (§8: no TestBed/DOM — asserts the exported validator stack + `liveFieldErrors$` stream + async-merge). Gates: vitest 511+69 green, lint/tsc clean, prettier check clean, playwright `--list` compiles, aircraft e2e green.
- [x] **T-11** — as-you-type sweep B: flight-type + location (incl. IOP rows) + person edit forms — same; spec asserts person + flight-type. **DONE:** all three forms converted P2+P3 — flight-type (name/code/minSeats → `liveFieldErrors`; name + code server-duplicate 409s rerouted from `setErrors` to the async slot so the debounced stream surfaces them; `markDuplicate` deleted, `clearDuplicateOnEdit` now clears the async signal), location (7 top-level fields → live, ICAO 409 rerouted through the async slot, all 4 IOP-row fields newly bound via a per-row `liveFieldErrors` cache built in the component injector — `iopErrors(row, field)` keyed off the row FormGroup so dynamic FormArray rows track typing), person (firstname/lastname/email → live, previously-SILENT city/mobile/memberNumber newly bound + live). Both T-11 e2e cases un-fixme'd (person + flight-type as-you-type) and passing against the mock dev server; the person case's stub chrome-entry was a `<table>`/`role=row` guess (the list is an af-data-table → `person-row-<id>` routerLink, same correction T-08 made on /clubs) — fixed + detail GET routed for a single person. New `persons-edit.live-errors.spec.ts` (§8: no TestBed/DOM — the lastname required + memberNumber maxLength stacks feeding the debounced stream). Gates: vitest 514 (+3) green, test:scripts 69 green, lint/tsc(main+e2e) clean (only the known pre-existing membership:269 exactOptional error), prettier check clean, playwright `--list` compiles.
- [x] **T-12** — as-you-type sweep C: planning-setup + user (+ roles-≥1 live) + profile 4 tabs — same. **DONE:** all 5 form files converted P2+P3 — planning-setup (startDate/endDate/locationId → `liveFieldErrors`), user (username/friendlyName/notificationEmail → live; previously-SILENT phone/remarks newly bound + live; roles-≥1 made LIVE via a debounced `rolesInteracted` value-change signal off the 5 role controls — the error now surfaces once the user empties the checkboxes, not only on submit, OR-ed with the existing `submissionAttempted` path), profile-account (friendlyName/notificationEmail → live + previously-silent phone bound; **languageId T-08 required validator PRESERVED** and now renders live), profile-personal (addressLine1/emailPrivate/emailBusiness → live + 8 previously-silent maxLength fields newly bound: addressLine2/zip/city/region/privatePhone/mobilePhone/businessPhone/faxNumber), profile-pilot (licenceNumber → live). No dedicated profile/user/planning-setup case existed to un-fixme (the representative trio is T-10/T-11; the only remaining `test.fixme` is flight-edit = T-13) — proof is two §8 component unit specs: `users-edit.live-errors.spec.ts` (username/email/phone stacks + the live roles-≥1 predicate) and `profile-account.live-errors.spec.ts` (friendlyName/email/phone + the languageId-required clear→error→re-pick-recovers, asserting T-08 not regressed). Gates: ng test 525 (+11) green, lint/tsc(main+spec) clean, prettier check clean, playwright `--list` compiles (238 tests). 7 files touched (5 forms + 2 specs) — within the sweep budget, no overflow.
- [x] **T-13** — flight edit: client required validators (flightDate/aircraft/pilot) + Save gated on validity + the dead `FlightValidator` wire-or-delete VERDICT recorded; spec case. **DONE.** Client fix: `buildFlightForm` grew `Validators.required` on `flightDate` + the GLIDER (primary) `aircraftId`/`pilotPersonId` only — the TOW sub-group stays unvalidated because it's a CONDITIONAL (aerotow-only) step, so a blank tow never blocks Save on a non-towing flight (`flight-form.model.ts`; `addValidators` followed by `updateValueAndValidity` — `addValidators` alone leaves the control stale-VALID). The 3 fields got `[required]` markers + as-you-type `[errors]` via the J-6b `liveFieldErrors` (T-10/11/12 pattern); BOTH Save buttons (header + sticky) now bind `[disabled]="saving() || formInvalid()"` off a REACTIVE `formStatus` signal (`toSignal(form.statusChanges)`) — the getter would lag the OnPush+zoneless binding (J-7 T-20 / T-09 race). `patch()` re-emits `updateValueAndValidity` after its `emitEvent:false` hydration so an edit-load's valid form enables Save; `finalSubmit` bails+markAllAsTouched on invalid (Enter-to-submit path). Save-gating e2e case un-fixme'd (last remaining fixme) + 3 red-first model unit tests (opens-invalid / valid-only-when-all-3 / tow-not-required). **FlightValidator VERDICT: KEEP (neither wire into create/update nor delete).** Reasoning: (1) the rules it encodes (required fields + start-type arms, port of legacy `FlightService.cs:1073-1136` ValidateFlightBasics) are the nightly Validate-job's verdict, NOT create/update gates — legacy saves an incomplete flight and flags it `Invalid` nightly; wiring it into `createFlight`/`updateFlight` as a hard reject would 400 every partial save the screen accepts today (a behavior change to shipped flights — the task's explicit risk). (2) It is NOT dead-to-delete: it is the named, still-pending dependency of **S-083** (`Port DailyFlightValidationJob`, status:todo, `depends_on:[…,S-062a]` — "this job reuses it") and **S-101** (validation-rejection depth), and is covered by `FlightValidatorTest` + `FlightCompositeValidatorTest` (the S-063 composite fork). Deleting would discard the ported rule set + force a re-port at S-083. So it stays untouched (no backend change → FE-only task, no `./gradlew`); the client `required` floor is the user-facing parity gain. Boyscout rider: a pre-existing **build-breaking** `users-edit.page.ts:366` TS7053 (T-12: `form.controls[r]` index-fails because `UserUpdateRequestRolesItem` widens to include the non-control `SYSTEM_ADMINISTRATOR`) blocked `ng serve`/the mock-e2e build — fixed minimally via `form.get(r)!` (idiomatic, no index constraint). Also fixed 3 existing flights happy-path specs (04-create/04c-paired/05-edit) whose mocks created/loaded pilotless flights (now select/hydrate a real pilot — the new required floor).
- [x] **T-14** — JDBC: `LanguageCodeLookup` → the RM-4 `Language` JPA repo (delete JdbcTemplate). **DONE:** added `LanguageRepository.findIdByCodeIgnoreCase` (explicit JPQL `SELECT l.id FROM Language l WHERE lower(l.code) = lower(:code)` on the RM-4 entity — bare-id projection, no entity hydration) and re-pointed `LanguageCodeLookup` at it, deleting the `JdbcTemplate` field + ctor + `EmptyResultDataAccessException` catch. Behavior-neutral: same case-insensitive `lower(code) = ?` semantics (the resolver already lowercases the param, so `lower(:code)` is a no-op on input), same per-JVM cache (unknown not cached), same `en` fallback, same return shape. `t_language` is system-global reference data (no `@TenantId`) → NOT a native-sql-register entry today and stays out (register is tenant-scoped-only); `NativeSqlRegisterTest` green. The "must stay JDBC / session-open recursion" rationale was FALSE for this class — it runs in the JIT servlet filter (`JitUserMaterializerImpl` → `UsersService.materializeFromJwt`, `REQUIRES_NEW` JPA tx already open), not Hibernate's tenant-resolver session-open path (that's `UserPrincipalLookup`, which legitimately stays JDBC); stale claims in `Language.java`/`CONVENTIONS.md` corrected. Unit test re-mocked onto `LanguageRepository` (assertions unchanged: known→id, unknown→en+no-cache-poison, null/blank→en+no-query, cache-after-first-hit); `UsersJitFirstLoginIT` (11/0, real DB) + `ApplicationModulesTest` (no `platform↔referencedata` cycle — both OPEN) green. Full `ALPENFLIGHT_TEST_FORKS=1 ./gradlew check` BUILD SUCCESSFUL.
- [x] **T-15** — JDBC: `JpaClubStateRepository` + `JpaCountryRepository` native → JPA. **RE-SCOPED (manager, escalation): both order by `COLLATE "de-CH-x-icu"` (ICU, no JPQL form) — a plain `ORDER BY` flips accented ordering + reds `CountryControllerIT`.** Behavior-neutral fix = move the ICU collation to the COLUMN via a Flyway `V<n>__*.sql` (`t_country.name` + `t_club_state.name` → `COLLATE "de-CH-x-icu"`; the locale already works in these clusters since the native query uses it), THEN convert both repos to derived `findAllByOrderByNameAsc()` (zero native SQL). VERIFY `CountryControllerIT` accent-ordering test (`Côte d'Ivoire` < `Cuba`) stays GREEN. Fallback ONLY if the ALTER reveals a real index/FK collation ripple that risks behavior: reaffirm both as documented non-tenant COLLATE-justified native reads (record the justification in-file) + tick — don't block. **DONE (migration path):** V40 ALTERs `t_country.name`/`t_club_state.name` to `COLLATE \"de-CH-x-icu\"` at column level (types preserved VARCHAR(100)/(50); no index/FK on either name col); both repos → plain JPQL `ORDER BY` (zero native SQL). `./gradlew check` green incl. `CountryControllerIT` accent-ordering (Côte d'Ivoire < Cuba). Verified+committed by manager (worker paused pre-check).
- [x] **T-32** — (gate-revealed, continues T-31) BASELINE run must be baseline-shaped THROUGHOUT: the gallery-gen step passed `--journey-under-work J-26` even when `is_baseline=true` → built the empty J-26 page → the `Live link-check (200 + captions)` step (gated on is_baseline==true) found NO J-0 captions → proof job red. **DONE:** gate `JUW_ARG` on `proof_is_baseline != 'true'` (ci.yml gallery-gen step) — a baseline run now deploys the J-0 baseline page (captions present); `--journey-under-work` + the J-26 Maintainability delta return when the real spec lands (is_baseline=false). actionlint clean. Manager-diagnosed+fixed inline.
- [x] **T-16** — JDBC: `PlanningDayPersistenceProbeImpl` → a reservations-module count port (`@NamedInterface`, per the register's own Remove-when) + retire the `planning-day-reservation-count` register entry + Modulith/arch guards green. **DONE:** new `reservations.api.ReservationCountPort` (`@NamedInterface("reservation-count")`) implemented by `JpaReservationCountAdapter` → `JpaAircraftReservationRepository.countActiveOnDayAtLocation` (plain JPQL over `AircraftReservation`; `date(reservation_start)=:planningDate` cast → derived half-open UTC-day range `reservationStart >= dayStart AND < dayStart+1day`, `@TenantId`-filtered, ZERO native SQL). `PlanningDayPersistenceProbeImpl` now delegates the count to the port (native `COUNT_SQL` + tenant resolver deleted). Register entry moved to `## Retired` (dated 2026-06-12). `ApplicationModulesTest` + `NativeSqlRegisterTest` + planning ITs green via `./gradlew check`.
- [x] **T-17** — register re-affirm pass: conflict-probe keep-GiST-vs-JPQL decision RECORDED; ShowcaseSeeder + persons cross-tenant entries re-affirmed or retired; register doc current. **DONE (DOC-ONLY, no code change):** full re-affirm pass over the live tree — all 5 `## Approved escape hatches` entries verified (caller file exists, still makes the native/JDBC call, justification still a structurally-pre-tenant / vendor-SQL-feature / system-actor-null-tenant seam), all **KEPT**, none retired. Conflict-probe **keep-vs-convert decision = KEEP GiST**, recorded in-entry: the `&&` range-overlap over the `GENERATED ALWAYS … STORED reservation_range tstzrange` (V4:210-211) riding the partial GiST `ix_arv_aircraft_range_gist` (V4:245-246) has no JPQL form — a vendor-SQL-feature fast path (rule lives on `AircraftReservation.conflictsWith`), NOT a complex-read read-model candidate; derived-JPQL fallback evaluated + rejected, so only the Hibernate-feature path now retires it. New `## Re-affirm log` records: (1) the per-entry verdicts; (2) the two structurally-pre-tenant `t_user` JDBC sites (`UserPrincipalLookup` session-open resolver + `PreTenantUserLookup` handshake provisioning) reviewed — they touch only `t_user`, which is **not** `@TenantId`-scoped (cross-tenant `User` aggregate), so they're correctly absent from a tenant-scoped-only register and stay JDBC; (3) T-14/T-15/T-16 + RM-4 conversions (`LanguageCodeLookup`, `JpaClubStateRepository`, `JpaCountryRepository`, `PlanningDayPersistenceProbeImpl`, `MeService`) confirmed gone — only javadoc-retirement notes remain in those files, none touched a tenant-scoped table so none held an entry to remove. Ground-truth native inventory cross-checked against the `@TenantId` roster: every tenant-scoped native site has an entry, every entry maps to a real site. Changed ONLY `native-sql-register.md` + this checklist (markdown) — no Java touched, so per DoD gradle not required; ran `NativeSqlRegisterTest` specifically → BUILD SUCCESSFUL (caller-scan + expiry-hygiene both green).
- [x] **T-18** — ~~IT seeding: `TenantScopedRowBuilders` (+ `TenantScopedEntityCatalog`) → production-code seeding~~ **FOLDED into T-20 (manager, empirical re-scope):** the named files are already ADR-0027-clean; their only JDBC seam (`SweepFixtureContext.jdbc()`) is inseparable from the 5 Sweep factories T-20 owns. No independent deliverable — see the worker note below. **Empirical re-scope of the whole IT-seeding block** (queried the tree, not estimated): of **84** JDBC test files, only **44 SEED (INSERT)** — the other **40 use JDBC purely for assertion (SELECT) / teardown (DELETE)**, which is NOT the ADR-0027-§3 seeding anti-pattern (no domain-invariant bypass). ADR 0027 §3's own rule is **convert per-touch, NOT a sweep** — so this journey converts the **shared seeders** (TwoClubFixture's club-seed T-19 + the 5 Sweep factories T-20) and records the ~38 per-IT seeders + 40 assert/teardown files as the standing per-touch rider (below), rather than a forbidden 84-file sweep.
  WORKER OVERFLOW NOTE (no convertible JDBC in T-18's named files; the JDBC is T-20's): both T-18 files are already ADR-0027-clean — `TenantScopedEntityCatalog` does pure classpath scanning (zero seeding), and `TenantScopedRowBuilders` builds **transient** aggregates via PRODUCTION factories (`MemberState`, `Location.create`, `Flight.createGlider`, `AircraftReservation.create`, `Article.register`, `FlightType.register`, `PersonClub.forSweepFixture`, `MutationAuditEvent.builder`) that the consumer (`LeakageSweepIT`) persists via the entity's Spring Data `JpaRepository` under `runAs(clubId,…)` — already the production path. `TenantScopedRowBuilders`'s ONLY JDBC is the `SweepFixtureContext(JdbcTemplate jdbc)` record (the shared seam); the 15 real `ctx.jdbc()` seeding/lookup calls all live in the **5 Sweep*Factory files** (`Flight`/`FlightReportRow`/`PlanningDay`/`Location`/`AircraftReservation`), which are **T-20's named scope** ("Sweep factories → production code"). Removing the `JdbcTemplate` from `SweepFixtureContext` REQUIRES converting those 5 factories' FK-parent INSERTs (`t_aircraft`/`t_person`/`t_location`/`t_flight`/`t_aircraft_reservation_type`) to production save paths — 5 distinct FK-parent aggregates, each a separate production path, >8 files, = T-20's whole body under T-18's id. **Resolution: FOLD T-18 into T-20** (the `SweepFixtureContext.jdbc()` retirement is inseparable from converting the factories that consume it); T-18 has no independent deliverable. Consumers (only 3: `LeakageSweepIT`, `TenantSweepFloorAndPinTest`, `NativeSqlRegisterTest` — NOT ~40) need no T-18 change. No commit made.
- [ ] ~~**T-19**~~ **RESOLVED → Option B (operator, 2026-06-13): production mints ids, tests DISCOVER them back (pure JPA, zero test JDBC, NO production supplied-id path).** The pinned-id `CLUB_A`/`CLUB_B` constants are replaced by ids captured from production club-creation. Split into backward-compatible batches (29 files total; small batches = death-resilient):
  - [x] **T-19a** — TwoClubFixture gains a production-create path: `seedReturningIds()` (or accessor change) creates clubA/clubB via the real `Club.create(...)` + `ClubRepository.save` under tenant context, returns the MINTED ids via `clubA()`/`clubB()`; a slug-sanitizer derives a valid unique `^[a-z0-9-]{3,64}$` slug from each consumer's name/key prefix (the ~24 invalid-slug prefixes). KEEP the old JDBC ctor (deprecated) so all 28 consumers still compile+pass. Teardown by minted ids. `./gradlew check` green. **DONE (finisher):** production-create path + deprecated JDBC ctor + slug sanitizer landed as drafted. Fixed the two gate reds: (1) `TwoClubFixtureProductionPathIT` collided on `ux_club_key`/`ux_club_slug` with `FlightReportQueryServiceIT` (both `IT_FRQ`) → re-prefixed to the task-id-derived, no-other-consumer `T19A`; made the production pre-clean rerunnable by matching prior rows by slug OR key INCLUDING soft-deleted (ux_club_key is a FULL index) and hard-deleting before re-mint. (2) the draft's per-club `deleteTenantScopedRowsFor(clubA)` then `(clubB)` broke cross-club FK order — a clubB flight/reservation on a clubA charter aircraft (S-058) → `DELETE t_aircraft ... violates FK`; restored the proven global child→parent ordering by making teardown take a `List<UUID>` and spanning every club with `IN (...)` at each dependency level before descending (legacy both-clubs path delegates to it; production path passes both prior clubs together). `./gradlew check` BUILD SUCCESSFUL (5m07s, all green).
  - [x] **T-19b** — migrate consumer batch 1 (9 ITs) to the minted-id API: drop the `CLUB_A`/`CLUB_B` constants, capture `clubA = fixture.clubA()` after seed, replace in-class references (incl. principal/tenant setup) with the captured ids. `./gradlew check` green. **DONE:** all 9 migrated to the production-create ctor (`@Autowired ClubRepository`/`CountryRepository`/`ClubStateRepository`; ids captured into `@BeforeEach` instance fields). Three carried the hard case — class/method-level `@WithTenant(<literal>)` can't hold a runtime-minted id, so the tenant context is now established at runtime inside each test via `TenantTestContext.runAs(clubA, …)` (`MemberStateTenantIsolationIT`, `TenantsRunAsIT`, `FlightTimeGateIT`); the no-tenant tests in those classes stay unscoped. The other six (Persons×2, both me-dashboards, FlightsTowLink, FlightProcessStatePatch) drove the id purely through runtime JWT claims / JDBC params, so were a straight constant→field swap. `./gradlew check` green.
  - [x] **T-19c** — migrate consumer batch 2 (~10 ITs), same pattern. `./gradlew check` green.
  - [x] **T-19d** — migrate the remaining ~8 ITs + DELETE TwoClubFixture's old JDBC insert ctor + the seed JDBC; keep ONLY hard-DELETE teardown JDBC (legit test infra). Fixture is now zero-seed-JDBC. `./gradlew check` green. **DONE:** the final batch of 9 consumers migrated to the production-minted-id path (`@Autowired ClubRepository`/`CountryRepository`/`ClubStateRepository`; ids captured into instance fields after `seed()`). Five were a straight `CLUB_A`/`CLUB_B` constant→field swap (`PlanningDayRepositoryIT`, `AircraftReservationRepositoryIT`, `DeploymentContextIT`, `CrossTenantPositiveSweepIT`, `LeakageSweepIT` — T-20's sweep-factory `SweepFixtureContext` wiring preserved); three carried the `@WithTenant(<literal>)` hard case (`ArticlesTenantIsolationIT`, `AircraftsTenantIsolationIT`, `LocationsTenantIsolationIT`) → annotation dropped, tenant entered at runtime via `TenantTestContext.runAs(clubA, …)`; `CrossTenantNotFoundContract` (abstract base) moved its `protected static final CLUB_A/CLUB_B` to captured `protected` instance fields its `LocationsCrossTenantNotFoundIT` subclass inherits (the subclass referenced no club-id constant directly). With all 28 consumers on the production path, **DELETED** the deprecated `TwoClubFixture(jdbc, clubA, clubB, …)` constructor, `seedViaLegacyJdbc`, `deleteClubs()`, the no-arg `deleteTenantScopedRows()`, and `insertClub()` — the fixture now has ZERO seed-INSERT JDBC (8 teardown DELETEs + the by-slug/key pre-clean remain, all legit test infra). `./gradlew check` BUILD SUCCESSFUL.
  ORIGINAL BLOCKER (why B, not pinned-id JPA): `Club.id` is `@GeneratedValue(UUID)`; Hibernate 7's generator overwrites any reflection-set id at insert, so no JPA write places a row at a chosen id (merge→optimistic-lock, persist→detached, stateless-insert→fresh id) — the ADR-0027-§4 reflection hatch is closed for `@GeneratedValue` ids. Option B sidesteps it by never pinning. ORIGINAL scope: **Empirical blocker (worker-proven on LAN PG / Hibernate 7):** `Club.id` is `@GeneratedValue(UUID)` with NO production supplied-id path; the 28 consumers require PINNED `CLUB_A`/`CLUB_B` ids; reflection-setting the id does NOT survive (Hibernate 7's `UuidGenerator` is a `BeforeExecutionGenerator` → overwrites it at insert; merge→optimistic-lock 0-rows, persist→detached, stateless-insert→fresh id). So the ADR-0027-§4 "use reflection for non-settable attrs" escape hatch is CLOSED for `@GeneratedValue` ids → pinned-id shared seeds can't go pure-production without either a production supplied-id factory (operator forbade prod-code-for-tests) or a 28-consumer id-discovery rewrite (out of scope, changes shape). This is the SAME structural reason the `tenancy-showcase-seed-deterministic-ids` register entry exists. Secondary: ~24 consumers seed slugs the production `^[a-z0-9-]{3,64}$` regex rejects (raw INSERT bypassed it). Awaiting operator call on pinned-id test-seeding policy (see AskUserQuestion). ORIGINAL scope: IT seeding: `TwoClubFixture` (28 consumers) **SEED** path → production code: the `t_club` INSERT + the `t_country`/`t_club_state` ref-data lookups become real aggregate creation (`Club` factory + `JpaRepository.save` under `runAs`/tenant context). **KEEP the DELETE teardown JDBC** — hard teardown that bypasses soft-delete is legitimate test infra, NOT the §3 seeding anti-pattern (don't convert it). All 28 consumers compile + pass UNCHANGED (behavior-neutral); `./gradlew check` green on LAN PG. Class-unique club ids preserved.
- [x] **T-20** — IT seeding: the **5 Sweep factories** (`Flight`/`FlightReportRow`/`PlanningDay`/`Location`/`AircraftReservation`SweepFactory) FK-parent JDBC seeding → production save paths + retire `SweepFixtureContext.jdbc()` (the T-18 seam). The 3 consumers (`LeakageSweepIT`, `TenantSweepFloorAndPinTest`, `NativeSqlRegisterTest`) pass unchanged; `./gradlew check` green. **Then record the per-touch remainder** (the ~38 individual-IT JDBC seeders + 40 JDBC assert/teardown files — NOT converted here per ADR 0027 §3 per-touch rule) as a `_BOYSCOUT.md` rider with the empirical inventory, so the long tail converts on next touch rather than as a sweep. **DONE:** all 5 factories' FK-parent JDBC INSERTs → production save paths; `SweepFixtureContext` is now a standalone class carrying the Spring `WebApplicationContext` (no `JdbcTemplate`) exposing typed `seed*` helpers (Aircraft/Person via direct save — cross-tenant; Location/Flight/AircraftReservationType via `runAs(realClub)` save so the FK parent gets a real `@TenantId` even under the NO_TENANT path; reference-data ids off the domain reference repos; `NOT_PROCESSED` straight off the `FlightProcessState` enum). **Every FK-parent id is MINTED, none externally pinned** → the T-19 `@GeneratedValue`-overwrite wall never applies; `SweepFixtureContext.jdbc()` fully RETIRED. The `FlightReportRowSweepFactory` reflection-pin (`setId`) is gone — it now projects off the persisted Flight's minted id; `seedFlight` removes the incidentally auto-projected read-model row (J-7 RM-1 `FlightSaved`→projector) through the production report-row repo so the sweep's own insert is the first (fails fail-closed at the operating_club_id FK, not the PK). 3 consumers unchanged; `./gradlew check` green. Per-touch remainder + pinned-id-stays-JDBC rider appended to `_BOYSCOUT.md` (84 JDBC test files: 44 seed / 40 assert-teardown — grep-verified).
- [x] **T-21** — lifecycle `@MappedSuperclass` verdict EXECUTED (ADR 0028): EXTRACTED the boundary-clean soft-delete base (`platform.persistence.SoftDeletableAggregate`) across Aircraft/Location/FlightType (the real clone was a triplet, not 5 — Flight/Person softDelete diverge); the `@DomainEvents` saved-event hook stays per-aggregate (module-coupled); cpd-baseline ratcheted 4883→4827.
- [x] **T-22** — FE hotspot extraction 1: shared form↔request + `errorPatch` helper; absorb `aircraft-edit.page.ts` + `users-edit.page.ts`/`users.store.ts`. **DONE:** extracted a shared store-`errorPatch` classifier `shared/util/form/error-patch.ts` — `classifyApiError(e, rules, fallback)` walks an ORDERED `SaveErrorRule[]` (`{status, when?, outcome}`) so each store keeps only its declarative `(status, body-predicate) → (kind, message)` table, no `if (status===…)` cascade (+ `problemDetailBody` / `genericSaveErrorMessage` helpers, barrel-exported). Wired `users.store.ts` (`userErrorRules(op)`) AND `aircraft.store.ts` (`aircraftErrorRules(immat)`); behavior verbatim (verb-dependent 409 discriminators, the `e.message` tails preserved). Form→request side leaned on the existing `withOptionals` (J-5): aircraft `formToCreate/UpdateRequest` collapsed onto a shared `aircraftRequestBase` + `aircraftSharedOptionals` (~36 hand `if (v.x !== '') req.x` branches → one pruning pass; update-only `flightOperatingCounterUnitTypeId` echo kept); users-edit `onSubmit` decomposed into `buildInviteRequest`/`buildUpdateRequest` (withOptionals) + a table-driven `checkedRoles`/`ROLE_KEYS` (also retires the T-13 `form.get(r)!` workaround — narrowly-typed role keys index cleanly). **Complexity (fallow):** `users.store errorPatch` CRAP 160→13.8 (file crap_max); `users-edit onSubmit` CRAP 156→30, `checkedRoles` (42) gone; aircraft `formToCreate/UpdateRequest` (71.3/79.4) both gone (file crap_above_threshold 4→2, total_cyc 143→119). T-23 folds flights+persons onto the same `classifyApiError`. preflight:web (lint+tsc+ng build) green; full `ng test` 533 green (incl. 5 new classifier tests); playwright `--list` 238 compiles. Pre-existing unrelated e2e flakes in locations/persons/reservations (confirmed red on clean HEAD, untouched features).
- [x] **T-23** — FE hotspot extraction 2: `flights-edit.page.ts finalSubmit` + `flight-form.defaults.ts applyLastContextThenPrefs` (CRAP 210) + `persons-edit.page.ts hydrate` onto the helper. **DONE:** persons `store.errorPatch` folded onto the shared `classifyApiError` (declarative `personErrorRules` table: 403 → forbidden, the two 409 `type`-URN discriminators ahead of the broad 409 catch-all; the `field`-prefixed generic tail kept byte-identical — the empty-`msg` branch falls to `err.message`, NOT `genericSaveErrorMessage`, to avoid surfacing an empty detail). Persons-edit form→request collapsed onto `withOptionals` (`personCreateRequest`/`personUpdateRequest` + shared `contactOptionals`/`membershipOptionals`; `onSubmit` CRAP 132→30, `membershipRequest` 42→below-threshold). Flights `applyLastContextThenPrefs` (the CRAP-210 worst offender — a precedence-merge, NOT error-patch-shaped) reduced via a table-driven `coalesceCrew(base, source, pairs)` over `GLIDER_CTX_PAIRS`/`TOW_CTX_PAIRS` (`base ?? ctx ?? null` per `[snapshotKey, ctxKey]`): **file crap_max 210.7→13.8**, cyc 29→below-threshold. Flights `finalSubmit` (prefs-persistence ladder, also NOT error-patch-shaped) split into `saveSnapshot` + `persistFlightPrefs`: CRAP 240→56, cyc 15→7. **Flights `store.errorPatch` deliberately UNTOUCHED** — its 412→field-conflict-resolver / 409→reload-toast handling is an optimistic-lock state machine, not a `{saveError,saveErrorKind}` kind-table; `classifyApiError` doesn't fit and folding it would change behavior (409/412 field-routing stays byte-identical). Behavior-neutral: all J-26 work preserved (T-04 forked-update + membership full-replace, T-11 liveFieldErrors, T-13 flight validators/Save-gating). +9 unit tests (6 persons `errorPatch` classification cases through the public `update` path + 3 `applyLastContextThenPrefs` precedence cases). `ng test` 542 green; `preflight:web` (lint+tsc+`ng build` prod+drift) green; flights+persons-edit e2e 11/11 green; `playwright --list` 238 compile. The 3 pre-existing mock-e2e reds (locations:448/persons-add-modal:430/reservations:690) stay red on clean HEAD (T-26b owns); `persons-edit.page.ts hydrate` (a plain `patchValue`, no error-patch/form-mapping shape) left as-is per judgment.
- [x] **T-24** — fallow dead code: 3 unused files + 6 unused deps + 1 unresolved import deleted; fallow snapshot improves. **DONE:** re-ran `npx fallow@latest dead-code` (current honest list, config-loaded) = 12 issues. Removed the genuinely-dead one-shot legacy-translations migration module `scripts/migrate-translations/` (`migrate.ts` + `emit.ts` were fallow's 2 flagged dead files; `parse-legacy.ts`/`transform.ts`/their 2 specs/`__fixtures__` only existed to feed the deleted CLI — deleted the whole cohesive unit, not a half, leaving no spec-only orphans; run once at S-057, output `src/i18n/{de,fr,it,en}.ts` committed, no `src/` or i18n-gate references). Removed the 5 redundant `@angular-eslint/*` devDeps (`builder`/`eslint-plugin`/`eslint-plugin-template`/`schematics`/`template-parser`) — all pinned-21.4.0 direct deps of the umbrella `angular-eslint@21.4.0`, so still installed transitively + hoisted (verified PRESENT post-install; `ng lint`'s `@angular-eslint/builder:lint` builder still resolves). `pnpm-lock.yaml` re-synced (`--frozen-lockfile` green). **3 fallow false-positives KEPT (not deleted), suppressed in `.fallowrc.json` so the snapshot is honest:** (1) `@angular/animations` "unused dep" — `provideAnimationsAsync()` dynamic-imports it (app.config.ts/.mock.ts); it's an OPTIONAL peer of platform-browser so removal would leave it uninstalled + break the build → `ignoreDependencies`; (2) `orval` "unresolved import" in `orval.config.ts` — installed devDep + used by `generate-api`, the import is correct/live → `ignoreUnresolvedImports` (api-drift green); (3) `e2e/proof-gallery/generate-previews-index.mjs` "unused file" — invoked by CI `rebuild-previews-index/action.yml` + dynamic-`import()`ed by 2 live specs → declared as a fallow `entry` point. **fallow dead-code 12→1** (the residual `@api/generated` "unlisted dep" is a tsconfig path-alias, never an npm pkg, out of T-24 scope). preflight:web deterministic stages all green: lint ✓ / tsc ✓ / `ng build` prod ✓ / api-drift ✓ (zero drift); `ng test` 542 green; `test:scripts` 53 green; `playwright --list` 238 compile. The preflight e2e-runtime stage reds on the **pre-existing esbuild `ng serve` dev-server concurrent-read deadlock** (CLAUDE.md §9) — confirmed IDENTICAL red on clean HEAD with my changes stashed, so NOT a T-24 regression (my diff touches zero `src/`/`e2e/`/build config).
- [x] **T-25** — proof infra: upload `test-results/**` (error-context.md + trace.zip) as a failure artifact in ci.yml proof + fanout BEFORE the gallery step mutates the dir. **DONE:** added an `actions/upload-artifact@v4` step gated on `if: failure()` immediately AFTER the real-idp Playwright `pw` step (so the per-test `error-context.md`/`trace.zip`/failure screenshot+video are captured intact) and BEFORE the staging/gallery/shots-present-guard steps that consume or mutate `test-results/` (the guard re-invokes Playwright, which cleans its output dir). Inserted in ALL THREE same-shape ci.yml jobs — `alpenflight-proof` (ci.yml:853, after the journey-under-work real-idp spec), the J-3 `dashboard-proof` (ci.yml:1661), and the REQUIRED J-4 `profile-proof` (ci.yml:2060) — plus the fanout AlpenFlight parity step (`alpenflight-proof-fanout.yml:776`, after the blocking `pw` parity specs; the non-blocking legacy aids are `continue-on-error` so their reds don't trip `failure()`). Artifact path `alpenflight/web/test-results/**` (verified empirically: Playwright sets no `outputDir`, per-test dirs land at `alpenflight/web/test-results/<test>/` — confirmed by a real local run carrying `error-context.md` + `trace.zip` + `video.webm`), `retention-days: 7`, `if-no-files-found: warn`, distinct artifact name per job (`playwright-test-results-<job>-${{ github.run_id }}`). The pre-existing always()-uploaded proof artifact stays (it lands after the gallery has consumed the dir, so only `proof-manifest.json` survives there). actionlint green on both files.
- [x] **T-26** — proof infra: staged==rendered single source of truth (fanout `add_shot` json emission ↔ `generate-gallery.mjs` pairing) + deployed-journey guard asserts BOTH sides of every declared pair render. **DONE:** the drift wasn't the two readers reading *different files* (both already read `screenshots.json`) — it was the `[shots-present]` guard globbing PNG presence while the generator rendered the `extractScreenshots` entries, two independent reads that could disagree (and the per-context tolerance let a one-sided render ship green per-push while the deployed page showed the other side "pending"). Structural fix: (1) new exported **`renderedShotKeys(shots, journey)`** in `generate-gallery.mjs` is the SINGLE source — it projects exactly the shots the generator renders to `<side>:<view>` keys; `loadStagedShotKeys` (spec) was rewritten to read through `extractScreenshots` → `renderedShotKeys`, so "present"==`"rendered"` definitionally (one function, two callers). (2) `tryAssertJourneyComplete` (the deployed-journey guard) now parses the deployed page's render keys off each `<img alt="<side> <view>">` and, for every `expected` pair in `expected-shots.json`, asserts BOTH sides RENDER on the deployed page — a one-sided declared pair REDS (was: ≥1-asset + assets-200, which a half-pair passed). Per-context tolerance mirrors `[shots-present]`: `GALLERY_PROOF_CONTEXT` wired into BOTH deployed-journey steps (ci.yml=`proof`, fanout=`fanout`) so a side `producedBy` the other context is tolerated-absent on this deploy; `pending`/un-captured shots are never asserted (all-pending early-journey case stays green, T-31/T-32). Proof: 3 new generator unit tests (`generate-gallery.spec.ts`) reproduce the one-sided-stage drift on a fixture and prove `renderedShotKeys` == the page's rendered img-alt keys (+ the absent-PNG drop). `pnpm test:scripts` 56 green; browserless `proof-gallery-links` + `[shots-present]` (J-1) green; tsc/eslint/prettier clean on the 3 touched files (only the pre-existing `persons-edit-membership.spec.ts:269` exactOptional error remains, untouched); actionlint clean on ci.yml + fanout. Files: `generate-gallery.mjs`, `generate-gallery.spec.ts`, `proof-gallery-links.spec.ts`, `ci.yml`, `alpenflight-proof-fanout.yml`. `_BOYSCOUT` staged-≠-rendered rider cleared.
- [x] **T-26b** — (gate residual, found at T-22) 3 PRE-EXISTING mock-e2e reds on clean HEAD + the clubs flake — all diagnosed to ROOT CAUSE and FIXED (no `.skip`, no mask). **DONE:** (1) `locations-crud:448` (IOP add/remove round-trip) — the `nested in/outbound points` row rendered ABORTED mid-way: the J-26 T-11 `iopErrors(iop,field)` called `liveFieldErrors()` (→ `toSignal()`) LAZILY FROM THE TEMPLATE, and `toSignal()` inside a reactive context throws **NG0602**, which silently killed the rest of each IOP row's bindings (the nested `@if` remove button + the per-row `[attr.data-testid]` for rows > 0 vanished). Fix: build each row's live-error signals EAGERLY in `makeIopGroup()` (`untracked` + `runInInjectionContext`, outside any reactive ctx) and make `iopErrors()` a pure cache READ — spec + testids unchanged. (2) `persons-add-modal:430` (edit round-trip) — the spec's mock `setupPersonsBackend` predated the J-26 T-04 forked-update, so the now-fired `PUT /persons/{id}/clubs/current` fell through to the live mock-auth net → 401 → store errorPatch → the page never left /edit; added the `clubs/current` PUT handler (full-replace, mirrors the J-26 membership spec, exactOptional-clean via the file's `optional()` helper). (3) `reservations-crud:690` (all-day band) — toggling all-day `disable({emitEvent:false})`'d the required time controls so the form went valid, but `emitEvent:false` SUPPRESSED `statusChanges`, so the `formStatus` signal driving `saveDisabled` stayed INVALID → Save stuck disabled → the create POST never fired; fix: a trailing `this.form.updateValueAndValidity()` in the all-day effect re-emits the status. (4) `clubs-crud:159` (edit round-trip) FLAKE — the post-`reload()` row click raced the bootstrap list re-fetch under suite load; stabilised by awaiting the `GET /clubs` 200 + the row's re-rendered text before clicking. Verified: each spec re-run green vs the local mock dev server (`ng serve --configuration=mock-auth`, single-worker — the 4-worker parallel run trips the CLAUDE.md §9 esbuild concurrent-read deadlock); all 33 tests across the 4 touched crud files + the 11 J-26 specs green; `ng test` 542 unit green; prettier/eslint/tsc(app) clean; `playwright --list` 238 compiles (only the KNOWN pre-existing `persons-edit-membership.spec.ts:269` exactOptional e2e-tsc error remains, untouched).
- [ ] **T-27** — thicken both specs to full real assertions; gallery pairing complete; clear every shipped `_BOYSCOUT.md` bullet; full-unskip prep for the gate.

## Assumptions made

1. The 60/40 rule is inverted for THIS sprint by explicit operator direction
   ("no new features… tech-debt, cleanup and ux only"); the user-facing share
   is bugfixes/UX corrections, still provable by one green Playwright run.
2. Multi-screen by design — hardening sprints span shipped screens (J-6b
   precedent); the one-screen rule applies to feature journeys.
3. The nightly real-idp fix RIDES this journey (no-tiny-stories); main's
   nightly stays red until merge — operator may request a hotfix PR instead.
4. Journey id J-26 (next free; J-8…J-25 assigned). Ships before J-8 — order ≠ id.
