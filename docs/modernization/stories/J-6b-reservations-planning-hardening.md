---
id: J-6b
title: Reservations & Planning hardening + inline form validation
epic: E-08
status: in_progress
journey0: false
carved: true
started_at: 2026-06-07
depends_on: [J-5, J-6]
rolls_up: []   # operator field-test polish, not horizontal S-stories
acceptance:
  # ≥60% feature — inline form validation (the new vertical capability)
  - "[happy] an edit form shows per-field validation messages WHILE TYPING, debounced ~200ms, inline under each field — client-side for trivial rules"
  - "[happy] a non-trivial field is validated by a server-side validate endpoint; its message surfaces inline on the same field without a full submit"
  - "[key-error] a field that fails client validation blocks submit AND shows its inline message; clearing the value clears the message (debounced)"
  # Reservations calendar polish (J-5 surface)
  - "[happy] clubadmin sees a 'Reservations' nav entry and navigates to /reservations"
  - "[happy] the Day/Week toggle renders the SELECTED button with the design's selected style (dark fg-bg + light text per screens-reservations.jsx:106-110), legible — not blacked-out"
  - "[happy] switching to Week mode changes the pager from day-stepping to week-stepping; the label shows the week start–end date range (e.g. '21–27 May 2026')"
  # Planning-day read-only / edit-mode (J-6 surface)
  - "[happy] read-only planning-day view renders ALL fields disabled/read-only — not merely the Save button hidden"
  - "[happy] read-only planning-day view exposes an 'Edit' affordance that switches the form into edit mode"
  - "[happy] Planning → edit planning-day → open a reservation from the inline list → Cancel → returns to the planning-day edit form (not the /reservations overview)"
  # Cross-cutting platform polish
  - "[happy] date inputs + date display render DD.MM.YYYY"
  - "[happy] the flights-list date-range picker works — selecting a from–to range filters the list (operator: currently broken)"
  - "[happy] EVERY aggregate list has ≥1 row for EVERY testuser — found EMPIRICALLY: seed the dev DB, then run a SELECT count per aggregate-table per testuser/club to find the actual empties, seed those (don't hand-enumerate; let the SELECTs tell the truth)"
  - "[happy] clubadmin1 opens the Users menu and the list renders (no 400 Bad Request)"
  - "[edge] clubadmin1 does NOT see a 'Clubs' nav entry"
screen: /reservations + /planning (hardening of J-5/J-6) + the shared edit-form + nav shell
headless_pulled_in: server-side form-validate endpoint(s) → consumed by inline-validation UX on edit forms
migration: N/A — no new entity; hardens shipped J-5 (AircraftReservation) / J-6 (PlanningDay). #6 is dev-seed, not a mapper.
parity_test: alpenflight/web/e2e/tests/real-idp/reservations-planning-hardening.spec.ts (+ the mock inner-loop specs forms/ + reservations/ + planning/ — see Notes)
# T-02 per-push gate scope (read by ci.yml `changes` job, off `integration/J-6b`):
#  • mock_test → the per-push `alpenflight-mock-e2e` filter. J-6b's OWN mock specs
#    span THREE feature dirs (reservations/planning/forms), so this is a single
#    Playwright positional REGEX (an alternation over the three J-6b spec stems),
#    NOT a dir — a dir token would also pull prior journeys' specs in that dir
#    (J-5 reservations-crud, J-6 planning-crud) and violate "ONLY J-6b's specs".
#    The derive step (`Derive journey mock-e2e filter`) recognises a regex token
#    by its alternation branches and validates each branch's path stem on disk.
#  • parity_test (real-idp half) → `tests/real-idp/reservations-planning-hardening.spec.ts`
#    (T-17): the heavy-chain sibling that proves the load-bearing flows ONLY a real
#    principal can — clubadmin1 (real CLUB_ADMINISTRATOR) sees Reservations + NO
#    Clubs (AC4/AC14), sysadmin DOES see Clubs (the role-gate positive control),
#    clubadmin1's Users list renders (AC13, no 400), the V36 seed gives ≥1 row per
#    aggregate (AC12), and the greenfield Day/Week calendar renders the legible
#    toggle + DD.MM.YYYY label (AC5/AC6/AC10) + the planning read-only/edit form
#    (AC7/AC8). The `Derive journey proof spec` step now resolves THIS spec (it is
#    under tests/real-idp/), no longer the J-0 Locations baseline fallback.
mock_test: alpenflight/web/e2e/tests/(reservations/reservations-hardening|planning/planning-hardening|forms/inline-validation)   # per-push mock-e2e runs ONLY J-6b's 3 specs (12 tests); prior journeys' mock specs run at the §4 gate + nightly
adr_refs: [0022, 0024, 0008, 0006]
---

## Context

J-5 (reservations) and J-6 (planning) shipped, then operator field-testing turned up
11 rough edges across those two surfaces plus the nav shell, the shared edit forms, and
dev-seed. This journey hardens that surface. Its **≥60% feature** is a genuinely new
cross-cutting capability — **inline validation-while-typing** on edit forms (client-side
where trivial, a server-side validate endpoint for the rest) — built on the planning +
reservation edit forms and extracted into shared infra. The **≤40% tech-debt** folds the
remaining bug/UX fixes (nav role-gating, toggle styling, week paging, read-only/edit-mode,
cancel-return nav, DD.MM.YYYY, seed coverage, Users-400, Clubs-menu-hide). Provable by one
green Playwright run driving the reservations calendar + planning edit + a representative
edit form's debounced inline validation.

## Spec must assert

**Inline form validation (the feature).** A representative edit form (planning-day or
reservation edit — both already on this surface) shows per-field error messages **while the
user types**, debounced ~200ms, rendered inline under the field via the existing
`<af-field-errors>` molecule (`shared/ui/molecules/field-errors/`, S-007). Trivial rules
(required, range, format) validate client-side from the reactive-form validators; a
non-trivial rule (the kind legacy enforced server-side — e.g. an overlap / cross-entity /
uniqueness check) calls a **server-side validate endpoint** and surfaces its message inline
on the offending field, *without* a full save round-trip. Today the edit pages only show a
field's errors on `touched` after blur/submit (`planning-edit.page.ts` field-error wiring) —
the spec asserts the message appears on debounced keystroke, and clears when the value
becomes valid. Legacy reference: AngularJS `$asyncValidators` + the server `Validate*`
actions (legacy-oracle to pin exact server-validated rules per form at ship time).

**Reservations calendar (J-5).** (a) A `Reservations` entry is present in the primary nav
for a club admin and routes to `/reservations`. (b) The Day/Week toggle's selected button
uses the design's selected styling — selected = `--color-fg` background + `--color-bg` text
(screens-reservations.jsx:106-110), legible; the current build renders it "blacked out"
(contrast inverted). (c) In Week mode the pager steps by **weeks** not days, and the header
label shows the week's start–end date range (design subtitle "Week 21 · 21–27 May 2026 ·
LSZF", screens-reservations.jsx:61) — `reservations-prev-week`/`-next-week` already exist;
the assertion is the label + step granularity follow the active view.

**Planning-day read-only + edit-mode (J-6).** (a) Opening a planning-day in read-only mode
renders every field disabled/read-only — assert an input is non-editable, not just that Save
is absent. (b) A read-only view exposes an **Edit** button that flips the form to edit mode
(fields become editable, Save returns). (c) From an edit planning-day, opening a reservation
from the inline list (`<af-reservation-row>`, J-6 T-08b) then pressing **Cancel** returns to
the planning-day edit form — not `/reservations`. The reservation-edit cancel handler must
honor a return-url/referrer passed by the planning inline list (today it hardcodes nav to
`/reservations`).

**Cross-cutting.** (a) Date inputs + display render **DD.MM.YYYY** (`<af-date-picker>` +
date display pipes). (b) Dev seed gives clubadmin1's club ≥1 row per aggregate (Persons was
empty) — assert a list that was empty now shows a row. (c) clubadmin1 opens the Users menu
and the list loads (no 400 — diagnose the users list query/authz for clubadmin1's role). (d)
clubadmin1 does **not** see a `Clubs` nav entry (role-gated out).

## Notes

**One-screen rule — deliberate exception.** Like J-5 (calendar+edit) and J-6 (list+edit+
setup), this is a cohesive hardening of an already-shipped feature *family*, not a fresh
multi-screen process. The spine is the shared **edit-form validation** capability; the
reservations/planning/nav fixes are demonstrated on the same run. If `/do-ship` finds the
inline-validation feature alone is a full journey, it may split the pure-bug riders back out
to ride a later same-surface gate — but the operator's intent (2026-06-07) is to fix these
J-5/J-6 rough edges now, with gate + gallery proof, rather than let them sit as riders with
no near-term same-surface journey.

**Likely task seams** (non-binding, for `/do-ship` — seam-granular):
- `shared/ui/molecules/field-errors/` + a shared debounced-validation directive/util — the inline-while-typing renderer + 200ms debounce (one molecule + one util).
- Server validate endpoint: one `…/validate` resource per form that needs server rules (reservation-overlap, planning-day uniqueness) — name the aggregate, not "the backend".
- `reservations/calendar/reservations-calendar.page.ts` — toggle selected-style (ADR 0024 tokens) + week-vs-day pager granularity + week-range label (one component).
- `planning/edit/planning-edit.page.ts` — read-only renders disabled fields + an Edit-mode toggle (one component).
- `reservations/edit/reservation-edit.page.ts` — Cancel honors a `returnUrl`/referrer query param (one component); planning inline list passes it.
- `shared/ui/organisms/af-date-picker/` + date display pipe — DD.MM.YYYY (one organism + the pipe).
- Nav role-gating: wherever the `af-nav-bar` `items()` list is assembled (the app shell) — add `Reservations`, gate `Clubs` by role/tenant (ADR 0008). `af-nav-bar.component.ts` itself just renders `items()`; the gating is upstream.
- Users list 400 for clubadmin1 — `features/users/` store/page query (likely a role/tenant param the clubadmin1 principal sends differently); diagnose at ship time, it may be backend.
- Dev seed: the V-series `*_dev_*_seed.sql` — **empirical full sweep, NOT analytical enumeration** (operator
  2026-06-07): boot the dev DB with the current seed, then run a `SELECT count(*) … GROUP BY club/owner` per
  aggregate table for each testuser/club (one query loop over `information_schema` t_* tables or an explicit
  per-table sweep) — let the SELECTs report which (testuser × aggregate) cells are empty, then add seed rows
  for exactly those. Persons-for-clubadmin1 was the one symptom the operator saw; the truth of the rest comes
  from the counts, not a guessed list. DoD: re-run the count sweep → zero empty cells for any aggregate a
  testuser should own.

**Pending _BOYSCOUT riders touching this surface** (so `/do-ship` folds them into the ≤40%):
- *Cascade-delete asserted only indirectly* (J-6 T-16, "rides the next planning touch") — this IS a planning touch; add the assertion that a deleted day's assignments are excluded from reads.
- *Producer dedupe soft-delete-blind* (J-6 T-11b/T-16) — only if a task touches the PlanningDay producer SELECT; this journey nominally doesn't, so likely defer (note, don't force).
- Generic infra riders available for the ≤40% fill (operator/`/do-ship` choose): structural post-deploy proof-gallery guard, un-mask migration-ingest constraint in dev/test, CI fail-aggregate, per-journey gallery shot-presence guard.

**Sacred cows / open calls for ship-time `legacy-oracle`:** RESOLVED — see `J-6b-oracle.md`.
Key: legacy has no async server validators; the server-validate endpoint pre-checks AlpenFlight's
EXISTING J-5 overlap-409 + J-6 uniqueness constraints inline (no new rule). DD.MM.YYYY confirmed
hardcoded in legacy. Clubs-hide is a NEW operator decision (legacy shows it to all). Users-400 is
NOT authz (diagnose request-shape/BE). Day/Week calendar is greenfield (no legacy parity).

## Tasks

Behavior oracle: `J-6b-oracle.md` (worker input; pruned at §5). FE seam map baked into scopes below.

- [x] **T-01** — spec stub + scaffold the J-6b proof-gallery page. Author `e2e/tests/` J-6b spec skeleton (selectors + flow for: reservations nav+toggle+paging, planning readonly/edit, inline validation, date format, nav gating) with thin assertions; scaffold the per-journey gallery page linked from the persistent index. Calendar is greenfield (AlpenFlight-only shots); edit forms can pair legacy where a legacy ref exists.
- [x] **T-02** — scope the per-push gate to J-6b. Set the journey's `mock_test:`/`parity_test:` frontmatter so per-push runs ONLY J-6b specs heavy (real-idp) + prior journeys (J-5/J-6/…) mock-IdP. Infra exists (J-6 T-02b derive-filter) — just wire J-6b's frontmatter + confirm. **DONE:** `mock_test:` set to a Playwright regex alternation over J-6b's 3 spec stems (its specs span 3 feature dirs, so a single dir token would pull prior journeys' specs); the `Derive journey mock-e2e filter` step extended to recognise+validate a regex token by its alternation branches (single-path case unchanged, all fail-safes preserved). Verified locally: the derived filter resolves to exactly J-6b's 12 tests / 3 files, no prior-journey leakage. `parity_test:` (real-idp half) stays the mock spec → `Derive journey proof spec` correctly falls back to the J-0 Locations baseline (J-6b has no real-idp spec until T-17). actionlint-clean.
- [x] **T-03** — shared inline-validation infra. `<af-field-errors>` molecule + a shared util/directive renders per-field errors WHILE TYPING, debounced ~200ms (valueChanges debounce, not `updateOn:'blur'`/touched-only). Client-side trivial rules. One molecule + one util; reused by T-06/T-07 and adoptable by other forms later.
- [x] **T-04** — reservation `…/validate` endpoint (overlap pre-check). A non-mutating validate path on the Reservation aggregate that runs the EXISTING J-5 aircraft-slot overlap check (the save-time 409) and returns a field-level result. One backend endpoint + reservations aggregate validate method. NO new rule. **DONE:** `POST /api/v1/aircraft-reservations/validate` (`@ReadOnlyQuery`, `isAuthenticated()`, tenant-scoped) → 200 `{valid, field, message}`. Reuses the SAME `existsActiveConflict` probe the save path calls; effective-span normalisation extracted onto the aggregate as `AircraftReservation.effectiveSpan(start,end,isAllDay)` (shared by save + validate, ADR 0022). Self-exclusion via `excludeReservationId`. 4 ITs (no-overlap→valid; overlap→invalid+`field:"start"`+message & no row persisted; edit-own-slot self-excluded→valid; other-club booking→valid/tenant-scoped). `./gradlew check` green; OpenAPI snapshot regenerated.
- [x] **T-05** — planning-day `…/validate` endpoint (uniqueness pre-check). Non-mutating validate path running the EXISTING J-6 (club,date,location) `ux_pln_club_date_loc` check, field-level result. One backend endpoint + planning aggregate validate method. NO new rule. **DONE:** `POST /api/v1/planning-days/validate` (`@ReadOnlyQuery`, `isAuthenticated()`, tenant-scoped) → 200 `{valid, field, message}`. Reuses the SAME `(club,date,location)` dedup the save path enforces via a new self-excluding repo probe `existsActiveForDayExcluding(date, location, excludeId)` (sibling of the bulk-expand `existsActiveForDay`, so the save path keeps its `ux_pln_club_date_loc` constraint AND the rule is checkable pre-save); offending field is `planningDate`. `excludePlanningDayId` self-excludes an edit. Envelope mirrors T-04's `{valid,field,message}` exactly so the FE (T-07) reuses one mapping. 4 ITs (no-duplicate→valid; existing (date,location)→invalid+`field:"planningDate"`+message & nothing persisted; edit-own-day self-excluded→valid; other-club same (date,location)→valid/tenant-scoped). `./gradlew check` green (cpdRatchet + PMD + arch-guards + OpenApiSnapshotIT); OpenAPI snapshot regenerated.
- [x] **T-06** — reservation-edit adopts inline validation. Client `required` on Date/Type/Pilot/Aircraft/Location + conditional Second-Crew (per oracle) while-typing via T-03; async overlap-validate via T-04 surfaced inline. `reservations/edit/reservation-edit.page.ts`. **DONE:** all required fields now render via the shared debounced `liveFieldErrors` (replacing the `touched`-only wiring); the STORE owns the T-04 `…/validate` call (`validateOverlap` rxMethod, debounced ~200ms, `excludeReservationId` on edit) and the result surfaces inline on the slot via `liveFieldErrors`'s `asyncErrors$` merge (start-time control) + a dedicated `reservation-start-server-error` element; submit blocked while invalid OR on an overlap conflict (save-path 409 stays the backstop). New testids: `reservation-start-validating`, `reservation-start-server-error`. Regenerated the orval client (confirmed method `validateAircraftReservationOverlap`). 3 store/logic unit specs (validate→inline error/clear, `overlapResultToErrors`, `overlapProbe`, `secondCrewRequiredFor`). `@partial`: conditional Second-Crew is wired (derived signal + dynamic validator) but evaluates `false` today — the driving flags (`IsInstructorRequired`/…/`NrOfSeats`) are NOT on the `AircraftReservationTypeListItem`/`AircraftPickerItem` picker projections; exposing them is a backend-projection seam outside T-06's one-FE-seam scope (flips on for free once the projections carry the flags). lint + tsc + ng test (28) + i18n parity + prod AOT build all green.
- [x] **T-07** — planning-edit adopts inline validation. Client `required` on Date/Location while-typing via T-03; async uniqueness-validate via T-05 inline. `planning/edit/planning-edit.page.ts`. **DONE:** Date + Location render their `required` messages inline WHILE TYPING via the shared T-03 `liveFieldErrors` (debounced ~200ms), replacing the touched-only `[errors]="ctl.touched ? …"` wiring; towing/operator/instructor stay OPTIONAL (oracle). The STORE owns the T-05 `…/validate` call (`validateUniqueness` rxMethod, debounced ~200ms, `excludePlanningDayId` on edit, no HTTP in the component) keyed on the same debounced (date,location) signal the reservations panel uses; the result surfaces inline on the date field via `liveFieldErrors`'s `asyncErrors$` merge (`store.uniquenessErrors`) plus a dedicated `planning-date-server-error` element, and blocks submit while a duplicate stands (the save-path 409 stays the backstop). Testids: `planning-date-validating`, `planning-date-server-error` (align the T-01 stub exactly). Reused T-06's regenerated orval `validatePlanningDayUniqueness` (no re-emit; zero generated drift). 5 logic/store specs (`uniquenessProbe` create/edit/incomplete, `uniquenessResultToErrors`, `validateUniqueness` duplicate/valid/error-clear, `clearUniquenessValidation`). lint + tsc + ng test (25) + i18n guards (32) + prod AOT build all green.
- [x] **T-08** — reservations calendar: toggle + pager + label (items #2,#3). `reservations/calendar/reservations-calendar.page.ts`: (a) reproduce the "blacked-out" selected toggle on the deployed page, align selected styling to ADR-0024/design (legible); (b) pager granularity follows view — DAY view steps ±1 day, WEEK view steps ±7 (today `shiftWeek` always steps 7); (c) period label follows view + formats DD.MM.YYYY (day) / DD.MM.YYYY – DD.MM.YYYY (week) (today `weekSubtitle` is locale `3 Jan – 9 Jan`). One component. **DONE:** (a) toggle buttons carry `[attr.data-selected]="view()===v"` (the T-01 spec hook); selected keeps the design fg-bg + bg-text (slate-900 ground + white text per screens-reservations.jsx:106-110) with a documented comment + a hover tint on unselected — legible, intentional active state. (b) `shiftWeek`→`shiftPeriod(delta)` steps `delta*stepDaysForView(view)` (±1 day / ±7 week); the single prev/next pair switches testid+aria by view (`reservations-prev-day`/`-next-day` in day view, `reservations-prev-week`/`-next-week` in week view, matching the T-01 stub). (c) `weekSubtitle`→`periodLabel` computed off the new pure `periodLabel(view, iso)` helper → `DD.MM.YYYY` (day) / `DD.MM.YYYY – DD.MM.YYYY` (week); rendered in the page-header subtitle AND a dedicated `reservations-period-label` span. New pure helpers `stepDaysForView` + `formatDdMmYyyy` + `periodLabel` in `reservations-calendar.model.ts` (+3 unit specs). i18n: added `calendar.prevDay`/`nextDay` (de/fr/it/en). The J-5 `selectCalendarDay` helper (was coupled to an always-present week pager) updated to drive week-nav through week view then return to day view. preflight:web green (111 passed; the two prior J-5 day-picker reds fixed). Testids/attrs for T-17: `[data-selected]` on `reservations-view-{day,week}`, `reservations-{prev,next}-day` (day view), `reservations-{prev,next}-week` (week view), `reservations-period-label`.
- [x] **T-09** — planning read-only + Edit toggle (items #10,#11). `planning/edit/planning-edit.page.ts`: the operator saw an editable read-only form though `form.disable()` runs on `isView()` — reproduce + find why (route never enters view-mode? disabled styling invisible? a control ignoring `disable`?); ensure ALL fields are visibly read-only AND add an "Edit" affordance to switch view→edit. One component. **DONE:** root cause was **the CVAs ignoring `form.disable()`** — `af-input` + `af-select` had **no-op `setDisabledState()`** (only honoured their `[disabled]` *input*, which the planning template never binds), so `form.disable()` marked controls DISABLED but the native `<input>`/`<nz-select>` stayed editable (operator #10: "only hides the save button" — Save is `!isView()`-gated so it DID hide; the route + `isView()` effect always worked). The `:id/view` route + the list's view-links were already correct — NOT a routing gap. Fix: both atoms now wire `setDisabledState(isDisabled)` → an internal `cvaDisabled` signal OR'd with the `[disabled]` input (fixes EVERY reactive form, not just planning). #11: VIEW mode now renders an **Edit** button (`planning-edit-toggle`, gated `canMutate()`) that navigates to the same day's `/edit` route — `:mode=edit` flips `isView()` false, the disable effect re-enables the form, Save returns; navigation keeps `isView()`/`canMutate()` on the single route source (no local mode signal). T-07's `liveFieldErrors`/`validateUniqueness` untouched — the view→edit reload rebuilds the form fresh, so validation re-wires with default `emitEvent` (no broken disabled-control valueChanges seam). New pure helper `planningEditLink(id)` (+2 unit specs). Testids for T-17: `planning-edit-toggle`. i18n: added `planning.form.edit` (de/fr/it/en). `pnpm preflight:web` green (111 e2e passed / 14 fixme; ng test 428).
- [x] **T-10** — reservation Cancel returns to planning day (item #9). `reservations/edit/reservation-edit.page.ts` Cancel honors a `returnUrl` query param (today hardcodes `/reservations`); the planning inline `<af-reservation-row>` `[openLink]` passes `returnUrl=/planning/:id/edit`. One component + the call-site. **DONE:** Cancel is now `onCancel()` (replacing the inline `router.navigateByUrl('/reservations')`) → reads the `returnUrl` query param, sanitizes it to an internal same-app absolute path (rejects external/protocol-relative/scheme/non-rooted → no open-redirect), navigates there, else falls back to `/reservations` (standalone-edit behaviour preserved). The Cancel button got `data-testid="reservation-edit-cancel"` (matches the T-01 stub). `af-reservation-row` gained an optional `openQueryParams` input (RouterLink `[queryParams]`); `planning-edit.page.ts` passes `{ returnUrl: <current planning-day url> }` via a `reservationReturnParams()` computed off the new pure `planningDayLink(id, mode)` helper — so Cancel returns to the EXACT page (view OR edit) the user opened the reservation from. Pure helpers `sanitizeReturnUrl` + `planningDayLink` unit-tested (Vitest, no TestBed): 7 new assertions (valid internal path used; absent/empty → fallback; external/`//host`/`javascript:`/non-rooted rejected; day-url for both modes; null in create). Updated the J-6 `planning-crud.spec.ts` inline-list href assertion to expect the new `?returnUrl=…` query (call-site behaviour change). Validated the full open→cancel→return flow with a throwaway full-mock e2e (passed, then deleted). T-06's inline-validation + T-09's read-only/edit-mode untouched. preflight:web: lint + tsc + build + ng test (433, +5) green; the one e2e red was the stale J-6 href assertion, now fixed + re-run green.
- [x] **T-11** — nav gating (items #1,#8). `app.component.ts`: add `{ path:'/reservations', label:'Reservations', icon:'calendar' }` to `TENANT_SECTIONS`; make `/clubs` sysadmin-only (remove from the non-sysadmin `base` so club-admins don't see it). One component. **DONE:** extracted the inline section consts + the role→sections switch out of `app.component.ts` into a pure `nav-sections.ts` helper (`navSectionsFor({isSystemAdmin,isClubAdmin})`) so the per-role matrix is unit-testable without a TestBed; the component's `sections` computed now just feeds the two `SessionStore` flags into it. Added `{ path:'/reservations', label:'Reservations', icon:'calendar' }` to `TENANT_SECTIONS` (between Flights and Planning); moved `/clubs` from the old `CROSS_CUTTING_SECTIONS` (shown to all) into `SYS_ADMIN_SECTIONS` (returned ONLY on the sysadmin branch). `calendar` icon already registered (`LucideCalendar`). Role predicates are role-string based (`isSystemAdmin`=`SYSTEM_ADMINISTRATOR`, `isClubAdmin`=`CLUB_ADMINISTRATOR`), so clubadmin1 (CLUB_ADMINISTRATOR, not SYSTEM) correctly drops Clubs at T-17. Matrix — sysadmin: `[/clubs]` only; club-admin: tenant (Flights/Reservations/Planning/Aircraft/Locations/Persons)+Users, NO Clubs; regular user: tenant, NO Users, NO Clubs. 4 Vitest specs (pure, no TestBed); `pnpm preflight:web` green (111 e2e passed / 14 skipped), lint + tsc + prettier clean.
- [x] **T-12** — date format DD.MM.YYYY (item #5). `shared/ui/organisms/af-date-picker` set `dd.MM.yyyy` on single + range pickers; align date display pipes to `dd.MM.yyyy` across touched templates. One organism + pipes. **DONE:** `af-date-picker` now applies `[nzFormat]="format()"` to BOTH `nz-date-picker` + `nz-range-picker`; the `format` input defaults to a new exported `DEFAULT_DATE_FORMAT = 'dd.MM.yyyy'`, so every consumer (incl. the flights range picker, no per-call config) renders DD.MM.YYYY — overridable for the rare exception. Format is display-only: `date-value-bridge` round-trip untouched, model stays `Date` (bridge spec still green). Display audit: most user-facing date renders already emit dd.MM.yyyy (flights-list `formatLegacyDate`, reservations-calendar `formatDdMmYyyy`+`EEEE, dd.MM.yyyy`); the one off-format user-facing date column was the start-page last-flight date (`Intl medium`, locale "May 21, 2026") → normalized to dd.MM.yyyy. Centralized a shared `@shared/util/date` (`formatDdMmYyyy` + `formatIsoDateDdMmYyyy`, pure, the UTC-midnight-safe string reorder) for new consumers; left the 2 pre-existing feature-local copies (they already emit the same shape; re-pointing flights-list is churn on T-13's surface). Date-TIME displays left intact (migrate-handshake token expiry; reservations HH:mm; start-page long greeting weekday header). 8 Vitest specs (format-date.spec.ts) + updated the start e2e date assertion (May 21, 2026 → 21.05.2026). preflight:web ALL GREEN (111 passed). Files: af-date-picker.component.ts, its index.ts, shared/util/date/{format-date.ts,format-date.spec.ts,index.ts}, start.page.ts, start.spec.ts (7). Left a clean base for T-13 (no range model/bridge refactor).
- [x] **T-13** — flights-list date-range picker fix (item #12, NEW). `features/flights/list/` — the date-range picker doesn't filter; diagnose (likely the `date-value-bridge` range model / af-date-picker range mode) + fix so a from–to range filters the list. Shares the af-date-picker organism with T-12 — order after it. One component/seam. **DONE:** ROOT CAUSE was an **asymmetric ISO↔Date round-trip in the flights-list page** (NOT the store, NOT the bridge, NOT af-date-picker — all of those were correct). `dateRangeValue()` parsed the store's `from`/`to` ISO date-only strings with `new Date('YYYY-MM-DD')`, which is **UTC midnight**; the write path (`toIsoDate`) used **local** calendar fields. West of UTC the picker re-displayed the picked range a day EARLY and the picker model permanently drifted out of sync with the store's `from`/`to` (reproduced via a throwaway e2e with `timezoneId: America/Los_Angeles`: picked 6th/12th → store correctly sent `from=2026-06-06`/`to=2026-06-12` but the picker re-rendered `05.06.2026`/`11.06.2026`). In a positive-offset zone (CH) the same-day parse masks the drift; the latent defect bites any negative-offset locale and leaves the model inconsistent. Fix: two pure TZ-symmetric helpers in `@shared/util/date` — `localDateFromIso` (parses to LOCAL midnight via `(y, m-1, d)`) + `isoDateFromLocal` (formats a local Date back to `YYYY-MM-DD`); `dateRangeValue()` now uses `localDateFromIso`, `onDateRangeChange` uses `isoDateFromLocal` (removed the page-local `toIsoDate`). Model stays `[Date, Date]`; zoneless-safe (no setTimeout); component stays HTTP-free (store still owns `listFlights` via `setDateRange`→`loadPage`). Testid `flights-date-range` already on the picker (T-17 hook) — confirmed, untouched. Tests: 5 new Vitest specs for the helpers (LOCAL-midnight parse, ISO→Date→ISO round-trip stable across `2026-01-01`/`-06-06`/`-12-31`/`-03-29`, malformed→null) + a store spec for cleared-range→no date params→unfiltered refetch (range→params was already covered). `ng test` 449 green; `preflight:web` ALL GREEN (build + tsc + lint + 111 e2e). 5 files. Post-fix the LA-TZ repro re-displayed 06.06/12.06 (matches the pick); repro deleted.
- [x] **T-14** — dev-seed empirical sweep (item #6). Boot dev DB with current seed, run a `SELECT count(*)` sweep per `t_*` aggregate per testuser/club, seed exactly the empty cells (Persons-for-clubadmin1 was the symptom). New V-series `*_dev_*_seed.sql`. DoD: re-run sweep → zero empty cells. **DONE:** swept a CLEAN Flyway-only DB (the showcase harness is `@Profile("showcase")` and does NOT run in plain dev/CI, so the operator's symptom is the clean-seed truth) — every testuser (clubadmin1..4/pilot1/pilot-empty1) binds to seed-club-1, so the matrix is each `t_*` club-scoped aggregate × seed-club-1. BEFORE (clean, empty cells with a user-facing per-club LIST): `t_member_state`, `t_person_club` (the Persons-list symptom), `t_aircraft`, `t_flight_type`, `t_article`, `t_flight`, `t_aircraft_reservation`. `V36__dev_aggregate_list_seed.sql` seeds exactly one realistic row each (reusing V34 locations/persons + V31 reservation type + V2/V3 reference data; seed-band `019e30c3-%` ids so controller-IT pre-clean guards spare them). AFTER: all seven now ≥1 (member_state 2, person_club 3, aircraft 1, flight_type 1, article 1, flight 1, reservation 1) → zero empty user-facing cells. Legitimately-empty (NO per-club list screen — sysadmin/global/future-journey) left empty + documented in the migration header: deliveries/delivery-items/delivery-creation-test(_item)/club_delivery_number_counter, accounting_rule_filter, club_extension/extension_value/email_template, person_category(_assignment), deployment. `./gradlew check` GREEN (V36 boots in the Testcontainers lifecycle; Flyway/arch/redaction/native-sql guards pass; no IT count-assertion broke — verified the count-asserting ITs either DELETE-and-rebuild their table for seed-club-1 or assert by finding their own row).
- [x] **T-15** — users-list 400 for club-admin (item #7). `features/users/` — diagnose the 400 for clubadmin1 (NOT authz per oracle; request-shape or BE query); fix so the list loads. May span FE param + backend; escalate only if a contract change is needed. **DONE (diagnosed; AC locked):** the `GET /api/v1/users` backend cannot emit a 400 — proven by a new full-stack IT (`UsersListControllerIT`): well-formed club-admin + reachable directory → **200**; missing/unresolvable tenant → **404** (`UserNotFoundException`); KC failure → **502** (`UserDirectoryException` wraps every `HttpStatusCodeException`). The FE sends a clean param-less `GET` (orval `listUsers()`, no params; OpenAPI snapshot declares zero params + only a 200) and `secureRoutes:['/api/v1/']` attaches the bearer, so no FE request-shape/401 path either. The REAL fragility: the realm-export gives dev club-admins a SYMBOLIC `clubId` attribute (`"club-1"`, non-UUID), so `JitUserMaterializationFilter.shouldMaterialise` SKIPS them (non-UUID `clubId`) and tenant resolution falls through to the `keycloak_sub`→`t_user` lookup — i.e. the Users list (and every tenant-scoped read) for clubadmin1 binds ONLY via the seeded `t_user` row. Before that row existed (pre-V8 / the operator's field-test window) the list returned **404 "No tenant context available"** (the seed file V8 header documents exactly this) — the menu-broken symptom; T-14's V8 seed already restored the row. Regression IT drives the EXACT dev clubadmin1 principal (symbolic `clubId="club-1"` claim + seeded row) → **200 tenant-scoped, no cross-tenant leak**, locking AC #13 for T-17. No FE change (store already covers loadAll happy + error paths). `./gradlew check` green (arch-guards + cpdRatchet + PMD + OpenApiSnapshotIT). NOTE: the literal "400" was NOT reproducible against `integration/J-6b` (backend never emits 400 here); if the operator still sees a 400 at the §4 real-stack gate, capture the response body there — but on current evidence the menu-broken bug is the missing-tenant-row case, already fixed.
- [x] **T-16** — planning cascade-delete assertion (rider, _BOYSCOUT J-6 T-16). Assert a deleted planning-day's `t_planning_day_assignment` rows are excluded from reads (the [key-error] delete proves the day leaves the list but never that assignments are gone). Spec/IT assertion. **DONE:** read layer ALREADY excludes them correctly (no leak — the rider's predicted bug did not materialise). New IT `PlanningDaysControllerIT.delete_excludesTheDaysAssignmentsFromEveryRead`: create a day with all 3 crew slots (→ 3 child `t_planning_day_assignment` rows), soft-delete it, then assert GET → 404, the `overview/future` list omits the day, and re-creating the same (club,date,location) yields a FRESH day with its OWN 3 assignments exposing only its own crew. Root mechanism locked: every read query filters `deleted_on IS NULL` at the PARENT (`JpaPlanningDayRepository.findActiveById` / `FUTURE_SELECT`), so a soft-deleted day is never loaded and its children — which physically remain (`countAssignments(id)==3` post-delete, since soft-delete means the V4 `ON DELETE CASCADE` never fires) — are unreachable through ANY read path. Test-only, one IT (no query fix needed). `./gradlew check` green.
- [x] **T-18** — conditional second-crew required (reservation parity, T-06 follow-on). T-06 wired a conditional `required` validator for Second-Crew but it's dormant: the driving flags (`IsInstructorRequired || ObserverPilotOrInstructorRequired || IsPassengerRequired` on the reservation-type, `NrOfSeats>1` on the aircraft) aren't on the picker projections. Add the flags to the `AircraftReservationTypeListItem` + `AircraftPickerItem` backend projections (+ regen client) so the validator goes live. One backend projection seam + the FE consuming the new fields. Runs before T-17. **DONE:** exposed `instructorRequired` on `AircraftReservationTypeListItem` + `nrOfSeats` on `AircraftPickerItem` — sourced from existing aggregate fields (`AircraftReservationType.is_instructor_required`, `Aircraft.nr_of_seats`), NO new columns. **AlpenFlight COLLAPSES legacy's three type-flags** (`IsInstructorRequired`/`ObserverPilotOrInstructorRequired`/`IsPassengerRequired`) into the single `is_instructor_required` column — only that flag is modeled, so the type-lane exposes that one collapsed boolean (the aircraft `NrOfSeats>1` lane is the second driver). Touched both projection records + their repo rows (`TypeListItem`/`PickerRow`) + JPA constructor-expression queries + mappers. Regenerated the OpenAPI snapshot + orval client. FE: refactored `secondCrewRequiredFor` from a `()=>false` stub into a pure `(type, aircraft) => boolean` driven by the live selections (component `selectedType`/`selectedAircraft` computeds off the form value + store pickers); T-06's dynamic `required` validator + the field SHOW condition now flip on the real flags. BE: extended the picker IT (asserts `nrOfSeats==2`) + added a reservation-type-list IT (instructor-required type → `instructorRequired:true`, solo → false). FE: 5 logic specs for `secondCrewRequiredFor` (instructor-type→true, multi-seat→true, plain+single-seat→false, nulls→false, absent-seats→false). `./gradlew check` green (OpenApiSnapshotIT + cpdRatchet + PMD + arch-guards); web lint + tsc + build + `ng test` (453) green. 9 files (2 projections + 2 repo rows + 2 queries + 2 mappers + FE page; +2 FE specs + 2 BE ITs + snapshot/client regen).
- [x] **T-17** — thicken the J-6b spec to full real assertions (all 14 ACs) + finalize gallery pairing + capture/commit any legacy reference shots for the edit forms. Final task (runs LAST, after T-18). **DONE:** un-fixme'd + thickened all 3 mock inner-loop specs to full real assertions (13 tests, mock-auth green locally): calendar toggle selected-style (luminance: dark ground + light text) + view-aware pager (±1 day / ±7 week) + DD.MM.YYYY label; planning read-only ALL-fields-disabled + Edit toggle; reservation Cancel returnUrl→planning; debounced inline validation (required while-typing, clears on valid, blocks submit) + async server-validate (planning uniqueness + reservation overlap, REAL `{valid,field,message}` envelope) inline. Authored the real-idp sibling `tests/real-idp/reservations-planning-hardening.spec.ts` (the heavy-chain `parity_test`) driving REAL principals: clubadmin1 (CLUB_ADMINISTRATOR) sees Reservations + NO Clubs (AC4/AC14), sysadmin DOES see Clubs (role-gate positive control), clubadmin1 Users list renders 200 (AC13), V36 seed gives ≥1 row per aggregate (AC12), the greenfield Day/Week calendar renders legibly + DD.MM.YYYY (AC5/6/10), the planning read-only/edit form (AC7/8). Repointed `parity_test:` frontmatter → that spec (T-02 fallback retired). **APP FIX (T-08 gap T-17 surfaced):** the selected toggle was STILL dark-on-dark ("blacked-out" #2) — `text-white` lost the cascade to ng-zorro's UNLAYERED global button reset; fixed with `!text-white`/`!text-slate-500` important utilities (they win over the unlayered antd rule). **J-5 coupling fix:** the real-idp `reservations-migration-parity.spec.ts` `selectCalendarDay` helper shared the pre-T-08 week-pager assumption (clicked `reservations-next-week` straight off day view, where that testid no longer exists) — fixed the same way the mock helper was (switch to week view, step, return to day view). Gallery: calendar Day+Week = greenfield AlpenFlight-only shots; planning-edit form pairs against the committed `planning/form.png`; reservation-edit form AlpenFlight-only/deferred (no local legacy stack to capture `reservations/form.png`; documented in legacy-reference/README.md). Mock inner-loop + J-5/J-6 regression (32 tests) green LOCALLY; lint + tsc + prettier clean. Real-idp heavy chain runs at the §4 gate (stack not booted locally — gate's job).
- [ ] **T-19** — CI derive-step robustness (gate-revealed). Both ci.yml derive steps (`Derive journey proof spec` + `Derive journey mock-e2e filter`) select the journey file via `ls docs/.../${jid}-*.md | head -1`, which alphabetically picks the sibling scratch `J-6b-oracle.md` over the journey file → the proof step's `grep -m1 '^parity_test:'` finds nothing and, under `set -euo pipefail` (no `|| true`), exits 1 → `detect changes` job RED, whole gate blocked. Fix: select by frontmatter `id:` (`grep -lE "^id:[[:space:]]*${jid}\\b"`) not `head -1`, in BOTH steps; add `|| true` defense to the proof-step grep. actionlint-clean; simulate both derive paths locally to confirm J-6b resolves to its real-idp spec + the 3-spec mock regex (not baseline/full-suite).

**Riders cleared from `_BOYSCOUT.md` by this journey:** cascade-delete assertion (→T-16). Producer-dedupe-soft-delete-blind stays (no producer-SELECT touch here). Generic infra riders (gallery guard, un-mask constraint, CI fail-aggregate, shot-presence guard) NOT pulled — defer to a journey that naturally touches that infra.
