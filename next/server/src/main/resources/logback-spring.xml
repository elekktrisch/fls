<?xml version="1.0" encoding="UTF-8"?>
<!--
  S-001 stub. S-031 swaps in JSON encoder. MDC keys reserved here so the
  pattern is set: request_id, tenant_id, actor_user_id are populated by
  S-022 (tenant resolver) and S-027 (audit filter).
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOG_PATTERN"
              value="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} ${LOG_LEVEL_PATTERN:-%5p} [%X{request_id:-},%X{tenant_id:-},%X{actor_user_id:-}] %-40.40logger{39} : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
