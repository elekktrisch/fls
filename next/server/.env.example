# Copy to .env (gitignored) and fill in for local dev. Spring Boot does not
# load .env natively; either source it from your shell or use direnv:
#     set -a; source .env; set +a; ./gradlew bootRun
# Future stories (S-009 datasource, S-019 Keycloak, etc.) add real keys.

# SPRING_PROFILES_ACTIVE=dev
# SERVER_PORT=8080
