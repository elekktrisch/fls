{
  "$comment": [
    "J-7 T-12 — the SHOTS-PRESENT contract (structural, operator grill J-5/J-6 retro).",
    "Per-journey-under-work, the paired legacy<->AlpenFlight views the proof gallery",
    "page is EXPECTED to carry. The pre-deploy `[shots-present]` guard in",
    "proof-gallery-links.spec.ts reads this + the staged screenshots.json and FAILS",
    "if an `expected` <side>:<view> is absent from the stage — replacing the old",
    "silent add_shot/add_pair drop (a partial-red capture could quietly ship a thin",
    "page while the generator stayed green). Mirrors the MapperBindingContract",
    "KNOWN_UNBOUND allowlist pattern: a shot that is legitimately deferred goes in",
    "`pending` (tolerated-absent, surfaced) until it lands; an `expected` shot is",
    "load-bearing and its absence reds the journey's proof job.",
    "",
    "Keys: <side>:<view>, side in {legacy, alpenflight}. The view + side MUST match",
    "the journey's add_shot/add_pair declarations in ci.yml (per-push) and",
    "alpenflight-proof-fanout.yml (nightly) so the guard checks the SAME entries the",
    "gallery renders. Greenfield AlpenFlight-only views have no `legacy:` peer.",
    "A journey absent from this file is NOT guarded (no enforced shots) — add a",
    "journey here when its proof page should carry paired shots."
  ],
  "journeys": {
    "J-1": {
      "expected": ["legacy:list", "legacy:form", "alpenflight:list", "alpenflight:form"],
      "pending": []
    },
    "J-2": {
      "expected": [
        "legacy:list",
        "legacy:form",
        "legacy:motor",
        "alpenflight:list",
        "alpenflight:form",
        "alpenflight:motor"
      ],
      "pending": []
    },
    "J-5": {
      "expected": [
        "legacy:list",
        "legacy:form",
        "alpenflight:list",
        "alpenflight:form",
        "alpenflight:scheduler"
      ],
      "pending": ["legacy:scheduler"]
    },
    "J-6": {
      "expected": [
        "legacy:list",
        "legacy:form",
        "legacy:setup",
        "alpenflight:list",
        "alpenflight:form",
        "alpenflight:setup"
      ],
      "pending": []
    },
    "J-7": {
      "$note": [
        "T-15 thickened flight-reports-parity.spec.ts to write the three full-page",
        "AlpenFlight parity PNGs (alpenflight-flightreports-{picker,result,custom}.png",
        "via testInfo.outputDir, each behind a J-2-T-43-style self-guard), so the AF",
        "three are EXPECTED — a missing AF shot REDS rather than silently passing.",
        "T-22 wired the legacy reporting-parity-J7 spec into the fanout (it now runs",
        "against the live legacy stack + stages legacy-flightreports-{picker,result,",
        "custom}.png), so the three legacy:* shots flip PENDING -> EXPECTED with",
        "producedBy:fanout — enforced in the fanout context (where they are produced),",
        "tolerated in the per-push proof context (the committed e2e/legacy-reference/",
        "reporting/*.png are captured-once from that fanout run, then committed).",
        "",
        "T-16 — PER-CONTEXT producing seam. J-7 is READ-SIDE: its AF reporting shots",
        "are written by the real-idp flight-reports-parity.spec.ts, which runs in the",
        "PER-PUSH proof job (ci.yml alpenflight-proof) — NOT in the fanout (the fanout",
        "runs only the migration parity specs J-0c/J-1/J-2/J-5/planning + the legacy",
        "capture, so it never produces the AF reporting shots). So the AF three are",
        "EXPECTED only in the `proof` context; in the `fanout` context they are",
        "produced-elsewhere → tolerated-absent (no false red). The guard keys on",
        "GALLERY_PROOF_CONTEXT: an `expected` shot is enforced only when its",
        "`producedBy` context matches the current one (a shot with no producedBy tag",
        "is enforced in BOTH contexts — the J-1..J-6 migration journeys, whose AF",
        "shots the fanout DOES produce). A real silent drop still reds: the AF three",
        "are guarded in the proof context where they are actually produced."
      ],
      "expected": [
        "alpenflight:picker",
        "alpenflight:result",
        "alpenflight:custom",
        "legacy:picker",
        "legacy:result",
        "legacy:custom"
      ],
      "producedBy": {
        "alpenflight:picker": "proof",
        "alpenflight:result": "proof",
        "alpenflight:custom": "proof",
        "legacy:picker": "fanout",
        "legacy:result": "fanout",
        "legacy:custom": "fanout"
      },
      "pending": []
    },
    "J-26": {
      "$note": [
        "Hardening sprint (no new screen, no legacy peer: the shots are",
        "AlpenFlight-only hardening evidence). The two views are the real-idp",
        "hardening-J26.spec.ts parity PNGs (membership round-trip + duplicate-",
        "FlightCode inline 409), written to testInfo.outputDir.",
        "",
        "T-27 thickened the spec from T-01 fixme stubs to full real assertions",
        "(≥1 active real-idp test) and flipped these PENDING -> EXPECTED with",
        "producedBy: proof — the J-7 read-side pattern: the real-idp spec that",
        "writes them runs in the PER-PUSH proof job (ci.yml alpenflight-proof,",
        "is_baseline=false once active), NOT the fanout. So they are EXPECTED",
        "(enforced) only in the `proof` context; in the `fanout` context they",
        "are produced-elsewhere -> tolerated-absent (no false red)."
      ],
      "expected": ["alpenflight:membership", "alpenflight:duplicate-code"],
      "producedBy": {
        "alpenflight:membership": "proof",
        "alpenflight:duplicate-code": "proof"
      },
      "pending": []
    },
    "J-8": {
      "$note": [
        "T-01 SCAFFOLD registered the J-8 (AccountingRuleFilter config screen)",
        "paired-shot contract; T-18 FLIPPED it pending -> expected. J-8 replaces",
        "legacy masterdata/accountingRules/, so it carries paired legacy<->",
        "AlpenFlight shots: `list` (the rule-filter table) + `form` (the filter-",
        "type-driven edit form).",
        "",
        "T-21 — PER-CONTEXT producing seam (the J-7 T-16 pattern). The two halves",
        "are NOT produced in the same context: the AlpenFlight halves",
        "(alpenflight:list/form) are written by the clean-seed real chain in",
        "`accounting-rules-parity.spec.ts` (step 6), which runs in BOTH the per-push",
        "`proof` job (ci.yml alpenflight-proof) AND the fanout — so they carry NO",
        "producedBy tag (enforced in both contexts). The legacy halves",
        "(legacy:list/form) are only producible in the FANOUT (the legacy flsweb",
        "stack runs only there, via `e2e/tests/accounting/accounting-parity-J8.spec.ts`",
        "in the fanout legacy-pw-accounting step) — so they carry `producedBy:",
        "fanout`: enforced in the `fanout` context (where they ARE produced),",
        "tolerated-absent in the per-push `proof` context (the committed legacy-",
        "reference accounting/*.png are captured-once from a fanout run, then",
        "committed). This is why the per-push proof page stages only the two AF",
        "halves (ci.yml `Stage per-push parity screenshots` J-8 block, add_af_only)",
        "while the fanout stages all four (alpenflight-proof-fanout.yml ~L1192).",
        "",
        "The [shots-present] guard keys on GALLERY_PROOF_CONTEXT: an `expected`",
        "shot is enforced only when its `producedBy` matches the current context",
        "(an untagged shot is enforced in BOTH). proof-context -> 2 AF shots",
        "required; fanout-context -> all 4 required. A real silent drop still reds",
        "in the context that produces the shot."
      ],
      "expected": ["legacy:list", "legacy:form", "alpenflight:list", "alpenflight:form"],
      "producedBy": {
        "legacy:list": "fanout",
        "legacy:form": "fanout"
      },
      "pending": []
    },
    "J-9": {
      "$note": [
        "T-01 SCAFFOLD — registers the J-9 (delivery-creation-test rules-engine",
        "harness) paired-shot contract so its per-journey proof page is declared",
        "from the journey's first task (the operator's glanceable window exists",
        "early). J-9 replaces legacy masterdata/deliveryCreationTests/, so it carries",
        "paired legacy<->AlpenFlight shots: `list` (the test table) + `form` (the",
        "pick-flight / dry-run / run+diff edit form). NONE are captured yet — the",
        "screen lands T-16/T-17/T-18 + the spec thickens T-19 — so all four are",
        "`pending` (tolerated-absent, surfaced by the [shots-present] guard) until",
        "T-19 flips them to `expected` once the parity spec + the fanout legacy",
        "capture produce them. An empty `expected` means the guard reds on NOTHING",
        "for J-9 today (scaffold-clean), the J-5 pending-placeholder pattern."
      ],
      "expected": [],
      "pending": ["legacy:list", "legacy:form", "alpenflight:list", "alpenflight:form"]
    }
  }
}
