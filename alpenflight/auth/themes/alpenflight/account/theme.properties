# keycloak.v3 is the Quarkus/React account console shipped as the
# default in Keycloak 26.x. The v2 account console still exists in the
# themes jar but is on the legacy track.

parent=keycloak.v3

locales=de,en,fr,it
