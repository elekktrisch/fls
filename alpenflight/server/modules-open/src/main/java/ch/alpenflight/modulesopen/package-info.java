/**
 * server/modules-open -- Apache-2.0, optional, both editions. Empty placeholder module: build
 * wiring only. The first open module (e.g. {@code flarm}) lands as a later slice.
 */
package ch.alpenflight.modulesopen;
