/**
 * server/modules-pro -- closed source, SaaS only. Empty placeholder module: build wiring only.
 * The first pro module (e.g. {@code charging}) lands as a later slice.
 */
package ch.alpenflight.modulespro;
